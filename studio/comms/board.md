# Tidewake board — current loop

Loop-local kanban. Mirrors GitHub issues; Project Manager keeps it in lockstep.
Keep `Doing` small (limit WIP). Cards: `#issue — short title — owner`.

> **Loop 0** bootstrapped v0: a playable sloop on an animated sea with islands, plus the
> auto-release pipeline and this studio structure. **Loop 1** will be planned from GitHub
> issues — Product Manager prioritises, Project Manager + Tech Lead refine before building.

## Backlog (top)
- Give sailing a *point*: a first destination/port with a payoff (PM to slice).
- Placeholder → real glTF sloop within budget (Graphic Designer + Tech Lead).
- Grow the playtest + a manual smoke checklist (QA).

## To do (next — set by Retro 11 / Loop 61: the modes now MATTER → CLOSE THE VERB LOOP, then fill the empty room)
- **Rumour loop CLOSED (Loop 62):** ~~#112 rumours that pay off + #111 chased-heading marker + #115 objectives.js~~ **SHIPPED** — chase a trade tip → map marker → arrival coin bounty + Ballad verse (**#111 + #115 CLOSED**; **#112 STAYS OPEN** as the umbrella for richer rewards). Next: **#127** trade live price-spike · **#128** disposition/bounty vessel (a 2nd rumour type) · sea/deed/rep kinds made chase-able.
- **#105 ashore-digest SHIPPED (Loop 63, CLOSED):** ~~"while you were ashore…" digest on Set Sail~~ — landfall→Set-Sail deltas (purse/hold, name/tier, a chased heading) read back on the HUD toast; offscreen world-sim (rival sacks a port / navy moves) filed as follow-ups.
- **#69 per-town music SHIPPED (Loop 64, CLOSED):** ~~per-town music identities (#94 phase 3)~~ — each port now sounds like itself (deterministic key/mode/timbre, transposition-first; the tavern drone re-keys to the nearest harbour, click-free crossfade). **TEMPO fixed this slice** (TL constraint). **Next:** richer per-town melodies/instrument sets + a dedicated docked cue + live tempo → **#129**.
- **Town/landfall DONE for now:** ~~#103 rumours~~ **SHIPPED (Loop 55, CLOSED)** · ~~#102 landfall ph1+2~~ **SHIPPED (Loops 56+58, CLOSED — crafted multi-sensory transition)** · ~~#104 port-remembers-you~~ **SHIPPED (Loop 59, CLOSED — save v9)** · ~~#107 mode-transition QA~~ **SHIPPED (Loop 57, CLOSED — + fixed a REAL perf-counter race)**. Breadth: ~~#71 islands~~ **SHIPPED (Loop 60, CLOSED)** · **#70 ocean whitecaps SHIPPED (Loop 61; #70 STAYS OPEN [STANDING-RULE])** — the sail-over curios (the issue's original slice 1) + the standing "1–2 sea-delight beats/loop" rule + #113/#114 remain.
- **#104b port-memory depth SHIPPED (Loop 65):** ~~last-deed-by-name~~ — a port now recalls your LAST notable deed BY NAME on return (combat win / rumour payoff attributed to the nearest port; "reputation precedes you" even before you next dock), plus the **"Your Harbour" home-port seed** (most-visited port greets you as its own; full investment system → **#118**). Save **v11**. **Next fillers:** #106 slice 1 (declarative registry) · **#110** fauna phase-2 dolphins · **#101** loose props (feed town) · #70 sail-over curios + #113/#114.
- **#94 — remaining phases** stay OPEN, but **phase-1 headline acceptance is MET** → NOT top-of-queue; #69 (phase 3) is promoted; real-audio-files slice PARKED on an asset decision.
- **[OWNER-DECISION / IMMINENT] #100 — arcade battle-modes** — HELD for the ~08:00 2026-06-28 Game-Designer owner brief (now ~07:00); infra #95 + seam #106-ph1 + QA #107 already built; battle is the one empty room. Do NOT promote above where the owner wants it — when it lands it likely jumps the top.
- **⏳ iOS re-test (batch):** #77 audio · #87 no-select · #93 ship's-wheel — all shipped best-effort, UNCONFIRMED; ask the owner to confirm all three in one pass.

## Doing
- _(empty)_

## Done
- Loop 65: #104b **Port-memory DEPTH — a port recalls your LAST notable deed BY NAME on return** (the #104 follow-up; #104 stays closed, depth commented on it). #104 remembered your FACE; #104b remembers what you DID there — a specific memory, not just a warm/cool tone. Pure `src/systems/port-memory.js` gains `deedPhrase(event)` (combat win / rumour payoff → a terse port-framed phrase, "the day you sent Captain Ironwake to the seabed in these waters"), `recordDeed(store, port, phrase)` (banks a deed WITHOUT a visit, so a deed in a port's waters is recalled even before you next dock — "your reputation precedes you", the #104 verb), and `recallLine` now LEADS with the named deed; the deed survives a save round-trip + later visits. Laid the **"Your Harbour" seed** — `homePort(store)` (most-visited past `HOME_MIN_VISITS`=3, name tie-break) so a frequented port greets you as its own (full claim-&-grow system tracked in **#118**). `main.js` thin: combat onEnd + rumour payoff attribute the deed to `ports.nearestPortName`; `onArrive` banks then reads the home flag; `tw.homePort` QA; **save v10→v11** (additive, fail-open). +16 TDD (`port-memory.test.mjs` → **595**); playtest ✓ zero errors; perf **29/130 draws · 89k/150k tris** (pure logic + text, zero new draws). Gallery `studio/qa/gallery/v0.0.20260628060652-port-remembers-deed.png` (first landfall at Saltpurse Quay — the harbourmaster already knows "the day you sent Captain Ironwake to the seabed in these waters"). CREATIVE SPARK (Game Designer): the harbourmaster doesn't just know your name, he knows your *story* — word reached here ahead of you. **Follow-ups (filed):** #118 full "Your Harbour" investment system · richer tone+deed blending (a deed recalled with a turned-pirate chill) · deeds for trades/duels-ashore (v0.0.20260628061042).
- Loop 64: #69 **Per-town music identity — each port sounds like ITSELF (transposition-first)** — **CLOSES #69** (from-owner P2, #94 phase 3: "if towns had different town musics that'd be awesome!"). Every harbour shared the one tavern bed; now each port gets a deterministic musical identity keyed to its name and the existing #94 PORT-layer tavern drone re-voices toward it. New **pure `src/town-theme.js`** — `townMusicIdentity(name)` → `{ root, mode, thirdInterval, tint, lowpassHz, leadType, tremoloHz, chordMidi }` via an FNV-1a hash spread across key/mode/tint palettes (deterministic, headless-safe on junk). `music.js` adds **`setTownTheme(identity)`** — transposes the 4 drone oscillators + re-aims the lowpass timbre tint + bellows-wheeze rate, all a click-free `setTargetAtTime` GLIDE (applied on start() if set before the engine is up). `main.js` re-keys to the **nearest** port (`ports.nearestPortName`, no-alloc 3-port scan), **debounced on change** — so the port layer already sounds like that town as it swells in on approach (the #94 auditory horizon now carries the town's identity), reverting on a fresh voyage. **TEMPO stays FIXED** (TL constraint — live tempo glitches the scheduler). `tw.townMusic` + `tw.townMusicFor(name)` QA. The 3 ports land distinct: **Saltpurse Quay = C minor/mellow · Barnacle Bottom = G major/woody · Gullet's Rest = E mixolydian/mellow**. +6 TDD (`town-theme.test.mjs` → **583**); playtest ✓ zero errors (audio inert headless); perf **32/130 draws · 90k/150k tris** (audio = pure CPU, zero new draws). Audio is not visible → no gallery shot. CREATIVE SPARK (Musician): you hear a town before you can see its jetty — the tavern wheeze drifts across the water already in that port's key, and sailing back greets you in the key it always had. **Follow-up (filed):** #129 — richer per-town melodies/instrument sets + a dedicated docked cue + live tempo (v0.0.20260628055104).
- Loop 63: #105 **"While you were ashore…" digest reads back your visit on Set Sail** — **CLOSES #105** (DL #4 / Retro 11 living-world legibility). Makes the mode spine's "world keeps living underneath" promise LEGIBLE: snapshot the delta-able world-state when TOWN takes the screen, then on **Set Sail** compose a short, in-character digest from the REAL deltas and show it on the shared HUD toast (replacing the generic "making sail" line). New **pure `src/systems/ashore-digest.js`** (`snapshotAshore` + `composeAshoreDigest` → `{title, lines}`; salience-ordered, capped, deterministic, fail-open, headless-safe) TDD'd first. Lines: pole flip (turned darker/respectable) ▸ tier climb (name travels + the going rates here warm — the renown price-modifier is tier-keyed, so a tier climb IS a real price drift) ▸ a heading taken to chase ▸ purse heavier/lighter (singular/plural coin) / reshuffled hold ▸ a deterministic ambient "the world turned a notch" line on a quiet call. `main.js` thin (snapshot in mode.onChange→TOWN, compose+toast in `leaveHarbour()`, `tw.ashore` QA). **Did NOT build offscreen world-sim** — surfaced only deltas the existing systems already move. +14 TDD (→**577**); playtest ✓ zero errors (+digest gate: snapshot live ashore → composed on Set Sail → consumed); perf **32/130 draws · 90k/150k tris** (text/DOM only — no new draws). Gallery `studio/qa/gallery/v0.0.20260628053624-ashore-digest.png` (the toast over open water: "⚓ While you were ashore… word to chase for Barnacle Bottom · purse 44 coins lighter"). CREATIVE SPARK (Game Designer): lingering has a consequence, and casting off is when you feel it — the watch reads you back what your time ashore amounted to. **Follow-ups (filed):** rival sacks a named port / navy moves (true offscreen world-sim, the issue's stretch) · price-drift keyed to a real economy tick · digest reads a chased-objective OUTCOME (#127/#128 hooks) (v0.0.20260628053810).
- Loop 62: #112 + #111 + #115 **Rumours that pay off — chase a tavern tip → map marker → arrival reward** — **CLOSES #111 + #115; #112 STAYS OPEN** (umbrella for richer rewards). Retro 11 #1 + DL #4 headline: the reactive **town → rumour → sail → reward** loop was half-built + silent + invisible — now real end-to-end for ONE rumour type (a **trade tip naming a port**). **#115 keystone:** new pure `src/objectives.js` (typed world-target model — `sanitizeTarget`/`makeObjective`/`resolvesAt`/`payoffFor`/`sanitizeObjective`): one source of truth the marker + arrival-detection + payoff all read, not three prose re-parsers; `composeRumours` now emits typed `{ text, target }` entries (trade tips carry `{ kind:'port', name }`). **#111 marker:** a **⚑ Chase this** affordance per chase-able tip sets the active objective (target enriched with the named port's live coords); minimap + bigmap draw a bright **sea-green pin** on the heading (bigmap labels it **⚑ chasing**), cleared the instant it resolves; the chosen tip reads back **⚑ Chasing…**. **#112 payoff:** arriving at the chased port pays a **modest deterministic 60c bounty**, sings a **'rumour' Ballad verse** (#78), clears the pin (`resolvesAt` guards double-pay). **Save bump v9→v10** (objective persisted, sanitised + **fail-open**); `sailing.restore`/`reset` + `newVoyage` carry/clear; QA `tw.objective`/`tw.chaseRumour()`/`tw.chaseTarget()`/`tw.tavern.targets`. Asset-light (2D-canvas marker + DOM), **zero new WebGL draws**. +18 TDD (objectives/rumours/voyage-log/save → **563**); playtest ✓ zero errors — new rumour-chase section: listen → chase a trade tip → marker carries coords → sail to the chased port → coins bounty + pin clears + Ballad verse + a fresh voyage clears it; perf in budget (canvas/DOM only). Gallery `studio/qa/gallery/v0.0.20260628052005-rumour-chase.png` (the route chart with the green ⚑ chasing ring + label on the chased port). CREATIVE SPARK (Game Designer): chasing a rumour is a real **verb** now — the tip becomes a pin you steer toward and a coin in the palm when you make port. **Follow-ups (filed):** #127 trade live price-spike · #128 disposition/bounty vessel · #105 ashore digest reads the outcome · sea/deed/rep kinds made chase-able (v0.0.20260628052005).
- Loop 61: #70 **Ocean micro-details — drifting whitecaps froth the sunny sea's crests** — **#70 STAYS OPEN** (slice 1 of N; reframed by the brief as the cheapest always-working sea-FEEL beat — the issue's literal sail-over-curio slice + the standing "1–2 micro-details/loop" rule remain). The flat sea now has small surface life: the wave **CRESTS** catch the light and froth white, breaking into drifting patches as the swell rolls, so the sunny Caribbean water reads alive and in motion. **Pure colour wash in the EXISTING ocean fragment shader — ZERO new draws/triangles**, gated on the swell **HEIGHT** the shader already computes (lock-step with the CPU `sampleHeight` #102/#65 by construction — no height/swell drift, ports/coasts stay dry). New **pure `src/sea-foam.js`** is the SINGLE source of truth (like swell.js): crest-window `smoothstep` (`FOAM_LO 0.7 → FOAM_HI 1.3`, both ≤ MAX_SWELL 1.4) + `crestFoam`/`whitecap` + tuning consts — the shader's foam GLSL is GENERATED from them. `ocean.js`: fragment whitecaps = crest factor × a drifting **streak** (sum of slow sines → broken moving patches, not a band) mixed toward a `uFoam` tint; + `whitecapAt()`/`foam` QA surface. `main.js`: `tw.sea` + `tw.whitecapAt(x,z,t)`. Visual tune (2nd commit): swapped the streak from a sine PRODUCT (peaked too rarely → barely visible) to a SUM so the crests clearly froth. +5 TDD (`sea-foam.test.mjs` → **542**); playtest ✓ zero errors; perf **29/130 draws · 89k/150k tris** (shader-only — identical to baseline, the ≤130/≤150k mandate held). Gallery `studio/qa/gallery/v0.0.20260628043637-sea-whitecaps.png` (under full sail: drifting foam patches frothing the mid-distance crests, clean troughs foreground, gulls aloft, wake astern). CREATIVE SPARK (Game Designer + Graphic Designer): the trade winds catch the crests — whitecaps drift and break across a sea that was glassy-flat. **Follow-ups (filed):** the original sail-over curios (on #70) · bow-spray flourish #113 · sea colour variation / current streaks #114 (v0.0.20260628043637).
- Loop 59: #104 **The port remembers you — persistent per-town memory greets you on return** — **CLOSES #104** (DL #3 theme 1; the reactive verb that makes town a place with a memory). Each port keeps a tiny PERSISTENT record of your prior dealings *there* — visit count + a snapshot of your standing as seen locally (tier + dominant pole) — and reflects it back on the next landfall, so renown becomes a relationship that accrues port-by-port. On arrival main.js RECALLS the PRIOR memory FIRST, then BANKS the visit + persists: a return leads the arrival toast AND the town harbourmaster line with a **remembered-return greeting** — warmer for a regular, cooler if you've turned pirate since, "you've grown since" if you've risen a tier — while a true first visit keeps the stranger's welcome. New pure `src/systems/port-memory.js` (`recallLine`/`rememberArrival`/`sanitizePortMemory`, deterministic, immutable, size-capped) TDD'd first. **Save bump v8→v9** (`portMemory` persisted, sanitised + **fail-open** like legends/onboarding/ballad); `sailing.restore`/`reset` carry/clear it; `ui/town.js`+`index.html` show + highlight the recall; QA `tw.portMemory`/`tw.portRecall`. Asset-light (text + #67 town UI), **zero new draws/tris**. +15 TDD + 4 save tests (→**530**); playtest ✓ zero errors — new section lands (visits=1, no recall) → sails out, grows a name → returns (visits=2, recall names the port, greeting visibly changes) → survives a save round-trip; perf **38/130 draws · 86k/150k tris** (no perf cost). Gallery `…055748-port-remembers.png` (Saltpurse Quay on return: highlighted gold "the rogue they recall has come back a credit to the lanes — welcome, and well met", HUD reads Harbourmaster · respected). CREATIVE SPARK (Game Designer): your reputation precedes you — the harbourmaster remembers YOUR face, not just your number. **Follow-ups (filed):** per-port "last deed" recalled by name (#104b) · the "Your Harbour" home-port stretch (claim + invest across sessions) · per-town greeting identities (#69) · familiarity decay over long absences. **Breadth continues: #71 islands next** (v0.0.20260628035943).
- Loop 58: #102 **Landfall phase 2 — glassy "moored" swell settle + downbeat "made port" stinger** — **CLOSES #102** (landfall craft is "done for now"; phase-2 DoD met). Deepened the SAME moment phase 1 shipped, adding the two named multi-sensory flourishes, each PURE-cored + TDD'd + deterministic + headless-safe. **(1) Glassy moored swell settle:** new pure `mooredSwellScale(blend)` (1 at sea → 0.2 ashore, monotonic, clamped) drives a new `uSwellScale` GPU uniform threaded through the ocean.js vertex shader **and** the CPU `sampleHeight` sampler in lock-step (no sampler drift) — the whole sea eases to a calm glassy "moored" amplitude as the ship comes to rest, back to full open-water life on Set Sail (a reactive verb: paused helm = still water). Colour/uniform write only, **no new draws**. **(2) "We've made port" stinger:** new pure `isDownbeat(step)` bar-clock + `music.stinger()` arming a bright bell-like D-major arpeggio fired once on the **next bar downbeat** as SAILING→TOWN eases in; routes through `musicGain` (mute covers it), **headless-inert** (no AudioContext → no-op). QA: `tw.landfall` now exposes `swellScale`; the playtest asserts full-at-sea → glassy-ashore → restored-at-sea + the stinger never throws. +2 TDD (→**512**); playtest ✓ zero errors; perf **38/130 draws · 86k/150k tris** (no perf cost). Gallery `landfall-102-ph2-moored-swell.png` (moored at Saltpurse Quay, the sea gone glass-calm, town not yet open). CREATIVE SPARK (Game Designer + Musician): the water goes glass-calm at her moorings and a bright chord rings out on the beat — *you've arrived*. **Loop pivots to BREADTH next** (#104/#71/#70). Follow-ups: per-town stinger/bed identity (#69) · constant-power sea→town crossfade · deeper gold/camera dolly settle (v0.0.20260628034134).
- Loop 57: #107 **Mode-transition QA coverage** — **CLOSES #107** (DL #3 theme 3: keep the gate trustworthy as the state space explodes). The mode surface is now rich (seam #95/#106 · landfall #102 · town #67 · sound #94) but the gate only walked the happy path. Locked it down with fast PURE tests + one gate invariant, and **fixed the long-standing perf-counter flake (a REAL race, not environmental).** **N×N transition matrix (manager level)** in `mode.test.mjs` — every `MODES×MODES` pair through the live manager, cross-checked vs the pure `canTransition` guard, asserting `current` · `playerPaused` · `onChange` fire-count (incl. no-op re-entry). **Golden mode-trace fixture** (`tests/unit/mode-trace.test.mjs`) — a pure replica of main.js's mode→landfall→combat wiring driven by a committed intent-log, pinning the exact `SAILING→TOWN→BATTLE→SAILING` event sequence + sampled gesture state (incl. *fight-ashore reverses the town gesture*). **Cross-mode "world keeps living" invariant** generalised to **BATTLE** in the playtest (helm freezes mid-fight, ≥1 vessel sails on). **Perf-flake fix:** counters are refreshed only by the rAF loop (throttled to fps≈0 headless), so the gate could read `drawCalls=0` before any real frame latched (reproduced 1/6 runs) — fixed deterministically with `tw.qaRender()` (synchronous render+measure before the read) + a defensive `isMeasuredFrame()` latch; 6/6 green after. Slice 3's pure mix decision was already covered by `music-director.test.mjs`; the gate-level mix exposure + once-per-transition bell are a follow-up (noted on #94). +15 tests (→**510**); playtest ✓ zero errors; perf **38/130 draws · 86k/150k tris**. Mostly invisible → no gallery shot. CREATIVE SPARK (Game Designer/QA): a fight can break out anywhere — even ashore — but you can never make port mid-broadside; the golden trace makes that *truth* a committed, replayable fixture (v0.0.20260628032245).
- Loop 56: #102 **Landfall — the SAILING↔TOWN transition as a crafted eased gesture** (phase 1 of multi-sensory; **#102 stays OPEN** for audio + swell). DL #3's headline "the transition IS the drama": making port (and Set Sail) was a snap — mode flipped, town screen appeared, camera teleported. Now it's a deliberate eased moment off the #95/#106 mode seam: the camera slides from the open-water chase cam to a closer, lower **3/4 "ashore" framing** of the moored ship; a warm **"golden harbour" grade** lerps fog/haze + sea-haze + sky horizon + sun toward a low gold glow (captured/restored exactly, never fights day-night #58, colour writes only — no new draws); the **town view (#67) opens only once ASHORE** so the gesture plays out in the open first, then the town takes the screen, and on Set Sail it closes at once while the reverse eases you back to sea; **skippable** (Enter/Space or a tap during the ease, never steals input at sea). New PURE `src/systems/landfall.js` — a phase/ease/duration state machine (idle→landing→ashore→leaving → eased `blend` 0→1; dt-driven not wall-clock, deterministic, headless-safe, interrupt-safe, skippable). QA `tw.landfall {phase,blend,active,townReady}` + `tw.skipLandfall()`. +11 tests (→**496**); playtest gains a landfall section (eases up · town held closed mid-gesture · skippable · reverse closes town), zero errors; perf **35/130 draws · 85k/150k tris** (no perf cost). Gallery `landfall-102-{making-port,set-sail}.png`. CREATIVE SPARK (Game Designer): "make landfall" should *feel like arriving* — the helm goes quiet, the ship glides to her moorings, the light turns gold, THEN the town opens around you. **Follow-ups (#102 OPEN):** transition stinger on the next downbeat (bar-clock) · glassy moored swell-amplitude lerp (needs a swell uniform) · deepen the gold (v0.0.20260628030539).
- Loop 55: #103 **Town tavern "listen for word"** — **CLOSES #103** (DL #3's #1 reactive-verb-ashore). Turns TOWN from a menu into a **destination**: the tavern gains a verb — **listen for word** surfaces 1–2 in-character rumours composed **DETERMINISTICALLY** from live state — the ports around you (what each *craves*/makes *cheap*), **who you've become** (Infamy↔Standing pole + renown tier; feared-pirate vs respected-governor diverge), and **recent deeds** (last fight foe + raised isle from the #78 voyage log). Each rumour **names a real target** → a **soft sea objective** you may chase with the verbs you already have (no quest log). New pure `src/rumours.js` (`composeRumours(world,{count,nonce})`, kind-balanced rep+trade-lead, `nonce` re-listen freshness without breaking determinism) TDD'd first; `src/ui/town.js` gains the 🍺 **Listen for word** affordance (button + **R** key, rumours block, fresh-visit reset) in the Ballad tone; `main.js` exposes `tw.tavern`/`tw.tavernListen()`. Asset-light (text + #67 town UI), zero new draws. +11 tests (→**485**); playtest ✓ zero errors; perf **35/130 draws · 85k/150k tris** (identical to baseline — no perf cost). Gallery `…024509-tavern-rumours.png`. CREATIVE SPARK (Game Designer): a hunched regular in the corner who "heard a thing" — a rumour that points the bow somewhere worth going. **Follow-ups (filed):** map **MARKER** for a rumour's heading · **economy/bounty hooks** so a chased rumour pays off · per-town rumour identities (#69) · stale-word time-decay (v0.0.20260628024509).
- Loop 54: #101 **Dress ports with CC0 Pirate Kit props** (phase 1+2 of 4; **#101 stays OPEN**). The world stops being all-procedural: each port is dressed as a **working harbour** — **barrels + crates as cargo on the jetty planks**, a pair of **palms framing the dock foot** — from the **CC0 Kenney Pirate Kit** (same family as the #32 hull). Loaded **like the #32 hull** (`GLTFLoader`, graceful fallback — sails **bare** if a fetch fails, never broken). Repeated props are **InstancedMesh'd (one draw per type per port)** and each port cluster is **distance-culled wholesale** (≤520u). New pure `src/systems/props.js` (`localToWorld`/`propWorldPlacement`/`placementsForType`/`clusterVisible`/`scaleForHeight`) TDD'd first; three.js `src/props.js` (`loadProps`, never rejects); `ports.js` exposes `portPlacements`; `main.js` ticks it + `tw.props`/`tw.qaTeleport`. Assets `assets/props/*` **CC0** (`CREDITS.md` + `assets/props/LICENSE.txt`). +8 tests (→**482**); playtest ✓ asserts props placed (21/3 clusters), drawn near a port (7), culled to 0 at sea; perf **35/130 draws · 85k/150k tris**. Gallery `…022029-pirate-kit-props.png`. CREATIVE SPARK (Graphic Designer): a port reads as a *place* now — cargo stacked, palms over the berth. **Follow-ups:** island dressing (palms/rocks/huts) · loose props (lanterns/market stalls, feeds #96) · more port-prop variety · embed prop textures into the GLBs like #32 (v0.0.20260628022029).
- Loop 53: #93 **Ship's-wheel touch steering (CLOSES #93; #98 was its dup)** — a rotatable **brass-and-wood ship's wheel** replaces the ◀▶ touch steer buttons, in its own bottom-left control zone. **Drag-rotation → analog steer** into the eased rudder (#20): clockwise = starboard, anti-clockwise = port, half-turn = half rudder; rotates 1:1 under the thumb and **self-centres** on release. Same eased path as the keyboard (no parallel physics); **desktop A/D unchanged** (axis sampled only when keys idle). Owns its gestures so it never fights the camera-orbit drag; lives in `#touch-controls` so TOWN hides it (no #66 re-overlap). New pure `src/ui/wheel.js` (drag-angle→steer deadzone/clamp + seam-safe delta) TDD'd; self-contained `createWheel()` (#53) renders an inline-SVG wheel; `tw.wheel`/`steerWheel`/`centreWheel` QA. +10 tests (476); playtest drives the wheel to full lock → asserts the ship turns + rudder eases back on release; perf **30/130 draws · ~84k/150k tris** (SVG/DOM, no new draws). Gallery `…015755-shipwheel.png`. ⚠️ **iOS UNCONFIRMED pending owner iPhone re-test** — don't stack dependent work yet. Follow-ups: haptic detents · skins · sensitivity setting (v0.0.20260628015755).
- Loop 52: #97 **Living sea fauna — gull flock (Phase 1)** — the first fauna beat: a small instanced flock of GULLS that keeps the ship company, wheeling overhead at sea and **drifting to hang over the shore** as you raise an island (reactive verb). One `THREE.InstancedMesh` (one draw call) of a two-triangle gull silhouette with a Y-scale wing-beat, **distance-culled wholesale** (0 draws) off-stage. Pure three-free `src/fauna-math.js` TDD'd first; thin `createFauna({world})` in `src/fauna.js`; ticked every frame so it lives on while the helm pauses. QA hook `window.__tidewake.fauna`. CREATIVE SPARK: gulls trail your wake for galley scraps, then peel off over the coast (pairs with gull SFX #68). +6 tests (456); playtest asserts the flock exists/draws/tracks the player; perf **32/130 draws · 83.8k/150k tris**. Gallery `…014411-fauna-gulls.png`. **#97 stays OPEN** — phase 2 dolphins + phase 3 other animals to follow (v0.0.20260628014411).
- Loop 0: v0 shipped — playable build + auto-release + studio structure.
- Loop 27: #59 Cannon Broadside — open fire (G) alongside the Insult Broadside duel; closed, depth follow-up #72 filed (v0.0.20260627130215).
- Loop 28: #63 Mobile MVP — installable PWA (manifest + brass-anchor icons, Add-to-Home-Screen) + heat-aware DPR cap atop existing touch controls/responsive HUD; closed. Follow-ups #74 (SW offline) + #75 (safe-area/landscape/low-end polish); docked overlap tracked in #66 (v0.0.20260627131832).
- Loop 29: PWA top-notch safe-area (owner request, §3c) — swept `env(safe-area-inset-*)` onto every top-anchored HUD element so nothing hides under the notch/status bar in standalone; also fixed the ≤560px media-query `#title` rule. #75 safe-area-top item done (partial; landscape/home-indicator/low-end remain) (v0.0.20260627133536).
- Loop 30: #73 Settings/options panel — self-contained `src/ui/settings.js` (per #53) with a ⚙ button / O key opening a ship's-brass control plate that renders feature toggles from a one-line registry. Ships **Sound** (live-backed by audio mute) + **Spyglass readout** (perf overlay, persisted); defaults keep the current look, weather #58 plugs in next. Persisted to localStorage, TDD'd (+11 tests = 260), playtest drives it. Closed #73 (v0.0.20260627135021).
- Loop 31: #76 **a1** Arcade island collision — islands now STOP the ship (no more phasing through land), soft & arcade-y. Pure `src/physics.js` resolver: forgiving circle hitboxes, radial push-out + slide along the coast, swept (no tunnelling), speed bled to ground-speed-made; docking preserved. Comic "Scraaape…" run-aground banner. +8 TDD tests (268); playtest asserts non-penetration; perf unchanged. **#76 stays OPEN** — remaining: (c) harbour/fight slow-to-stop, (b) ship-vs-ship, (a2) slide polish (v0.0.20260627140435).
- Loop 32: #76 **c** Arcade slow-to-stop for harbour & combat — the ship eases to a near-stop (via the existing `approach()` damping) instead of teleport-freezing for a fight or barrelling through a berth; normal throttle returns when it ends, and the berth assist yields the moment you press W to leave (never strands you at the dock). Pure settle model (`harbourSlowFactor`/`settledTargetSpeed`/`SETTLE_RATE`) TDD'd in `tests/unit/settle.test.mjs` (+12); folded in the in-flight a1 beach fix (+4 → 284 tests). `state.settling` on the QA hook; playtest drives a port approach (42→2.8) + a fight (10.9→~0) and asserts the ease-down. CREATIVE SPARK: "Sails reefed — battle stations" / "Easing into the berth…". **#76 stays OPEN** — remaining: (b) ship-vs-ship, (a2) slide polish (v0.0.20260627182358).
- Loop 33: **Owner fast-lane bug batch** (two fixes, one release). **(1)** #76 bigger island hitbox (owner: "still go into a bit of sand") — `ISLAND_HITBOX` 1.12 → **1.18** so the visible bow stops a clear hull's-width off the sand on every island (the bow overhangs the collision circle by ~1); docking preserved; bow-clearance regression test added. #76 comment, stays OPEN. **(2)** #77 iOS Safari/PWA audio (owner: "no music on iOS safari or pwa") — broad gesture binding + `resume()` + 1-sample silent-buffer iOS unlock + retry-until-running + `visibilitychange` re-resume; pure `unlockEventNames`/`needsUnlock` helpers TDD'd. 287 tests, playtest green, CI green, live 200. **iOS final confirmation pending owner's iPhone.** Closed #77 (v0.0.20260627185044).
- Loop 36: #58 **Optional day & night cycle** behind the #73 settings toggle — the first real VISUAL toggle. **DEFAULT OFF** so the sunny Caribbean look (#61) stays the default; flipping OFF restores it **byte-for-byte**. ON plays a slow (~150s) loop dawn → bright noon → golden afternoon → dusk → soft moonlit night (sun arcs; sky dome / hemisphere / scene haze+fog / ocean sun+sea-tint drift warmly, never gloomy). Pure `dayNight(t)` TDD'd (`tests/unit/daynight.test.mjs`: keyframes, smooth interp, seamless loop, **NOON === sunny constants**); thin `createDayNight()` factory. `window.__tidewake.daynight`/`setDayPhase`. CREATIVE SPARK: first dusk → "🌅 The sun dips toward the yardarm…". +6 tests (310); playtest flips ON at golden hour & asserts the shift, OFF & asserts the exact sunny restore; perf unchanged (no new draw calls). Live QA: default sunny unchanged / ON golden-hour / OFF restores (gallery `…195729-daynight-golden.png`). **Closed #58**; full weather (rain/storm) = follow-up (v0.0.20260627195729).
- Loop 35: #76 **b** Arcade ship-vs-ship collision — the player ship now BUMPS NPC vessels instead of sailing clean through them. Reuses the island circle-hitbox resolver (`src/physics.js`): each NPC is a moving circle, solid boundary = the two hulls' forgiving radii summed (`SHIP_RADIUS 7 + NPC_RADIUS 9`), so they rest gunwale-to-gunwale. **Player-only** resolution → wander AI, duel #33, cannon #59, harbour slow-to-stop #76c all untouched & deterministic. Head-on shoulder bleeds speed (soft pile-up), glancing bump slides along & keeps its way on, swept (no tunnelling). New pure `NPC_RADIUS`/`shipCircles()`/`sweepShipCollision()`; `collisionRadii` on the QA hook. CREATIVE SPARK: comic "🛶 Hulls collide! — Timbers groan, mind the other captain!" bump banner. +9 TDD tests (298); playtest drives the player into an NPC and **asserts non-overlap** (minDist=16.0 = the boundary). Perf unchanged. **#76 stays OPEN** — remaining: **(a2) slide polish** (v0.0.20260627194005).
- Loop 34: **Owner iOS P1 bug batch** (three fixes, one release). **(1)** #86 ocean ShaderMaterial not rendering on iOS (the headline — sea a flat teal void): root cause a strict-mobile vertex/fragment **varying precision mismatch** failing the program link → explicit `precision highp float` in both stages + camera-relative fragment maths + a flat-but-coloured **fallback sea** (`src/sea-color.js`) so it's never a void; CPU/GPU swell still in sync. Closed. **(2)** #77 **REOPENED** — audio still didn't unlock (touch buttons `preventDefault`): bound the unlock in the **capture phase** on window + `input.js` calls `audio.unlock()` directly from the controls; pending owner re-test. **(3)** #87 mobile no-select hardening on body+canvas; closed. 289 tests (+2 pure colour), playtest green, perf unchanged, CI green, live 200; desktop sea verified unchanged. **All three need owner re-confirmation on his iPhone** (v0.0.20260627192458).
- Loop 39: #19 **Island names + comedic landfall line** — **CLOSES #19**. Every island carries an authored, in-tone name + a daft flavour line that hails you the FIRST time you sail close (once per session; re-armed on a new voyage), via the shared HUD toast. CREATIVE SPARK — a 12-isle authored archipelago: **Gallows Cay** ("they hang folk here for bad puns — sail careful, and rhyme nothing"), **Rumlost Reef** ("a whole galleon of rum sank here — the fish have never been merrier"), **Sotweed Hollow**, **Mutineer's Folly**, **Kraken's Footstool**, **Doubloon Dunes**, **Scurvy Point**, **Petticoat Spit**, **Tankard Rock**, **Cutlass Bend**, **Wailing Wharf**, **Gibbet Green**. Pure three-free `src/islands.js`: `ISLAND_LORE` pool, `assignIslandNames()` (stable + unique BY INDEX, no port-name clash), `detectApproach()` (nearest not-yet-introduced isle within `r+APPROACH_RANGE`, once-only), `createIslandNamer()`+`reset()`. QA hook: names on `tw.islands` + `tw.islandsIntroduced` + `tw.nearestIsland`. Bonus #54: the route chart now LABELS every island. +12 TDD tests (`tests/unit/islands.test.mjs` → 332); playtest sails at an isle & asserts the named banner fires before grounding. Perf unchanged (77/130 draws · 85.2k/150k tris). Live QA: banner reads well + map labels (gallery `…204815-landfall.png`) (v0.0.20260627204815).
- Loop 38: #76 **a2** Tangential slide polish — **CLOSES #76**. On contact the hull keeps the velocity skimming ALONG the surface and loses only the part pressing INTO it: a glancing graze of a coast or another hull **slides on and keeps its way**, a head-on hit still bleeds to a stop. One shared pure rule — new `slideVelocity(vx,vz,nx,nz)` in `src/physics.js` strips only the into-surface component; `resolveCircleCollision`/`sweepIslandCollision`/`sweepShipCollision` return the **outward contact normal** so island & ship-vs-ship slide identically. Replaced the old displacement/dt heuristic → jitter-/substep-free, no stop-dead on a glance. CREATIVE SPARK: a lighter "scrape… you slip past" SLIPS line for a graze vs the head-on SCRAPES groan. +16 TDD tests (`tests/unit/slide.test.mjs` → 320); playtest green, non-penetration/non-tunnel/dock-reachable all still asserted; perf unchanged (pure CPU math). **#76 COMPLETE** — a1+c+b+a2 all shipped (v0.0.20260627202808).

- Loop 41: #79 **False Colours** — **CLOSES #79** (DL#2 deception-as-a-verb). The captain flies a displayed flag — **True/Black** (honest pirate) vs **False/Merchant** (a disguise) — cycled with **C** / the HUD chip, persisted (save **v8**) and shown on the player's own pennant (skull hidden while disguised). The world reacts to the colours **SHOWN, not just renown**: true black while feared → nearby vessels **FLEE**; false merchant → the disguise works, they stay **calm and let you approach** even when infamous. **Treachery payoff:** strike a ship crept up on under false colours (duel OR cannon) → **+50% Infamy bonus** + a surprise opening dent (foe starts weakened); the honest black attack earns none. The Ballad (#78) gains a distinct false-colours verse. Pure `src/colours.js` (disposition / treachery bonus / banter) TDD'd first, then wired into `duel.js`/`cannons.js`/`npc.js`/`hud.js`/`save.js`/`voyage-log.js`/`main.js`; QA hook `tw.colours`/`setColours`/`cycleColours`. CREATIVE SPARK: hoist banter, a smug *"they bought it whole"*, and the foe's betrayed splutter — *"Treachery! TREACH— oh, bother."* +19 TDD tests (→ **367**); playtest asserts NPCs flee under black but not merchant, a disguised approach lands, and the treacherous win applies the Infamy bonus (+273) + perfidy deed + ballad verse. Perf unchanged. Gallery `docs/false-colours.png`. Follow-up **#91** (Letters of Marque → Standing; seen-through disguise) (v0.0.20260627214813).
- Loop 40: #78 **The Ballad of Your Voyage** — **CLOSES #78** (DL#2 anecdote factory). An auto-composed, shareable in-character log of the player's run: notable systemic deeds — islands raised (#19), insult duels won (#33), cannon fights won (#59), crowned legends (#46) — are recorded into a small **pure** voyage log and composed into a witty balladeer narrative, openable via the 📜 button / **B** key, with **copy-to-clipboard** sharing (empty log → a graceful "yet unwritten" line). Pure `src/voyage-log.js` (`recordEvent` record/dedupe/order/cap + deterministic `composeBallad`) per the #53 house standard; thin parchment panel `src/ui/ballad.js`. Persisted in the save (schema **v7**, fail-open) so the ballad survives a reload; a new voyage starts the tale blank. `window.__tidewake` exposes `.voyageLog`/`.ballad`/`openBallad`/`copyBallad`. CREATIVE SPARK — the balladeer's voice: *"You ran out the guns on Old Thunderbottom and sent her to the seabed in one roar of powder…"*. +16 TDD tests (voyage-log + save round-trip → **348**); playtest sinks a ship & asserts the deed accrues, names the foe in the composed ballad, the panel opens, and the deed persists across a reload. Perf unchanged (77/130 draws · 85.2k/150k tris). Live QA: parchment scroll reads beautifully over the sunny sea, copy works (gallery `…210918-ballad.png`); tab parked. Follow-up #90 (more deed types / richer composition / share-as-image) (v0.0.20260627210918).
- Loop 43: #91 **Letters of Marque** — **CLOSES #91 (MVP)**, the lawful mirror of #79 False Colours. **Lawful privateering → Standing:** an HONEST (true-colours) kill of a PIRATE/outlaw vessel earns **Standing** (letter-of-marque); honestly sinking an innocent MERCHANT earns none and **fines** it (the lawful path is actually lawful). Light deterministic NPC **disposition** (`vesselKind` by slot — ~1 in 3 outlaw, blood-dark flag). **"Seen-through" risk:** at high Infamy a false-colours approach is **pierced** on a seeded, rising (capped <1) roll — the vessel reads your true renown and **flees**, so the disguise is real risk/reward at the top. Ballad (#78) sings a lawful privateer verse. Pure `src/colours.js` (`lawfulStanding`/`vesselKind`/`seenThroughChance`) TDD'd first, then wired into `cannons.js`/`duel.js`/`npc.js`/`voyage-log.js`/`main.js`; QA hook `tw.colours.seenThrough`/`seenThroughChance` + `npc.kind`. No save bump (deterministic kinds). CREATIVE SPARK: grateful-port nod / piracy wince / *"…I know that rigging — that's NO merchantman!"*. +17 TDD tests (→**384**); playtest sinks a pirate honestly (**Standing +104** + lawful verse) and triggers seen-through at infamy 5000 (chance 0.85, vessel flees). Perf unchanged, CI green, live 200. **Both poles now mirror:** treachery→Infamy vs lawful→Standing. Follow-ups: faction/bounty system + persisted commission item + more false ensigns (v0.0.20260627222104).
- Loop 44: #20 **Smooth steering — eased rudder** — **CLOSES #20**. The helm no longer yaws at a constant rate the instant A/D is held — the ship eases a **rudder** toward the steer input, so the turn **accelerates in** as you hold and **settles smoothly back amidships** on release (a weighty wheel, not a switch). Applied yaw = eased rudder × speed-scaled `steerRate` (turn authority still firms up with speed); the hull also **banks/leans into the turn** (visual only, scales with eased rudder + speed). Throttle/speed already ease (throttle ramp + `approach()` + the #76c slow-stop) so they were left **untouched to avoid double-damping into sludge**; while fighting/settling steer is 0 so the rudder eases back on its own. Pure `easeRudder()` + `RUDDER_RATE` in `src/physics.js` (reuses `approach()` — frame-rate independent, never overshoots), TDD'd first in `tests/unit/steering.test.mjs`; wired into `sailing.js` (`state.rudder` per `tw.step()` sub-step). `__tidewake` hook exposes `state.rudder`. Desktop keyboard + mobile touch share the eased path. CREATIVE SPARK: a real wheel has heft — soft wheel-creak SFX noted for the Sound Engineer. +9 TDD tests (→ **393**); playtest ✓ PASSED — all autopilot nav still works under the new feel, zero console errors; perf unchanged (77/130 draws · 85.2k/150k tris). Feel-only → no gallery shot. Follow-up: a key-**remapping UI** (the `input.js` seam is now clean for it) (v0.0.20260627223424).
- Loop 45: #65 **Sea no longer shows inside the hull** — **CLOSES #65** (owner P1 visual bug, "our ship looks like it has water in it :D"). The open-topped hull bowl let the single ocean plane (y≈0) draw *inside* the boat through the open ring between the inset deck and the gunwale. Fix: ONE opaque interior **sole/bilge cap** (the hull's own interior cross-section at **SOLE_Y=1.5**, woodHull) occludes the sea in **+1 draw call**; the ship is **NOT raised** (sampleHeight/wake/camera untouched). New pure three-free `src/hull.js` is the shared single source of truth (ship.js + node tests); SOLE_Y coordinated with `swell.js` MAX_SWELL and asserts above-crest/below-deck at import. +4 TDD tests (`tests/unit/hull.test.mjs` → **397**). **Visual DoD screenshot-verified over open water (owner's sharpened criteria): (1) no water from the top — deck/hull interior read solid wood; (2) no deck/plank past the gunwale.** Gallery `studio/qa/gallery/v0.0.20260627230647-hull-fix.png` (+topdown); tab parked. CREATIVE SPARK: a holystoned sole. Perf 78/130 draws · 85.2k/150k tris, CI green, live 200 (v0.0.20260627230647).
- Loop 44: #20 **Smooth steering — eased rudder** — **CLOSES #20**. The helm eases a rudder toward the steer input (turn accelerates in, settles back amidships on release) + bank-into-turns; pure `easeRudder()`/`RUDDER_RATE` in `physics.js`, TDD'd (+9 → 393); feel-only, perf unchanged (v0.0.20260627223424).
- Loop 42: #55 **Art-asset sourcing research** (research-only, NO release). Surveyed real, citable CC0/permissive low-poly age-of-sail 3D sources and verified each licence: **Quaternius** + **Kenney** Pirate Kits = **CC0** (glTF, no attribution needed); **Poly Haven**/ambientCG = CC0 (sky/textures); **Poly Pizza** = MIXED CC0+CC-BY (per-model check); **Sketchfab** = mostly CC-BY (the prominent low-poly pirate ship is CC-BY *and* 73.2k tris → avoid). Verdict: **CC0-first**, $0, S–M effort. Hero-ship (#32) recommendation = swap to a Quaternius/Kenney CC0 ship via `GLTFLoader` (already in the import map), match the contract (bow +Z, length ~16, re-attach `userData.flag`), keep the procedural ship as fallback; add a `CREDITS.md` even for CC0. Report → `docs/art-sourcing.md`; ROADMAP #55-row + memory updated. **#55 CLOSED, #32 commented (ready to build).** Caveat (public repo): CC0/self-made only for core art; never a franchise lookalike.

- Loop 46: #32 **CC0 glTF hero hull** — **CLOSES #32**. Swapped the procedural box-hull sloop for the CC0 **Kenney Pirate Kit** `ship-pirate-small.glb` (1461 tris, byte-verified against the official kit; CC0 in `CREDITS.md`). Embedded the kit's external `Textures/colormap.png` into the .glb → single self-contained, CDN-friendly file (no build step, no 404). `loadShip()` keeps the procedural seam (bow +Z, len ~16, keel-centred, `userData.flag`=[pennant, skull] → False-Colours + flap intact) and falls back to the procedural hull on error; pure scale/centre math in three-free `src/ship-loader-math.js` (Node-testable). +6 tests (→403); playtest ✓ zero errors; perf **30/130 draws · 84k/150k tris** (glTF *cut* draws). Visual DoD verified live: textured carrack over sunny sea, wake + camera intact (gallery `…235613-gltf-hull.png`) (v0.0.20260627235613).

- Loop 47: #72 **"Strike the Colours" — crew morale & the merciful-capture path** (depth follow-up to #59; **#72 advanced, follow-ups still open**). The aim you pick now **decides the foe's fate**: a **full broadside** drowns the hull (the bloody **Infamy** road) but barely dents morale; **chain-shot** shreds rigging and **rattles the crew**, eroding a new **NERVE** stat — break their nerve while the hull is wounded and they **strike their colours and YIELD**. A capture is the **merciful road** — a ransom + **lawful Standing** (governor pole) and far less Infamy than a sinking — so the two combat outcomes now mirror the two identity poles. **Broadside-spam still SINKS before any yield → the deterministic playtest is unchanged.** Pure logic TDD'd in `src/cannons.js` (`crewShock`/`strikesColours`/`captureSpoils`/`strikeLine` + morale through `resolveExchange`), wired into the controller (`'capture'` finish), `hud.js` (a third gold **"Their nerve"** bar + help copy), `index.html` (CSS), `main.js` (capture banner/deed), `voyage-log.js` (a distinct **capture verse**). +10 tests (→**411**); playtest ✓ zero errors; perf **30/130 draws · 84k/150k tris** (no geometry). Visual DoD: cannon panel three-bar shot + in-game capture verified (gallery `…000955-strike-colours.png`). CREATIVE SPARK: the aim IS the moral choice — drown for swagger or break their nerve for mercy. Follow-ups on #72: hull-damage visuals, tougher foes/gunnery spread, more aims (grapeshot/raking)+range/reload, fleeing chase, cannon audio, foe initiative (v0.0.20260628000955).

- Loop 48: #95 **Mode system — explicit world-state machine** — **CLOSES #95** (from-owner P1 keystone, the spine the rest of the owner batch stands on). Generalises the implicit `duel.active || cannons.active` freeze into a real machine: new pure, three-free `src/mode.js` (`SAILING ↔ TOWN ↔ BATTLE`, guarded `enter/leave/current`, a `playerPaused` truth, `onChange`). `main.js update()` drives BATTLE from combat (**zero behaviour change for a fight**) and now runs `npcs.update()` **every frame** — the world keeps living underneath while the player's helm is paused, so other vessels never snap-freeze (#95's headline). TOWN proven via the `tw.enterMode` seam (town scene is its own slice #67/#96). QA hook `tw.mode`/`enterMode`/`leaveMode`; +9 TDD (`tests/unit/mode.test.mjs` → **420**) + a headless mode check (boot SAILING → TOWN eases the ship to a stop while a vessel still sails → leave SAILING). Perf unchanged (30/130 draws · 84k/150k tris — pure logic, no draws). Invisible/structural → no gallery shot. Follow-ups: #67/#96 town scene, #100 battle content, #94 mode-aware sound (all plug into this seam) (v0.0.20260628003252).

- Loop 49: #67 + #96 **Auto-harbour into a real TOWN/market mode + Leave Harbour** — **CLOSES #67, #96, #66** (from-owner P1, first new mode on the #95 seam). Sailing into a port's dock radius now **makes landfall into a deliberate TOWN mode**: the helm pauses, the world sails on, and a real town/market view (port name + blurb + harbourmaster + a rotating **barker cry** + standing badge + the **market board wired to the economy**) takes the screen instead of the corner trade panel. The single explicit, reversible exit is the **"⚓ Set Sail" plank (or L)** — back to SAILING, helm re-armed and **bow nudged seaward** so a berth never traps you (owner-flagged risk). In town the at-sea touch clusters + corner trade panel are hidden via `body.town`, which **also fixes the iPhone overlap #66**. New pure `src/systems/harbour.js` (edge-trigger + leftHarbour slow-assist latch + seaward heading; +8 TDD → **427**) + self-contained `src/ui/town.js` (#53); `main.js` drives landfall off the arrival edge + `leaveHarbour()` + `tw.town`/`tw.leaveHarbour` QA; `ports.js` stores the jetty seaward angle. Playtest ✓ (new auto-harbour→trade→leave section; collision test retargeted to a portless isle); perf **30/130 draws · 84k/150k tris** (no new draws). VISUAL DoD: town view at Saltpurse Quay (gallery `…005033-town-market.png`). CREATIVE SPARK: landfall is edge-triggered like crossing the harbour mouth; the market opens with a barker's holler; leaving is one warm pull of the wheel. Follow-ups: #94 mode-aware sound + the per-town/harbour-bell *audible* cue; walking-ashore + town activities (P2 per #96); ~200u "town ahead" announce (v0.0.20260628005033).

- Loop 50: #94 **Unified mode-aware sound — Phase 1: procedural proximity crossfade** (Phase 1 of 5; **#94 stays OPEN**). The first always-working slice of the owner's unified sound system, on the #95 mode seam + #67 town. New pure **`src/music-director.js`** — `resolveMix({mode, portDistance, dockRadius})` → per-layer target gains (context already shaped for the future sail-zones hook). `music.js` splits the score into a named **SEA** layer-gain (the sailing theme) + a new **PORT** layer-gain hosting a warm lowpassed D-major **tavern drone** with an accordion-bellows tremolo (one shared key centre); `setMix()` crossfades them via `setTargetAtTime` (no clicks). **The bed reacts to WHERE you are:** SAILING = open-sea bed, the port layer **swells up from `PORT_CUE_RADIUS` 260u (outside `DOCK_RADIUS` 90)** on approach + ducks the sea under it — the **audible "harbour ahead" cue from a distance (delivers #67 acceptance #3)**; TOWN = tavern theme owns it, sea to a hush; BATTLE = the whole bed settles to a low keel so the guns read. All under the one shared `{ctx, master}` bus → the existing single mute still covers it; headless-safe (no AudioContext → inert, zero console errors). +11 TDD (`tests/unit/music-director.test.mjs` → **438**); playtest ✓; perf **30/130 draws · 83.8k/150k tris** (audio = pure CPU). CI green, live 200. Audio not visible → no gallery shot. CREATIVE SPARK (Musician + Sound Engineer): a port has an *auditory horizon* — a warm tavern wheeze drifts across the water long before the dock is in sight. **Remaining on #94:** rotating sea themes · per-town identities (#69) · real battle cue · real audio files behind `loadTrack` (v0.0.20260628010142).

- Loop 51: #106 **Mode-seam hardening — Phase 1** (first DL #3 build slice; **#106 stays OPEN**). Turns the #95 keystone into a robust Thinnest-Viable-Platform contract before #67 town / #94 sound / #100 battle pile onto one fragile `onChange`. Pure, three-free `src/mode.js`: **legal-transition guard** — `canTransition(from,to)` + exported `TRANSITIONS` graph (SAILING is the hub, always reachable; combat preempts — **TOWN→BATTLE legal**, a fight can break out ashore — but you can **never make port mid-broadside**, BATTLE→TOWN illegal = "TOWN must not interrupt BATTLE"); `enter()` refuses an illegal-but-known move with `false` (poll-safe, no throw), unknown still throws; `canEnter()` advertises it. **Multi-subscriber bus** — `subscribe(fn)→unsubscribe` so each system attaches/detaches independently; `onChange` kept as backward-compat subscriber #0; exit fires exactly once, a throwing subscriber is isolated (never corrupts state nor blocks peers), unsubscribe idempotent. **Deterministic `reset()`** lands SAILING from any stance → wired into `newVoyage()`. Invariant: `playerPaused === (current !== SAILING)`. +14 TDD (`mode.test.mjs` → **464**); playtest ✓ zero errors; perf **30/130 draws · 84k/150k tris** (pure logic, no draws). CI green, live 200. Invisible/structural → no gallery shot. CREATIVE SPARK (Game Designer): SAILING is the hub — the open hand on the helm every stance falls back to; the seam itself refuses to let town interrupt a live broadside. **#106 OPEN for slices 1 (declarative `{[mode]:{onEnter,onLeave}}` registry — sugar over the bus) + 4 (per-mode disposal — blocked on #100 battle meshes); #107 N×N/golden-trace QA rides this contract** (v0.0.20260628012450).

- Loop 60: #71 **Islands TLC — per-isle palette + silhouette variety + instanced dressing** — **CLOSES #71** (from-owner P2 breadth, owner "Islands, a little bit of TLC there too"). Each isle now wears a deterministic FACE from its INDEX (stable across reloads, pairs with named-isle lore #19): new pure three-free **`src/systems/island-style.js`** — `islandPalette` (bounded correlated HSL offsets applied via `material.clone().offsetHSL`, the npc.js fleet trick), `islandSilhouette` (wider squash 0.65–1.5 + tall/peak/lean), `islandDressing` (deterministic rocks · palms · driftwood · grass tufts in isle-local space). `world.js` rebuilt to consume it — hue-jittered bodies + softer beach blend, and the repeated dressing **INSTANCED per type across the whole archipelago** (5 InstancedMeshes in their own group, off the `world.islands` iteration so collision/naming/NPC/fauna are untouched). Collision stays the squashed ellipse; the playtest collision check made ellipse-aware via the new `tw.islandStyles` QA hook. +10 TDD (`island-style.test.mjs` → **540**); playtest ✓ asserts every isle dressed + varied tones/silhouettes + look byte-stable across reload; perf **32/130 draws · 90k/150k tris** (instancing held the budget). No new external assets. Gallery `studio/qa/gallery/v0.0.20260628042000-islands-tlc.png` (Mutineer's Folly: warm-sand ellipse + grassy dome, three palms, a rock, driftwood + a hut; a second differently-sized isle on the horizon). Follow-ups: deferred M silhouette remodel (noise-displaced geo + LOD) · per-biome palettes/variants · re-coordinate sand tone with the Caribbean sea pass #61 (v0.0.20260628042000).

## Blocked
- _(empty)_
