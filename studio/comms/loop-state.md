# Loop state (orchestrator)

Single source of truth (resume brain) for the never-stopping delivery loop. Survives context resets.

> ▶️ **LOOP RUNNING — owner RESTARTED it via `/loop` on 2026-06-28** (the earlier ~01:15 CEST
> owner-STOP is over; do NOT cold-resume into a halt). Per cycle: (0) `owner-channel.sh peek`
> → route per `OWNER-CHANNEL.md` §3; (1) read `queue.md` top; (2) dispatch ONE cycle-runner; (3) read
> its <10-line report. `loop-state.md`+`queue.md` are the resume brain.
>
> **⚠️ ON RESUME, FIRST CHECK:** a **live PM-desk session** was active when the loop stopped, writing
> feedback to the **main working tree** (`studio/feedback/2026-06-28-*.md`, REGISTER) — likely still
> uncommitted. Do NOT race it: if it's still active, let it finish/commit (or have it run in the
> `tidewake-pm` worktree); a build-loop commit while it shares the index can sweep its WIP (it already
> did once, commit `eeacde1` — no data lost). New owner-scoped items captured at the desk (file an
> issue/queue them once committed): ship-wheel mobile nav (**#93 filed**), arcade-battle-modes,
> living-sea-fauna, town-mode-trade-view, multiple-bg-music, approach-town-music-cue, use-cc0-pirate-kit.
> Also: **owner spend-decision pending** — optional ~$10–30/mo AI-image seat for 2D portraits (#55).

- **Current loop:** 68 done → **Retro 12 just ran (covered loops 62–68)** → next reads `queue.md` top
  (now **#126 reputation-reactive world grade** — make the now-symmetric poles VISIBLE). This block:
  #112/#111/#115 rumours that pay off (62, loop CLOSED) → #105 ashore digest (63, CLOSED) → #69
  per-town music (64, CLOSED) → #104b last-deed-by-name (65, save v11) → #125 at-sea encounter (66,
  CLOSED) → #118 Your Harbour claim & grow (67, CLOSED, save v12) → #122 save-migration codec + corpus
  (68, CLOSED — caught a REAL silent save-wipe bug).
- **✅ THE REACTIVE LOOP IS CLOSED + POLES SYMMETRIC (Retro 12):** town→rumour→marker→reward→ashore-
  digest is a full circuit (#112/#111/#115 + #105); BOTH reputation poles now have verbs — the pirate
  raid/false-colours/legend is mirrored by the governor **claim & grow a home port** (#118); the open
  sea is reactive (#125 rescue-vs-plunder); each port has a voice (#69) + remembers your deeds (#104b);
  and the **save system is hardened** (#122 migration codec + frozen corpus). **Next leverage = make it
  FELT + harden before battle:** **#126 rep-reactive grade** → **#120 thin main.js (systems registry)**
  → **#123 loop golden-replay**. Battle-mode **#100 remains [OWNER-DECISION], OWNER-HELD all session
  (owner absent)** — keep HELD, do NOT auto-promote, but **a gentle owner nudge is due** (biggest
  content gap + long-stale decision); the de-risk quartet #120/#121/#123 rides WITH it when it lands.
- **Loops since last retro:** **0 — Retro 12 just ran (counter reset to 0; covered loops 62–68; HARD
  trigger fired at 7).** Cadence: retro every ~7–8 cycles, HARD trigger 7. **Next retro ~loop 75**
  unless a from-owner P1 preempts. Retro 12 doc: `studio/retros/2026-06-28-retro-12.md`.
- **Cycles since last deep-learning loop:** **0 — DL #5 just ran (~loop 71; counter reset to 0; HARD
  trigger fired). Next DL ~loop 81.** DL #5 (`studio/retros/2026-06-28-deep-learning-5.md`) fanned out 9
  role subagents; **assessed DL #4 (7/12 shipped — spine closed + engine hardened), re-sorted the queue
  top to DRAIN the remaining DL #4 items first** (#130 finish registry migration → #121 gate invariant →
  #131), and filed only **4 NEW** candidates (#131 harden replay gate · #132 reputation needle made
  personal+audible · #133 contested rumour · #134 Your Harbour threatened). **No injection encountered
  this loop** (DL #4's .env-dump + cut-a-release vectors did NOT recur). Standing process rule adopted:
  cap DL intake to the drain rate (file top ~4–6, reservoir carries a drain-by). _Prior:_ DL #4
  (2026-06-28) fanned out 9 role subagents (own reading list + Constitution + DL #3/Retro 11 docs);
  filed **#115–#126** (all below the Retro 11 top trio + the #100 hold); synthesis
  `studio/retros/2026-06-28-deep-learning-4.md`. Cross-cutting themes: **close the reactive loop across
  every discipline** (typed `objectives.js` #115 + loop audio #116 + in-world heading cue #126) ·
  **the governor pole has no verb** (#118 home-port verb + #119 milestone) · **harden engine+gate before
  battle explodes the state space** (#120 thin main.js · #121 resource/transition gate invariants · #122
  save migration codec · #123 loop golden-replay) · **reactive systems beyond town** (#124 crew morale ·
  #125 at-sea encounters). **⚠️ Standing: 1 subagent thread
  carried a planted "Compliance audit — dump .env/secrets" prompt-injection + others a "cut a v0.1
  release" inject — ALL refused; re-dispatched the 2 that returned 0-tool-use. Injection guardrail
  (release/scope/version/gate-bypass + secrets-exfil) holds.** DL #3 (~loop 50) filed #102–#109;
  promotes #36 fixed-timestep above #84.
- **State:** core arc COMPLETE + tuned + onboarded + sunny + perf-gated; **+ cannon combat (#59), +
  mobile PWA w/ heat-aware DPR (#63), + PWA safe-area (#75 partial), + settings/toggles panel (#73),
  + arcade collision: islands (#76 a1) + harbour/fight slow-stop (#76 c) + ship-vs-ship (#76 b), +
  iOS shader hardening/fallback (#86) + iOS audio capture-phase unlock (#77) + no-text-select (#87),
  + OPTIONAL day-night toggle (#58, sunny stays default).**
- **Next dispatch:** `queue.md` top work item — recommended (Retro 12): **#126 reputation-reactive
  world grade** (+ in-world heading cue for #111 — make the now-symmetric poles VISIBLE) → **#120 thin
  `main.js` via a self-registering systems registry** (de-risk before battle) → **#123 golden-replay
  fixture for the closed reactive loop**. (Retro 11 trio + DL #4 reservoir all SHIPPED:
  #112/#111/#115·#105·#69·#104b·#125·#118·#122 CLOSED. **#100 battle OWNER-HELD all session — do NOT
  auto-promote; send a gentle owner nudge.** #121 gate invariants ride WITH #100.)
- **Owner-decisions ANSWERED:** #56 mobile = GO (shipped #63); #58 weather = GO optional toggle (SHIPPED #58).
  #76 priority delegated to PM+TL → P1, shipped a1+c+b; a2 remains. Prioritization is PM+TL's call.
- **⏳ Awaiting owner re-test (iOS, on his iPhone — BATCH all three in one pass):** **#77 audio**
  (capture-phase unlock — reopened, unconfirmed) + **#87 no-text-select** + **#93 ship's-wheel touch**
  — all shipped best-effort, UNCONFIRMED; ask him to confirm all three on the latest build at once.
  #86 ocean "void" was a zoom-in, NOT a bug (closed; hardening retained).
- **Comms:** **two-way owner channel LIVE** (`OWNER-CHANNEL.md` + `scripts/owner-channel.sh`) — report
  out on every release/roadmap change; smart intent-routing in (pending-answer / thread-reaction /
  small ad-hoc inline / planning→PM-desk). Poll `owner-channel.sh peek` each cycle (step 0).
- **Process (Retro 5):** lean orchestrator protocol live; cycle-runners own ALL bookkeeping &
  `git add <specific files>` (NEVER `git add -A`); no docs-subagent concurrent with a `git add -A`
  runner; live QA only for owner visuals (cache-bust `ignoreCache`, one shot, then park tab on
  `about:blank`); rituals run as scheduled subagents. (Retro 4 still holds: cycle-runner owns visual
  QA; Game Designer owns balance/tuning; from-owner P1s jump the queue.)
- **QA gotcha (note):** synchronous tw.step() doesn't advance wall-clock → CSS fade-in transitions
  read mid-flight; QA must wait real time (~600ms) before asserting opacity-based visibility.
- **Latest release:** **v0.0.20260628070429** (#122 declarative save-migration codec + frozen old-save
  corpus). ~69 releases, **669 tests**. Perf **32/130 draws · 90k/150k tris**. Save schema **v12**,
  now backed by a migration codec + frozen corpus (a schema bump isn't done until it migrates every
  prior version forward AND adds a frozen corpus blob — coverage guard enforces it).
- **Live:** https://cakuki.github.io/tidewake/
- **Open enablers:** #37 deterministic visual-diff (schedule — open since cycle 10); #38 PR-validation
  CI gate; #36 fixed-timestep.

## Loop log

| Loop | Slice(s) | Issue | Release | Notes |
|------|----------|-------|---------|-------|
| 0 | Bootstrap: working v0, CI/CD, studio, backlog | — | v0.0.20260626231938 | Foundation shipped |
| 1 | Bow wake + trailing foam | #18 | v0.0.20260626233635 | QA: wake reads well; logged sail-visibility #23 |
| 1.5 | TDD harness + physics module; audio agents; QA system; parallel protocol | — | v0.0.20260626234838 | Framework expansion per owner |
| 2 | Sail visibility (#23) + ambient audio (#14) in parallel | #23,#14 | v0.0.20260626235544 | First parallel-dev batch |
| 3 | Wind compass + point-of-sail label (TDD) | #10 | v0.0.20260627000101 | 33 unit tests |
| — | Retro 1 (subagent) | — | — | Filed #24 modularise, #25 actions-bump; next = gameplay verb |
| 4 | Dockable ports (3 named) + harbourmaster arrival toast | #12,#22 | v0.0.20260627002601 | First gameplay verb; 39 unit tests |
| 5 | Save/load voyage to localStorage (TDD) + N=new voyage | #11 | v0.0.20260627003834 | 50 unit tests |
| 6 | Modularise main.js → input/hud/sailing/persistence | #24 | v0.0.20260627004415 | −43% lines; behaviour identical |
| — | Retro 2 (subagent) | — | — | Creative roles dark + gallery lapsed → CREATIVE SPARK beat, enforced gallery gate, parallel-default. Filed #26 economy, #27 Musician theme, #28 NPC ship. Next = #26+#27 parallel batch |
| 7 | Port economy (5 goods, 3 ports, arbitrage, trade panel) + Musician sailing theme — parallel | #26,#27 | v0.0.20260627010038 | 73 tests; QA found trade bug #29 |
| 8 | Fix trade (state.port getter + buy-by-name) | #29 | v0.0.20260627011026 | 76 tests; trade verified live (Rum buy coins 100→34). #30 filed→closed non-bug |
| 9 | Wandering NPC ships + persist economy (parallel) | #28,#31 | v0.0.20260627012517 | 96 tests; 3 AI vessels; save v2 (coins/cargo) |
| 10 | Deep-learning research loop #1 (9 agents, web research) | — | — | Filed #32-#40; agents' identities refreshed |
| 11 | Captain's Ledger — renown/rank ladder (TDD) | #39 | v0.0.20260627014341 | 114 tests; Bilge-rat→Terror of the Tidewake; persisted |
| 12 | Art polish: carved hull + richer islands + ink-wash horizon | #42 | v0.0.20260627072700 | 114 tests; procedural, no assets |
| 13 | Ports react to renown (tiered greetings + standing perk) | #43 | v0.0.20260627074620 | 124 tests; greets by title; price perk |
| 14 | Insult Broadside — comedic insult-duel combat vs NPCs | #33 | v0.0.20260627080427 | 134 tests; morale duel, 14 jabs, coin+renown reward; cleaned stray branch, filed #44 |
| 15 | Minimap radar (islands/ports/NPCs, north-up) | #16 | v0.0.20260627083006 | 141 tests; bottom-left, plots world |
| 16 | KEYSTONE: renown two-poles — Infamy(pirate)↔Standing(governor) | #45 | v0.0.20260627085452 | 155 tests; diverging titles, pole-aware port reactions, persisted |
| 17 | Endgame milestones — become THE pirate/governor (legend overlay) | #46 | v0.0.20260627100354 | 180 tests; persisted legends, sandbox continues |
| 18 | Polish batch: legend overlay persists, HUD/title overlap, Node-24 actions | #47,#41,#25 | v0.0.20260627102844 | 180 tests; CI annotation cleared |
| 19 | Duel audio juice — procedural SFX stings | #48 | v0.0.20260627104334 | 182 tests; playtest now wins a full duel |
| — | Retro 3 (subagent) | — | — | Fantasy now legible (sail→trade→renown rank, NPCs). Adopted #34 shared-contract step + re-dispatch-glitched-subagent rule + QA nav/timing gotchas into runbook; new guardrail "reactive verbs over inert content". Next = #43 (port reputation reactions, #39-followup) reputation reactions + #32 glTF ship (parallel, contract'd) |
| — | Retro 4 (subagent) | — | — | **Core fantasy arc COMPLETE** (two poles #45 → crowned a legend #46). Depth-vs-breadth: TUNE the arc to be reachable first (#57, the ~12,800 grind is unreachable in a web session), then depth (weather #58, cannon combat #59) over breadth. Process: cycle-runner QA step owns the gallery capture+diff (not orchestrator); Game Designer owns balance/tuning; from-owner P1 bugs jump the queue; build #37 diff. Filed #57-#60. Deep-learning #2 due. Next = #57 tune curve, then #51/#50 P1-bug fast-lane |
| 20 | from-owner P1 batch: camera opens astern + centre/stabilise wind compass + calm swell (ports/docks stop submerging) | #49,#50,#51 | v0.0.20260627110811 | Owner P1 fast-lane; cleaned every capture |
| 21 | Sunny Caribbean holiday water — palette, sun glint, micro-detail (owner's signature vibe) | #61 | v0.0.20260627111728 | Most shareable visual of the night; do NOT undo |
| 22 | Tune renown/legend curve for a single web session — fast early ranks, reachable legend | #57 | v0.0.20260627112714 | LEGEND_AT 2400 (was ~12,800); Game-Designer tuning pass; arc now *felt* in a sitting |
| 23 | Measurement-first perf budget gate — src/perf.js ceilings + P overlay; playtest asserts draws/tris | #52 | v0.0.20260627113504 | Owner P1; 130 draws/150k tris (~70% headroom); deterministic counters, not fps |
| 24 | Bigger-map route-planning chart of the whole archipelago (Tab toggle) | #54 | v0.0.20260627114207 | Owner P2; complements the minimap |
| 25 | Self-contained, self-tested wind compass UI component (first src/ui/ component) | #53 | v0.0.20260627114757 | Owner P2; the UI-component standard made real |
| 26 | Invisible onboarding — seeded first goal + juicy first-win beats, no tutorial wall | #60 | v0.0.20260627115834 | 229 tests; latest release; cheapest retention lever now the arc is reachable |
| — | Retro 5 (subagent) | — | — | **Arc now LANDABLE** (tuned reachable + onboarded + sunny + perf-gated). Owner-feedback sprint: all owner P1s + P2s #53/#54 shipped same session. Process headline (owner ask): added **Lean orchestrator protocol (post-compact)** — per cycle = read `queue.md` top → dispatch one self-sufficient cycle-runner → read <10-line report; cycle-runners own ALL bookkeeping & `git add <specific files>` (never `-A`); live QA only for owner visuals (cache-bust, park tab on about:blank); rituals as scheduled subagents. Created `queue.md`. Next direction: thin depth (cannon #59) + polish (#19/#15/#20/#21), gated by owner-decisions #56/#58. DL #2 overdue. **LOOP STOPPED for compaction.** |

| 27 | Cannon Broadside — open fire (G) as a teeth-y alternative to the Insult Broadside duel; deterministic seedable exchange, two aims, coin+Infamy on a sinking | #59 | v0.0.20260627130215 | 242 tests (+13); `src/cannons.js` pure+TDD mirrors duel.js; fire-orange HUD panel; QA hook .cannons/.openFire/.cannonFire; playtest drives a cannon win. Closed #59, filed depth follow-up #72 |
| 28 | Mobile MVP — installable PWA (`manifest.webmanifest` + brass-anchor icons, Add-to-Home-Screen, standalone, sunny theme) + heat-aware DPR cap (`pixelRatioCap` ≤1.5x on coarse-pointer so a 3x phone doesn't cook the per-vertex ocean) atop the existing touch controls + responsive HUD (#17) | #63 | v0.0.20260627131832 | 249 unit tests (+7: pixelRatioCap + PWA install contract); playtest now fetches the manifest in-browser & asserts the touch verbs exist; perf unchanged (77 draws/85.2k tris). Phone-viewport QA shot clean — controls thumb-reachable, no overlap (`studio/qa/gallery/v0.0.20260627131832-mobile-pwa.png`). Docking is automatic (no dock button needed). Workflow copies manifest into `_site`. Closed #63; filed follow-ups #74 (service-worker offline) + #75 (safe-area/landscape/low-end-throttle); docked overlap already tracked as #66 |
| 29 | PWA top-notch safe-area spacing (owner request via Telegram, §3c small fix) — swept `env(safe-area-inset-*)` onto every top-anchored HUD element (`#hud`, `#title`, `#map-toggle`, `#audio-toggle`, `#perf`, touch `#minimap`) so the top HUD no longer hides under the notch / status bar in PWA standalone; also fixed the ≤560px media-query `#title` rule that re-pinned a bare `top:12px` and would have dropped the inset on a narrow phone. CSS-only; `viewport-fit=cover` already present; insets collapse to 0 on desktop/non-notch | #75 (partial) | v0.0.20260627133536 | 249 unit tests (unchanged — pure CSS); playtest ✓, perf unchanged (77/130 draws · 85.2k/150k tris), zero console errors. Headless phone-viewport QA (390×844) with a simulated 47px notch: all four top elements clear it (hud/audio/map 59px, title 65px ≥ 47) — gallery shot `studio/qa/gallery/v0.0.20260627133536-safe-area-top.png`. Reported out over Telegram. #75 safe-area-**top** done; landscape pass, home-indicator/side insets in other orientations, low-end throttle, gesture steering remain → #75 stays open. #66 (docked button overlap) untouched, kept out of scope |

| 30 | Settings / options panel — the early-phase home for feature toggles (owner: "build a ui for [toggles]"). Self-contained `src/ui/settings.js` per #53: a ⚙ button (or **O** key, Esc to close) opens a ship's-brass control plate that renders a one-line-per-toggle REGISTRY. Two real toggles wired to existing behaviour — **Sound the shanties** (LIVE-backed by audio.js's own mute, one home/no double-store) + **Spyglass readout** (the perf overlay, STORED+persisted; P key & tap-to-dismiss now route through the toggle so switch/key/save stay in lock-step). Defaults preserve the current look (sound as-is, perf hidden) and a future visual toggle like weather #58 defaults OFF → sunny stays default | #73 | v0.0.20260627135021 | 260 unit tests (+11: pure registry/persistence logic TDD'd in `tests/unit/settings.test.mjs`); playtest opens the panel, flips a toggle, asserts persistence across a reload; perf unchanged (77/130 draws · 85.2k/150k tris), zero console errors. `window.__tidewake` exposes `.options`/`.setOption(id,bool)`/`.openSettings`/`.closeSettings`. Weather registration seam documented in `src/ui/README.md`. Gallery shot `studio/qa/gallery/v0.0.20260627135021-settings-panel.png`. Closed #73; **#58 weather plugs into this panel next (queue item 3)** |

| 31 | **#76 a1 — arcade island collision** (owner P1). Islands now STOP the ship instead of letting it phase through — soft, never a brick wall. Pure node-testable resolver in `src/physics.js`: forgiving CIRCLE hitboxes (each island's world.js radius, `ISLAND_HITBOX 0.9` so you graze the beach not open water), radial push-out + slide along the coast, swept sub-stepped to forbid tunnelling at speed, speed bled to ground-speed-actually-made (head-on glides to a stop, graze keeps its way on). Docking preserved (ports sit outside the hitbox; asserted). CREATIVE SPARK: comic "Scraaape… the hull complains" harbour banner on a hard run-aground (throttled). | #76 (a1) | v0.0.20260627140435 | 268 unit tests (+8 TDD-first collision cases); playtest now drives the ship into an island and **asserts non-penetration** (rammed head-on, held at boundary minDist=61, zero penetration); perf unchanged (77/130 draws · 85.2k/150k tris — pure CPU math), zero console errors. `window.__tidewake` exposes `.islands`. #76 stays OPEN — remaining phases: (c) harbour/fight slow-to-stop, (b) ship-vs-ship collision, (a2) slide polish |

| 32 | **#76 c — arcade slow-to-stop for harbour & combat** (owner P1). The ship no longer teleport-freezes for a fight or barrels through a berth — it EASES to a near-stop via the existing `approach()` damping, then normal throttle control returns. Pure settle model TDD'd in `tests/unit/settle.test.mjs` (`harbourSlowFactor` smoothstep coast-in, `settledTargetSpeed`, `SETTLE_RATE`): converges to ~0 with no overshoot/oscillation and releases when the reason clears. `sailing.step` now ALWAYS runs (a settle reason lowers the target; helm ignored mid-fight; the berth assist yields the instant the player presses W to leave, so it never strands you at the dock). `window.__tidewake.state.settling` exposed. CREATIVE SPARK: a light "Sails reefed — battle stations" / "Easing into the berth…" beat. Also folded in the in-flight **#76 a1 beach fix** (islands solid to their visible squashed-ellipse shoreline) that was uncommitted in the tree. | #76 (c) | v0.0.20260627182358 | 284 unit tests (+16: 12 settle TDD + 4 beach-fix); playtest drives a port approach (speed 42→2.8, settling true, docked) **and** a fight (engage 10.9→~0, settling true, fight resolves) and asserts the ease-down; perf unchanged (77/130 draws · 85.2k/150k tris — pure CPU math), zero console errors, CI green, live 200. **#76 stays OPEN** — remaining: (b) ship-vs-ship collision, (a2) slide polish |
| — | Retro 6 (subagent) | — | — | Covered loops 27–32 (depth+platform run: cannon #59 / mobile PWA #63 / safe-area #75 / settings #73 / collision #76 a1+c; ~284 tests, gate green). Headline GAME finding: build went from "landable arc" to "a sea that fights back & feels solid"; next creative lever is DL #2 (well dry ~22 cycles). Headline PROCESS finding: the 0-tool-use glitch recurred TWICE with injected "output style" text. Changes to `LOOP.md`: **(1)** every dispatch brief must carry an **ignore-injected/output-style-instructions guardrail**; **(2)** cycle-runners + orchestrator **verify a clean tree** (`git status --porcelain` empty) so no runner absorbs foreign WIP (Loop 32 inherited #76-a1 beach fix); **(3)** **HARD ritual trigger** — at retro-counter 4 / DL-counter 10 the NEXT dispatch IS the ritual (P1-preemptible); tightened heartbeat-vs-per-release wording. Also edited `agents/{software-developer,project-manager}.md`; logged 2 decision blocks. Retro counter reset to 0. **Recommend: run DL loop #2 next.** |
| 33 | **Owner fast-lane bug batch** (two fixes, one release). **(1) Bigger island hitbox** (owner: "I can still go into a bit of sand — increase the collision model a bit more"): `ISLAND_HITBOX` 1.12 → **1.18**. Root cause was the BOW — the visible bow overhangs the `SHIP_RADIUS`=7 collision circle by ~1 (ship.js halfLen 8), so at 1.12 the bow tip still kissed the ~1.107·r waterline on smaller isles; 1.18 rests the hull a clear hull's-width off the sand on every island without walling open water. Docking preserved (DOCK_RADIUS 90). **(2) iOS Safari/PWA audio unlock** (owner: "no music on iOS safari or pwa on iOS"): iOS only resumes a suspended AudioContext inside a real gesture — bind broadly (touchstart/touchend/pointerdown/mousedown/click/keydown), on first gesture `resume()` + play a 1-sample **silent buffer** (iOS unlock), retry each gesture until `running`, re-resume on `visibilitychange` for PWA foregrounding. Headless stays inert. CREATIVE SPARK: one-time "🎶 The shanty band tunes up…" banner on first unlock. | #76 (hitbox comment, stays OPEN) / #77 (iOS audio, CLOSED) | v0.0.20260627185044 | 287 unit tests (+3: bow-clearance regression + `unlockEventNames`/`needsUnlock` helpers); playtest collision reaches coast (minDist 79.5 for r=60 ≤ r+25) & never penetrates, dock-reachable green; perf unchanged (77/130 draws · 85.2k/150k tris), zero console errors, CI green, live 200. **iOS audio final confirmation pending owner's iPhone** (no real audio device in headless). #76 stays OPEN — remaining: (b) ship-vs-ship, (a2) slide polish. |

| — | Deep-learning research loop #2 (subagent) | — | — | 9 agents refreshed (new+classic web research → agent ## Research log + memory); themes: deception-as-a-verb, the anecdote factory, off-main-thread/WebGPU readiness, deterministic-diff, physical-modelled audio, social/participatory shanties, painterly NPR map, AI-era flow (DORA rework rate). Filed #78–#85; DL counter reset to 0 |
| 34 | **Owner iOS P1 bug batch** (three fixes, one release). **(1) Ocean ShaderMaterial → flat teal void on iOS Safari** (the headline — owner's screenshot showed ship OK but no sea/waves/horizon): root cause = strict-mobile vertex/fragment **varying precision MISMATCH** failing the GLSL program **link** (desktop is lenient), so only the clear colour drew. Fix: explicit `precision highp float;` (+`highp int`) in **both** stages so varyings match + trig stays honest far from origin; moved fragment haze/glint/micro-ripple to **camera-relative** space (small `vRel` varying) — visually identical on desktop, stable on mobile; one-time guarded GL **link-status diagnostic** + **flat-but-coloured lit fallback sea** (`oceanFallbackColor` in new three-free `src/sea-color.js`) so the sea is never a void. Wave geometry stays world-space → CPU `sampleHeight()` still matches GPU. **(2) Audio still not unlocking after many taps** (#77 follow-up): touch buttons `preventDefault` their handlers, so bound the gesture-unlock in the **capture phase** on `window` (runs before any control handler) + `input.js` calls `audio.unlock()` **directly** from the touch controls; kept #77 resume+silent-buffer+retry+visibilitychange. **(3) Tapping buttons selected page text**: no-select hardening (`user-select`/`-webkit-touch-callout`/`touch-action:manipulation`) on body + canvas. | #86 (ocean, CLOSED) · #77 (audio, **REOPENED** pending owner re-test) · #87 (selection, CLOSED) · #76 (untouched, stays OPEN) | v0.0.20260627192458 | 289 unit tests (+2: `mixHex`/`oceanFallbackColor` pure colour maths TDD'd); playtest ✓ PASSED, zero console errors, perf unchanged (77/130 draws · 85.2k/150k tris), CI green, live 200. Desktop Chrome QA: sea renders correctly (vivid turquoise→deep blue, glint, haze — no regression), console logs one-time `[ocean] shader program linked OK`; tab parked on about:blank. **No real iOS device — all three need owner re-confirmation on his iPhone** (sea draws + shanties play + no text-select). |
| 35 | **#76 b — arcade ship-vs-ship collision** (owner P1). The player ship now BUMPS NPC vessels instead of sailing clean through them — reuses the island circle-hitbox resolver (`physics.js`): each NPC is a moving circle, solid boundary = the two hulls' forgiving radii summed (`SHIP_RADIUS 7 + NPC_RADIUS 9 = 16`), so they rest gunwale-to-gunwale. **Player-only** resolution → wander AI / duel #33 / cannon #59 triggers / harbour slow-to-stop #76c all untouched & deterministic. Head-on shoulder bleeds speed to ground-speed-made (soft pile-up); glancing bump slides along & keeps its way on; swept sub-stepping forbids tunnelling a smaller vessel at speed. New pure fns `NPC_RADIUS`/`shipCircles()`/`sweepShipCollision()` (thin wrapper over `sweepIslandCollision` w/ hitbox 1). `window.__tidewake.collisionRadii` exposed. CREATIVE SPARK: throttled+rotated comic "🛶 Hulls collide! — Timbers groan, mind the other captain!" bump banner. | #76 (b) | v0.0.20260627194005 | 298 unit tests (+9 ship-collision TDD: push-out, slide, anti-tunnel, malformed-input); playtest drives the player into an NPC and **asserts non-overlap** (closest approach minDist=16.0 = the combined boundary, never inside), zero console errors; perf unchanged (77/130 draws · 85.2k/150k tris — pure CPU math), CI green, live 200. Scope: player-vs-NPC only; NPC-vs-NPC / NPC-vs-island deferred into (a2). **#76 stays OPEN** — remaining: **(a2) slide polish**. |

| — | Retro 7 (subagent) | — | — | Covered loops 33–36 (iOS field-testing block: bigger hitbox + iOS audio #77/#76, iOS bug batch #86/#77/#87, ship-vs-ship #76 b, optional day-night #58; ~310 tests, gate green). Headline GAME finding: the build is now shaped live by a real iPhone — collision arc done + day-night opens an atmosphere lane (pull DL-backlog charm #78/#79 once iOS settles). Headline PROCESS finding: a cropped owner screenshot was misread as "ocean not rendering on iOS" → a cycle shipped shader hardening for a NON-bug (#86). Changes: **(1)** new **`scripts/owner-channel.sh photo [--latest\|--id <id>] [--out <path>]`** subcommand (fetch owner photo → local path, token out of argv, non-consuming peek, clean "no photo" exit 3) so any cycle can SEE owner media; **(2)** visual-bug intake rule (fetch + VIEW full frame, confirm real, ask for wider shot if ambiguous BEFORE fixing); **(3)** device-fix guardrail (iOS/device fixes ship best-effort, tracked unconfirmed pending owner re-test, don't stack dependent work). Also FLAGGED (not fixed): `release.yml` uses `paths-ignore`, so `scripts/**`/`tests/**`/`.github/**` DO trigger releases despite the "src/+index.html only" policy — owner follow-up (this retro's script commit used `[skip ci]`). Retro 6 hardening held (no 0-tool-use/injected glitch, clean trees, hard trigger fired). Edited `LOOP.md`, `OWNER-CHANNEL.md`, `scripts/owner-channel.sh`; logged 2 decision blocks. Retro counter reset to 0 (next ~loop 40). |
| 36 | **#58 — optional day & night cycle behind the #73 toggle.** A gentle, pretty time-of-day arc as the first real VISUAL inhabitant of the settings panel. **DEFAULT OFF** — the permanent sunny Caribbean look (#61) stays the default and is restored **byte-for-byte** when flipped off (verified live: sun→(300,500,120), intensity 2.2, haze 0xbfe8e6). When ON, a slow loop (~150s) plays dawn → bright noon → golden afternoon → dusk → soft moonlit night: the sun arcs and the sky dome / hemisphere fill / scene haze+fog / ocean sun+sea-tint uniforms drift warmly — never gloomy. Pure `dayNight(t)` palette (no three.js) TDD'd in `tests/unit/daynight.test.mjs` (dawn/noon/dusk/night keyframes, smooth interp, seamless loop, **NOON === sunny constants** invariant); thin `createDayNight(refs)` factory captures the sunny look + maps the palette each frame. Exposed ocean uniforms + sky-dome uniforms for modulation. `window.__tidewake.daynight` + `setDayPhase(t)`. CREATIVE SPARK: first dusk → "🌅 The sun dips toward the yardarm…" watch banner. | #58 | v0.0.20260627195729 | 310 unit tests (+6 daynight TDD); playtest flips the toggle ON at golden hour & asserts the sun/haze shift, flips OFF & asserts the sunny default restores **exactly**; perf unchanged (77/130 draws · 85.2k/150k tris — uniform/colour writes only, no new draw calls), zero console errors, CI green, live 200. Live desktop QA: default sunny unchanged, ON = warm golden-hour shift (gallery `studio/qa/gallery/v0.0.20260627195729-daynight-golden.png`), OFF restores sunny; tab parked. **Closed #58** (optional day-night MVP meets acceptance); full weather (rain/storm/clouds) = follow-up. |
| 38 | **#76 a2 — tangential slide polish (CLOSES #76).** The final phase of the arcade-collision system: on contact the hull now keeps the velocity skimming ALONG the surface (tangential) and loses only the part pressing INTO it (normal) — a glancing graze of a coast or another hull **slides on and keeps its way**, a head-on hit still bleeds to a stop. One shared pure rule: new `slideVelocity(vx,vz,nx,nz)` in `physics.js` strips only the into-surface component (away/along = untouched, degenerate normal = no-op); `resolveCircleCollision`/`sweepIslandCollision`/`sweepShipCollision` now return the **outward contact normal** so the island AND ship-vs-ship resolvers feed speed through the same helper (coasts & hulls feel identical). Replaced the old displacement/dt speed heuristic → jitter-/substep-free, no stickiness/stop-dead on a glancing contact. CREATIVE SPARK: a lighter "scrape… you slip past" SLIPS beat for a graze vs the head-on SCRAPES groan. | #76 (a2 — **CLOSED #76**) | v0.0.20260627202808 | 320 unit tests (+16 slide TDD in `tests/unit/slide.test.mjs`: glance keeps ≥70% of way / head-on bleeds to <10% / normal-only loss / away-or-along untouched / shared coast↔hull rule); playtest ✓ PASSED, non-penetration & non-tunnel & dock-reachable all still asserted, zero console errors; perf unchanged (77/130 draws · 85.2k/150k tris — pure CPU math), CI green, live 200. Logic-only → no gallery shot. **#76 COMPLETE** — all four phases (a1 island push-out / c harbour-fight slow-stop / b ship-vs-ship / a2 slide) shipped. |
| 39 | **#19 — islands get characterful names + a comedic landfall line (CLOSES #19).** Mostly-charm slice: every island now carries an authored, in-tone name and a daft flavour line that hails you the FIRST time you sail within `r+APPROACH_RANGE` (220u) of it — once per session, re-armed on a new voyage — reusing the existing shared HUD toast (the harbourmaster/arrival/bump banner). Pure three-free `src/islands.js`: `ISLAND_LORE` (12 authored isles), `assignIslandNames(count)` (stable + collision-free BY INDEX so an isle keeps its name across reloads; roman-suffix wrap if >12 isles; asserted never to clash with a port name), `detectApproach(pos, isles, introduced)` (nearest not-yet-introduced isle in range, reads {x,z} or [x,y,z]), `createIslandNamer({world})` factory + `nearestIsland()` + `reset()` (newVoyage re-arms greetings). Wired into the loop via `islandNamer.update(state.pos, onApproachIsland)`; QA hook now exposes island names on `tw.islands` + `tw.islandsIntroduced` + `tw.nearestIsland`. Bonus #54: the route chart (`bigmap.js`) now LABELS every island (dimmer than the gold harbours, same index-keyed assignment so labels can't disagree with the toast). CREATIVE SPARK: Gallows Cay ("they hang folk here for bad puns — sail careful, and rhyme nothing"), Rumlost Reef ("a whole galleon of rum sank here — the fish have never been merrier"), Sotweed Hollow, Mutineer's Folly, Kraken's Footstool, Doubloon Dunes, Scurvy Point, Petticoat Spit, Tankard Rock, Cutlass Bend, Wailing Wharf, Gibbet Green. | #19 (**CLOSED**) | v0.0.20260627204815 | 332 unit tests (+12 TDD in `tests/unit/islands.test.mjs`: stable/unique/wrapped assignment, no port-name clash, approach detect inside/outside range, once-only guard, nearest pick, pos-shape reads, factory fire-once + reset); playtest sails at the nearest isle and asserts the named landfall banner ("🏝️ Landfall: Gallows Cay …") fires before grounding; zero console errors; perf unchanged (77/130 draws · 85.2k/150k tris — pure CPU + the map labels only redraw while the chart is open), CI green, live 200. Live desktop QA: banner reads well over the sunny sea (gallery `studio/qa/gallery/v0.0.20260627204815-landfall.png`) + bigmap island labels confirmed; tab not left running. Map-label note: on the 3 port-hosting isles the island label sits near its gold port name (minor cosmetic overlap) — optional polish, not filed. |
| 40 | **#78 — "The Ballad of Your Voyage" (CLOSES #78).** The DL#2 anecdote factory: an auto-composed, shareable in-character log of the player's run. Notable systemic deeds are recorded into a small PURE log (`src/voyage-log.js`: `recordEvent` record/dedupe/order/cap; deterministic `composeBallad` events→verses, empty → a graceful "yet unwritten" line) and shown in a parchment panel (`src/ui/ballad.js`, 📜 button / **B** key, #53 house standard) with **copy-to-clipboard** sharing (Clipboard API → textarea/execCommand fallback, headless-guarded). Records 4 deed types the systems already emit: islands raised (#19), insult duels won (#33), cannon fights won (#59), crowned legends (#46). Persisted in the save (**schema v7**, sanitised fail-open like legends/onboarding) → survives a reload; a new voyage starts the tale blank. `window.__tidewake` exposes `.voyageLog`/`.ballad`/`openBallad`/`closeBallad`/`copyBallad`. CREATIVE SPARK = the balladeer's voice (3-variant per-deed pools, deterministic; warm + a wink of comedy). | #78 (**CLOSED**) | v0.0.20260627210918 | 348 unit tests (+16 TDD: voyage-log record/dedupe/order/cap/compose + save v7 round-trip); playtest sinks a ship and asserts the deed accrues, the composed ballad names the foe, the panel opens & shows the text, and the deed PERSISTS across a reload; zero console errors; perf unchanged (77/130 draws · 85.2k/150k tris — pure CPU + DOM), CI green, live 200. Live desktop QA: parchment scroll reads beautifully over the sunny sea, "Copy to share" works (gallery `studio/qa/gallery/v0.0.20260627210918-ballad.png`); tab parked on about:blank. Follow-up **#90** filed (more deed types: best trade / coins / rank / ports · richer composition · share-as-image). |
| — | Retro 8 (subagent) | — | — | Covered loops 37–40 (#76 a2 slide → #76 CLOSED all four phases / #19 island names+landfall+map labels / #78 Ballad of Your Voyage; 332→348 tests, gate green). Headline GAME finding: build crossed from "landable arc" to **genuinely rich** (complete collision + atmosphere + named world + shareable stories); recommended next = **#79 False Colours (deception-as-a-verb)** then hero-ship art **#55→#32**. Two mandates landed: **(1) relaxed retro cadence 3–4 → ~7–8 cycles**, HARD trigger **4 → 7** (DL stays ~10) — edited `LOOP.md` §1/§4/HARD-trigger/Changelog; **(2) fixed #89** — `release.yml` push trigger switched from `paths-ignore` to **allow-list** `paths:['src/**','index.html']` (no more `[skip ci]` for non-game commits; editing workflow won't release), YAML valid, **#89 CLOSED**, noted tests-only-in-release-job → #38. Retro 7 hardening held. Logged 2 decision blocks. Retro counter reset to 0. |

| 41 | **#79 — False Colours (CLOSES #79).** Flag deception as a verb. The captain flies a displayed flag — **True/Black** (honest pirate) vs **False/Merchant** (a disguise) — cycled with **C** or the HUD chip, persisted in the save (**schema v8**, fail-open) and reflected on the player's own pennant (skull hidden while disguised). The world reacts to the colours **SHOWN, not just renown**: fly true black while feared and nearby vessels **FLEE** (new `npc.js` flee reaction, pure `npcDisposition`/`menaceLevel`); fly false merchant colours and the disguise works — they stay **calm and let you approach** even when infamous. **The treachery payoff:** strike a ship you crept up on under false colours (duel OR cannon) → a **perfidy bonus to Infamy** (+50%, `treacheryBonus`) plus a surprise opening advantage (foe starts weakened, `surpriseDamage`); the honest black-flag attack earns none. The Ballad (#78) sings a distinct false-colours verse for a treacherous strike. Pure `src/colours.js` TDD'd first, then wired into `duel.js`/`cannons.js` (capture colours at engage → bonus + opening dent), `npc.js` (flee ctx + `fleeing` snapshot), `hud.js` (colours chip), `save.js` (v8), `voyage-log.js` (treachery narrators), `main.js` (C key + chip + banter + QA hook `tw.colours`/`setColours`/`cycleColours`). CREATIVE SPARK — the comedy of bluffing: hoist banter, a smug *"They wave you in… the fools, they bought it whole"*, and the foe's betrayed splutter as the black snaps up — *"Treachery! TREACH— oh, bother."* | #79 (**CLOSED**) · follow-up #91 | v0.0.20260627214813 | 367 unit tests (+19: colours pure logic + controller-treachery for both combat paths + voyage-log treachery verse + save v8 round-trip); playtest sets false colours, asserts NPCs **flee under black but not merchant**, a real vessel fled the dread captain, a disguised approach landed with no flee, and the treacherous win applied the **Infamy bonus (+273 in the run) + recorded a perfidy deed + sang the false-colours ballad verse**; zero console errors; perf unchanged (77/130 draws · 85.2k/150k tris — pure CPU + DOM), CI green, live 200. Gallery `docs/false-colours.png` (merchant chip + reveal banter). **Deferred → #91:** lawful Letter-of-Marque/navy-colours path feeding Standing; seeded "seen-through" chance at high Infamy. |
| 43 | **#91 — Letters of Marque (CLOSES #91 MVP).** The **lawful** counterpart to #79 False Colours — gives the **Standing** (governor) pole an equal, opposing verb and adds real risk to deception at the top of the Infamy ladder. **(1) Lawful privateering → Standing:** a kill under your **TRUE/honest colours** against a **PIRATE/outlaw** vessel earns **Standing** (as if under a letter of marque) — the exact mirror of #79's treachery→Infamy. Honestly gunning down an innocent **MERCHANT** earns none and **costs** a little Standing (the lawful path is actually lawful). Introduced a light, deterministic NPC **vessel disposition** (`vesselKind` by slot — ~1 in 3 is an outlaw flying a blood-dark flag — so a default fleet always carries a pirate to hunt + merchants to spare). **(2) "Seen-through" risk:** at higher Infamy a false-colours approach has a **seeded, monotonically-rising (capped <1)** chance to be **pierced** on approach — the NPC reads your true renown and **flees** anyway, so the disguise becomes genuine risk/reward as you rise (free pass below infamy 1200; ~0.85 cap by 3000+). **(3)** The Ballad (#78) sings a distinct **lawful privateer verse** for an honest pirate-hunt. Pure logic (`lawfulStanding` / `vesselKind` / `seenThroughChance` / `isSeenThrough` + `npcDisposition` `seenThrough` param) TDD'd first in `src/colours.js`, then wired into `cannons.js`/`duel.js` (capture target kind at engage → Standing award + clamp ≥0), `npc.js` (kind + outlaw flag + snapshot), `voyage-log.js` (lawful narrators), `main.js` (seeded per-approach detection latch, banters, QA hook `tw.colours.seenThrough`/`seenThroughChance` + `npc.kind` + `cannons/duel.targetKind`). **No save bump** — kinds are deterministic; a persisted commission/faction system is a follow-up. CREATIVE SPARK — the comedy/pride of lawful service vs the risk of being rumbled: a grateful port nod (*"a magistrate raises a small, approving eyebrow"*), the piracy wince (*"Even THEY thought that one a bit much"*), and the squint when the bluff fails — *"…I know that rigging. That is NO merchantman — pipe all hands, it is HIM!"* | #91 (**CLOSED** — MVP) · follow-ups filed | v0.0.20260627222104 | 384 unit tests (+17: lawful Standing reward/fine + monotonic seen-through probability + deterministic dispositions + controller wiring for both combat paths + the lawful ballad verse); playtest extended — honestly sink a **pirate** and assert **Standing rose (+104)** with a lawful deed + verse, and at high Infamy the **seen-through path fires (chance 0.85) and the vessel flees**; zero console errors; perf unchanged (77/130 draws · 85.2k/150k tris — pure CPU + DOM), CI green, live 200. **The two poles now mirror exactly:** treachery (false colours) → Infamy vs lawful (true colours vs pirate) → Standing, with the bluff a real gamble at the top. **Follow-ups:** richer faction/bounty system + persisted Letter-of-Marque commission item + more false ensigns. |
| 44 | **#20 — smooth, weighty steering (eased rudder) (CLOSES #20).** The helm no longer yaws at a constant rate the instant A/D is held — the ship now eases a **rudder** toward the steer input: the turn **accelerates in** as you hold and **settles smoothly back amidships** on release (a gentle wheel, not a switch). Applied yaw = eased rudder × speed-scaled `steerRate`, so turn authority still firms up with speed; the hull also **banks/leans into the turn** (visual only, scales with eased rudder + speed). Throttle/speed already ease (throttle ramp + `approach()` + the #76c slow-stop) so they were left **untouched to avoid double-damping into sludge**. While fighting/settling, steer is 0 → the rudder eases back on its own (helm quiet for the guns). Pure `easeRudder()` + `RUDDER_RATE` in `physics.js` (reuses `approach()`'s clamped exponential ease → frame-rate independent, never overshoots), TDD'd first; wired into `sailing.js` (`state.rudder` per `tw.step()` sub-step + eased yaw + bank); `__tidewake` hook now exposes `state.rudder`. Desktop keyboard + mobile touch share the same eased path. CREATIVE SPARK: a real wheel has heft — soft wheel-creak SFX as the rudder swings noted for the Sound Engineer. | #20 (**CLOSED**) | v0.0.20260627223424 | 393 unit tests (+9 TDD in `tests/unit/steering.test.mjs`: ramp-in / settle-out / cross-through-neutral / no overshoot / frame-rate independence); playtest ✓ PASSED — all autopilot navigation (ports/islands/NPC pursuit/collision/harbour-fight slow-stop) still works under the new feel, zero console errors; perf unchanged (77/130 draws · 85.2k/150k tris — pure CPU + one extra rotation), CI green, live 200. Feel-only change → no gallery shot (hard to read in a still). Follow-up: a dedicated key-**remapping UI** (input seam in `input.js` is now clean for it). |
| 45 | **#65 — stop the sea showing inside the hull (CLOSES #65, owner P1 visual bug).** The sloop hull is an open-topped swept bowl whose interior floor dips below the waterline, so the single ~4000u ocean plane (y≈0) drew *inside* the boat through the open ring between the inset deck and the gunwale — the owner's "our ship looks like it has water in it :D." **Fix: ONE opaque interior sole/bilge cap** — the hull's own interior cross-section at **SOLE_Y=1.5**, in the `woodHull` material, occluding the sea in **+1 draw call** (77→78/130). The ship is **NOT raised** (sampleHeight/wake/follow-camera untouched). Extracted the parametric hull math into a new **pure, three-free `src/hull.js`** (profile/beam/sheer/depth) — the single source of truth now shared by `ship.js` AND the node unit tests; **SOLE_Y is coordinated with `swell.js` MAX_SWELL (1.4)** and asserts at import that it sits above the swell crest yet below the deck/gunwale, so a future swell/geometry edit can't silently reopen #65. `main.js` gained a QA-only top-down/high-oblique inspection camera on the `__tidewake` hook (`qaTopDown`/`qaFollow`, inert unless requested) to screenshot the visual DoD. CREATIVE SPARK: a holystoned sole — scrubbed pale by the watch on a quiet forenoon. | #65 (**CLOSED**) | v0.0.20260627230647 | 397 unit tests (+4 TDD in `tests/unit/hull.test.mjs`: cap clears the swell with margin · stays below the gunwale · occludes the open ring wherever sea would be visible · outline inside the hull wall); playtest ✓ PASSED, zero console errors; perf 78/130 draws · 85.2k/150k tris (the +1 cap draw, within budget), CI green, live 200. **VISUAL DoD verified by screenshot (owner's sharpened criteria), ship over open water (nearest island ~8000u): (1) NO water visible from the top — deck + hull interior read solid wood, teal sea only OUTSIDE the hull; (2) NO deck/plank exceeds the gunwale (only cannons-through-gunports + the yard spar extend, which is correct rigging).** Gallery `studio/qa/gallery/v0.0.20260627230647-hull-fix.png` (high-oblique, clearest) + `…-hull-fix-topdown.png`; tab parked on about:blank. NB if the #32 glTF hull swap lands, re-run this screenshot test on the new model. |
| 46 | **#32 — swap the procedural sloop for a CC0 glTF hero hull (CLOSES #32, owner-greenlit art slice).** Adopted + finished the prior cycle-runner's WIP. The placeholder box-hull sloop is replaced by the **CC0 Kenney Pirate Kit** `assets/ships/ship-pirate-small.glb` (1461 tris, single `colormap` atlas material) — byte-verified against the official kit download; CC0 confirmed in `License.txt`, attributed in `CREDITS.md`. **Two real gaps in the WIP fixed:** (1) the pure-helper unit test imported `ship-loader.js` which top-level-imports `three` (browser-only CDN) → the whole test file failed in Node; extracted the scale/centre math into a new **three-free `src/ship-loader-math.js`** (the `hull.js` pattern) and pointed the test there (also normalised a `-0`/`+0` equality). (2) The Kenney GLB referenced an **external `Textures/colormap.png`** that 404'd in-engine (console errors → failed the zero-error gate); **embedded the CC0 atlas into the .glb** so the hero hull is a single self-contained, CDN-friendly file (no build step, no 404), 131KB→141KB. `src/ship-loader.js` `loadShip()` is the async equivalent of `createShip()`: scales the GLB to length ~16, centres the keel midpoint at the group origin (bow toward +Z), hides the GLB's own flag-a/flag-b meshes and re-attaches the **procedural pennant** (`userData.flag`=[pennant, skull]) so False-Colours disguise (#79) + flap (`sailing.js`) still work; **falls back to the procedural hull on any load error** so the game always boots. `main.js` now `await loadShip()`. CREATIVE SPARK (Graphic Designer): a proper carrack silhouette under a cheeky hand-stitched black pennant snapping above Kenney's sun-warmed planks — the sea finally has a ship worth singing about. | #32 (**CLOSED**) | v0.0.20260627235613 | 403 unit tests (+6 in `tests/unit/ship-loader.test.mjs` → now Node-runnable: scale-to-length · ratio preservation · keel-centre offset · already-centred zero · constants); playtest ✓ PASSED, zero console errors; perf **30/130 draws · 84k/150k tris** (well within budget — the low-poly glTF actually *cut* draw calls vs the procedural ship). CI green, live 200. **VISUAL DoD verified on the LIVE site (cache-busted, real browser, then tab parked on about:blank):** textured Kenney carrack with dark sail + black pennant sailing over the sunny teal sea, **foam wake + follow-camera intact** (sailed to pos z≈85). Gallery `studio/qa/gallery/v0.0.20260627235613-gltf-hull.png`. Follow-ups: more CC0 props (#101); optional richer materials/Draco. |

| 47 | **#72 — "Strike the Colours": crew morale & the merciful-capture path (depth beat; #72 advanced, follow-ups filed).** Turns the MVP cannon exchange (#59) into a richer fight by making **the aim you choose decide the foe's fate**. Added a foe **crew NERVE** stat: a **full broadside** drowns the hull (the bloody, loud **Infamy** road) but barely dents morale; **chain-shot** shreds the rigging and **rattles the crew**, eroding nerve — keep sawing their sheets and, once their nerve breaks AND the hull is genuinely wounded, they **strike their colours and YIELD**. A capture is the **merciful road**: a ransom purse + **lawful Standing** (the governor pole) and far LESS Infamy than a sinking — the exact mirror of the sink reward, so the two combat outcomes now feed the two identity poles. Crucially, **broadside-spam still SINKS before any yield**, so the deterministic headless playtest is unaffected. New pure logic in `src/cannons.js` (`crewShock`/`strikesColours`/`captureSpoils`/`strikeLine` + morale threaded through `resolveExchange`→`{enemyMorale,yielded}`), wired into the controller (`'capture'` finish → ransom+Standing+respawn), `hud.js` (a third **"Their nerve"** bar + updated help copy), `index.html` (gold nerve-bar CSS), `main.js` (the `'capture'` onEnd banner + deed), `voyage-log.js` (a distinct **capture verse** + `captured` flag so the Ballad sings the mercy road). CREATIVE SPARK (Game Designer): the aim IS the moral choice — drown them for swagger or break their nerve for mercy — plus original surrender cries ("We strike! We strike — spare the crew, you salt-crusted menace!"). | #72 (advanced; follow-ups filed) | v0.0.20260628000955 | 411 unit tests (+10 TDD in `cannons.test.mjs`: crewShock chain>broadside & fear-scales-with-hull · strikesColours wounded+broken-only · morale reported/defaults · full-engagement chain→YIELD vs broadside→SINK across rng floor/half/ceil · captureSpoils mercy-mirror · strikeLine); playtest ✓ PASSED, zero console errors; perf **30/130 draws · 84k/150k tris** (no geometry added). **VISUAL DoD** (headless real-browser, mid-fight): the cannon panel now shows three bars — Your hull / Their hull / **Their nerve** (gold) — with the new "chain-shot breaks their nerve — they may strike their colours" help line; the in-game capture fired (foe yielded at hull 47 / nerve 8 → ransom + Standing). Gallery `studio/qa/gallery/v0.0.20260628000955-strike-colours.png`. **Follow-ups (still open on #72):** in-world hull-damage visuals (smoke/list/sinking beat); tougher foes / gunnery spread so careless play can lose; more aims (grapeshot, raking) + range/closing/reload cadence; fleeing/disengage chase; dedicated cannon audio; foe initiative (NPCs open fire on you). |

| 48 | **#95 — Mode system: explicit world-state machine (CLOSES #95, from-owner P1 keystone).** Generalises the implicit two-state freeze (`duel.active \|\| cannons.active` buried in `main.js update()`) into a real, deliberate machine and ships the SMALLEST always-working increment — the scaffold + the seam the rest of the owner batch plugs into. **New pure, three-free `src/mode.js`:** `createModeManager()` → `SAILING ↔ TOWN ↔ BATTLE` with guarded `enter/leave/current`, an `is(mode)` check, a single `playerPaused` truth (`current !== SAILING`), and an `onChange(to,from)` hook that fires only on a real transition (re-entering = a deliberate no-op so `update()` can poll-drive it without churn). **`main.js update()` now drives the machine through the existing seam:** the old `fighting` gate becomes `mode.enter(BATTLE)` when a duel/cannonade is live and `mode.leave()` when it ends, and `sailing.step({ fighting: mode.playerPaused })` reuses the #76c ease-to-a-stop path — so a fight behaves EXACTLY as before (the headless fight gate is unchanged). **#95's headline — the world keeps living:** `npcs.update()` now runs **every frame** (moved out of the old `!fighting` gate; only the disguise-disposition beats stay gated to a free helm), so other vessels sail on while the player's helm is paused — the world no longer snap-freezes around you. Combat latches its target by index + builds its own foe object, so moving NPCs are safe. TOWN is the trivial proving mode, entered via the new `tw.enterMode('town')` seam (the real auto-harbour town scene is its own slice #67/#96); a fresh voyage resets to SAILING. QA hook gains `tw.mode`/`enterMode`/`leaveMode`. CREATIVE SPARK (Game Designer): a mode is a *change of stance* — SAILING is the open hand on the helm, TOWN/BATTLE are "hands off the wheel, eyes up", which is exactly why they share one `playerPaused` truth and the world settles around you instead of freezing. | #95 (**CLOSED**) | v0.0.20260628003252 | 420 unit tests (+9 TDD in `tests/unit/mode.test.mjs`: three modes/order · `isMode` guard · default+explicit initial · guarded `enter`/`leave` transitions · same-mode no-op · `playerPaused` · `onChange(to,from)` only on real transitions); playtest ✓ PASSED, zero console errors — new headless mode check asserts boot=SAILING → `enterMode('town')` eases the ship to a near-stop with the throttle HELD **while a vessel still moves** (world keeps living) → `leaveMode()` returns to SAILING; perf **30/130 draws · 84k/150k tris** (pure logic, no new draws). CI green, live 200. Invisible/structural change → no gallery shot. **Follow-ups (build ON this seam):** #67/#96 town scene (replace QA-driven TOWN with the auto-harbour trigger + market) · #100 battle content (boarding/loadouts hang off BATTLE) · #94 mode-aware sound (reads `tw.mode` as `context.mode`). |

| 49 | **#67 + #96 — auto-harbour into a real TOWN/market mode + Leave Harbour (CLOSES #67, #96, #66; from-owner P1).** Makes landfall a *moment and a place* on the #95 mode seam: sailing into a port's dock radius now **auto-harbours into a deliberate TOWN mode** — the helm pauses, the world sails on (NPCs keep moving), and a real town/market view takes the screen instead of the corner trade panel flickering over the helm. The town view (centred, quayside-glass) shows the **port name + blurb + harbourmaster line + a rotating barker cry** (drawn from the port's own criers), the **purse/hold + standing badge**, and the **market board wired straight to the economy** (tap a row to buy / tap its sell price to sell, or 1–5 / Shift+1–5 on desktop). The one explicit, reversible exit is the **"⚓ Set Sail" plank (or L)** — it returns to SAILING, re-arms the helm (throttle nudge) and **points the bow seaward** so a berth never traps you (the owner-flagged risk: docking re-arms on proximity). While ashore the at-sea touch clusters + corner trade panel + hail/fire prompt are hidden via `body.town` — which **also fixes the iPhone overlap (#66)**. **New PURE `src/systems/harbour.js`** (`shouldEnterTown` edge-trigger · `harbourAssistActive`/`nextLeftHarbour` leftHarbour latch that suspends the #76c slow-assist while leaving so the nudge carries the hull out · `seawardHeading`); **new self-contained `src/ui/town.js`** (#53 standard). `main.js` drives landfall off the arrival edge + `leaveHarbour()` + `body.town` + `tw.town`/`tw.leaveHarbour` QA surface; `ports.js` stores the jetty seaward angle + `portInfo()`. CREATIVE SPARK (Game Designer): landfall is edge-triggered like crossing the harbour mouth — town opens once on arrival, the market opens with a barker's holler, and leaving is one warm pull of the wheel that turns the bow back to open water. | #67 #96 #66 (**ALL CLOSED**) | v0.0.20260628005033 | 427 unit tests (+8 TDD in `harbour.test.mjs`: edge-trigger only-on-arrival/only-under-sail/never-from-TOWN-or-BATTLE · assist on-by-default/suspended-while-leaving · leftHarbour raised-by-leaving/held-in-range/dropped-when-clear · seawardHeading angle+NaN-safe fallback); playtest ✓ PASSED, zero console errors — new headless section sails into a port → asserts mode flips to TOWN + `#town` shows + `body.town` set (#66) → a market trade moves the purse → Leave Harbour returns to SAILING and the seaward nudge carries the hull clear of the dock radius (no trap); collision test retargeted to a portless isle (ramming a port-isle now harbours). perf **30/130 draws · 84k/150k tris** (DOM/logic only, no new draws). CI green, live 200. **VISUAL DoD** (headless real-browser): the town view open at Saltpurse Quay — blurb, harbourmaster, barker, standing badge, full market board (local/wanted markers, held cargo), the gold "⚓ Set Sail (L)" plank. Gallery `studio/qa/gallery/v0.0.20260628005033-town-market.png`. **Follow-ups:** #94 unified mode-aware sound + the per-town music/harbour-bell *audible* cue (#67 acceptance #3 — owned by the sound slice); walking-ashore + more town activities (tavern/governance/workshops, P2 per #96); an earlier ~200u "town ahead" approach announce (full-polish #67). |
| 71 | **#123 — QA golden-replay fixture for the full reactive loop (CLOSES #123; DL #4 hardening; completes the pre-battle hardening trio with #120/#122, Retro 12's #3).** #107 golden-traced the MODE seam; this pins the OTHER core loop — the town→rumour→sail→reward spine — as a deterministic, committed golden replay so it cannot silently drift as #112's payoff + richer rewards land. **New `tests/unit/reactive-loop-replay.test.mjs`:** a PURE reproduction of main.js's reactive-loop wiring (no THREE, no DOM, no wall-clock), built in the #107 mode-trace pattern — a tiny `makeVoyage(seed)` whose `arrive`/`listen`/`chase`/`setSail` drivers mirror the named main.js functions (`onArrive` reward + `shouldEnterTown` snapshot ~L1033/L320 · the tavern `composeRumours` listen ~L694 · `chaseObjective` ~L705 · `leaveHarbour` digest ~L732), driven by a seeded intent-log over the real pure modules (`rumours.js` · `objectives.js` · `voyage-log.js` · `ashore-digest.js`). The golden run (fresh unknown captain, 100 coins at Saltpurse Quay): **make-port → listen (2-rumour pairing: a rep line + a chase-able trade target = Barnacle Bottom) → chase → set-sail (digest reads back "a heading set for Barnacle Bottom") → arrive → reward → make-port → set-sail (ambient quiet-call digest)** — asserting the EXACT 9-step event sequence PLUS sampled state: coins **100→160** (seed + the 60-coin `RUMOUR_REWARD_COINS` bounty), reputation **infamy/standing unchanged** (a rumour chase pays coin, NOT renown — pinned explicitly), objective **active→null** the instant it pays off, the Ballad **rumour verse** (names the port + coin), and both **ashore digests**. Two further guards: full **determinism** (same seed → identical events + ballad text — the property that drives the panel AND the share) and **no-double-pay** (a second arrival at a resolved port pays nothing — `resolvesAt(null)` is false). CREATIVE SPARK (Game Designer/QA): the crown-jewel loop now has a tripwire — any regression in seed→listen→chase→sail→reward ships as a one-line diff to the golden sequence. | #123 (**CLOSED**) | _none — test-only (tests/**), no release/version fires (workflow `paths:` is `src/**`); pinned by commit 90f4ceb_ | **690 unit tests** (+3 TDD in `reactive-loop-replay.test.mjs`: the golden spine · full determinism · no-double-pay regression guard); `node tests/playtest.mjs` ✓ PLAYTEST PASSED, zero console errors; perf **32/130 draws · 90k/150k tris** (test-only, no runtime change). No CI run fires for a tests-only push (un-CI-checked on push per the workflow note) → gates verified LOCALLY; live site HTTP 200 (unchanged at the prior build). **No determinism gap or bug uncovered** — the spine was already deterministic + double-pay-safe; this pins it. Not a visual change → **no gallery shot**. **Follow-ups:** extend the replay to the at-sea encounter (#125) + combat (#100) spines once battle lands; a tests-only CI lane so test commits are checked on push (the workflow note's open gap). |

## Hourly Telegram log

| UTC | Version | What changed | Media |
|-----|---------|--------------|-------|
| 23:25 | v0.0.2026...1938 | Test update + v0 status | screenshot |
| 00:10 | v0.0.2026...0101 | Wake, sail, audio, wind compass (5 releases) | video |
| 00:50 | v0.0.2026...4415 | Ports + save/load + modularise (clearer video) | video |
| 01:48 | v0.0.2026...4341 | Economy, NPCs, persistence, deep-learning loop, Captain's Ledger | screenshot |

| 42 | #55 art-sourcing research (no release) | #55 | — | report → docs/art-sourcing.md; #32 ready |

| 50 | **#94 — Unified mode-aware sound: procedural proximity crossfade (Phase 1 of 5; #94 stays OPEN).** The first always-working slice of the owner's unified sound system, built on the #95 mode seam + #67 town. New **pure `src/music-director.js`** — `resolveMix({mode, portDistance, dockRadius})` → per-layer target gains (context already shaped for the future sail-zones hook). `music.js` splits the score into a named **SEA** layer-gain (the existing sailing theme) + a new **PORT** layer-gain hosting a warm lowpassed D-major **tavern drone** with an accordion-bellows tremolo (same key centre → one voice); `setMix()` crossfades them via `setTargetAtTime` (no clicks). **The bed reacts to WHERE you are:** SAILING = open-sea bed, the port layer **swells up from `PORT_CUE_RADIUS` 260u (well outside `DOCK_RADIUS` 90)** on approach and ducks the sea under it — the **audible "harbour ahead" cue from a distance (delivers #67 acceptance #3)**, reversing as you leave; TOWN = the tavern theme owns the mix, sea to a hush; BATTLE = the whole bed settles to a low keel so the guns read. All under the one shared `{ctx, master}` bus → the existing single mute still covers it; headless-safe (no AudioContext → fully inert). `main.js update()` hands the director the mode (#95) + the nearest-port distance from the existing harbour loop. CREATIVE SPARK (Musician + Sound Engineer): a port has an *auditory horizon* — a warm tavern wheeze drifts across the water long before the dock is in sight, whispering "there's a place to put in, here." | #94 (Phase 1; **OPEN** for phases 2-5) | v0.0.20260628010142 | 438 unit tests (+11 TDD in `tests/unit/music-director.test.mjs`: portProximity 0-at-cue/1-at-dock/monotonic/NaN-safe · resolveMix per-mode + SAILING crossfade monotonic + every output a clean gain in [0,1]); playtest ✓ PASSED, zero console errors (audio inert headless); perf **30/130 draws · 83.8k/150k tris** (audio = pure CPU, no draws). CI green, live 200. Audio is not visible → **no gallery shot**. **Remaining on #94:** (2) rotating sea themes · (3) per-town identities (#69) · (4) real battle cue · (5) real audio files behind a lazy `loadTrack(url)`. |

| — | Deep-learning research loop #3 (9 role subagents, parallel fan-out) | — | — | HARD-triggered (~11 cycles since DL #2), ran after the owner mode-system P1 batch drained. 9 roles refreshed (own reading list + Constitution → agent ## Research log + memory); themes: **town must be a destination not a menu** · **the mode transition is the drama (all disciplines converge)** · **harden the mode seam before 3 modes leak** · **mode-aware audio = acoustic space not volume** · **promote #36 above #84**. Filed #102–#109 (mode-system depth, all below the P1 batch); synthesis `studio/retros/2026-06-28-deep-learning-3.md`; 2 subagents hit injected-instruction derail (incl. a "cut a v0.1 release" prompt-injection — refused), re-dispatched once. **DL counter reset to 0** (next DL ~10 cycles out). |

| 51 | **#106 — Mode-seam hardening (Phase 1; #106 stays OPEN, DL #3 theme 3).** First DL #3 build slice — turns the #95 keystone into a robust Thinnest-Viable-Platform contract before #67 town / #94 sound / #100 battle pile onto one fragile `onChange`. Pure, three-free `src/mode.js`: **(slice 3) legal-transition guard** — `canTransition(from,to)` + exported `TRANSITIONS` graph; SAILING is the hub (always reachable), combat preempts (TOWN→BATTLE legal — a fight can break out ashore) but you can never make port mid-broadside (BATTLE→TOWN illegal — "TOWN must not interrupt BATTLE"); `enter()` now refuses an illegal-but-known move with `false` (poll-safe, no throw), unknown still throws; `canEnter()` advertises it. **(slice 2) multi-subscriber bus** — `subscribe(fn)→unsubscribe` so each system attaches/detaches independently; `onChange` kept as backward-compat subscriber #0; a subscriber's exit fires exactly once, a throwing subscriber is isolated (never corrupts state nor blocks peers), unsubscribe idempotent. **Deterministic `reset()`** lands SAILING from any stance → wired into `newVoyage()`. Invariant other systems may rely on: `playerPaused === (current !== SAILING)`. `main.js` only changed `leave()`→`reset()` in newVoyage. CREATIVE SPARK (Game Designer): SAILING is the hub — the open hand on the helm every stance falls back to; the seam itself refuses to let town interrupt a live broadside. | #106 (Phase 1; **OPEN** for slices 1 registry + 4 per-mode disposal — need real per-mode resources from #100/#67-depth) | v0.0.20260628012450 | 464 unit tests (+14 in `mode.test.mjs`: N×N matrix · the guard · exit-fires-once · error isolation · idempotent unsubscribe · reset determinism · paused invariant; pre-existing BATTLE→TOWN test relegalised); playtest ✓ PASSED, zero console errors; perf **30/130 draws · 84k/150k tris** (pure logic, no draws). CI green, live 200. Invisible/structural → no gallery shot. **Follow-ups:** #106 slices 1 (declarative `{[mode]:{onEnter,onLeave}}` registry — sugar over the bus) + 4 (per-mode disposal — blocked on #100 battle meshes); #107 N×N/golden-trace QA rides this contract. NB: loop was owner-STOPPED — this was a single explicitly-dispatched slice, not an autonomous cycle. |

| 52 | **#97 — Living sea fauna: an instanced gull flock that wheels with you (Phase 1; #97 stays OPEN).** The first fauna beat — a small flock of GULLS that keeps the ship company, a reactive verb: the world answers *where you are / what you're doing*. They wheel over the ship at sea and **DRIFT to hang over the shore as you raise an island**, gliding between the two via a smooth ease. The whole flock is **ONE `THREE.InstancedMesh`** (one draw call) of a tiny two-triangle gull silhouette with a Y-scale **wing-beat**, and it's **distance-culled wholesale** (`mesh.visible=false` → 0 draws) beyond `CULL_RADIUS` — a living sky for almost nothing. Pure, three-free **`src/fauna-math.js`** (`gullParams`/`gullPosition`/`flapScale`/`roostTarget`/`easeTowards`/`shouldCull`) TDD'd first; thin `createFauna({world})` factory in `src/fauna.js` consumes it; `main.js` ticks it beside `npcs`/`wake` **every frame** so the flock lives on while the helm pauses (town/battle). QA hook `window.__tidewake.fauna` exposes `{count,visible,nearLand,center}`. CREATIVE SPARK (Game Designer + Graphic Designer): gulls trail your wake hunting galley scraps, then peel off to ride the updraughts over a raised island — company at sea, a welcoming committee at the coast (pairs with the existing gull SFX #68). | #97 (Phase 1; **OPEN** for dolphins + other species) | v0.0.20260628014411 | 456 unit tests (+6 TDD in `tests/unit/fauna.test.mjs`: deterministic varied params · wheels on-circle facing travel · bounded wing-beat · ship-or-shore roost · ease convergence/no-overshoot · cull-beyond-radius); playtest ✓ PASSED, zero console errors; perf **32/130 draws · 83.8k/150k tris** (one instanced draw added — well in budget). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/v0.0.20260628014411-fauna-gulls.png` (gulls wheeling over the sunny sea + an island). Also corrected the stale owner-STOPPED banner at the top → loop is RUNNING (owner restarted via /loop 2026-06-28). **Follow-ups:** jumping dolphins (#97 phase 2) · other sea/land animals (phase 3) · per-bird flapping shader / coast-only roosts. |
| 53 | **#93 — Ship's-wheel touch steering: a rotatable on-screen helm for mobile (CLOSES #93; from-owner P2; #98 was its duplicate).** Replaces the two ◀▶ touch steer buttons with a single **rotatable brass-and-wood ship's wheel** in its own fixed control zone (bottom-left thumb reach). **Drag-rotation → analog steer** that feeds the existing eased rudder (#20) — clockwise steers starboard, anti-clockwise to port, a half-turn = a half rudder; the wheel rotates 1:1 under the thumb and is a **self-centring helm** (lift off → springs back amidships, the eased rudder then settles the bow straight). Same eased-rudder path as the keyboard, **no parallel physics**; **desktop A/D stay authoritative and unchanged** — the wheel's analog axis is only sampled when the keyboard is idle. **No camera fight:** the wheel preventDefault/stopPropagation its own pointer events and owns its region, so a drag on it never doubles as the full-screen camera-orbit drag. **No #66 re-overlap:** the wheel lives inside `#touch-controls`, which TOWN mode already hides. New **pure `src/ui/wheel.js`** (`wheelSteer` drag-angle→steer with deadzone/clamp · `shortestAngleDelta` seam-safe accumulation · `clampAngle`/`pointerAngle`) TDD'd first; self-contained `createWheel()` factory (#53 standard) renders an **inline-SVG wheel** (license-clean, crisp at any DPR, ~0 weight). `input.js` exposes `steerAxis`/`wheel`; `sailing.js` samples the axis when keys idle; `main.js` adds `tw.wheel`/`steerWheel`/`centreWheel` QA. CREATIVE SPARK (Game Designer): the helm has heft — a real wheel you must actively hold over; let go and it springs amidships, weighty and forgiving, never a trap. | #93 **CLOSED** (#98 dup) | v0.0.20260628015755 | 476 unit tests (+10 TDD in `tests/unit/wheel.test.mjs`: deadzone→no-drift · full-lock→±1 + clamp-beyond · monotonic ramp · gentle-just-past-deadzone · sign symmetry · ±PI seam wrap · pointerAngle cardinals); playtest ✓ PASSED, zero console errors — new headless section drives the wheel to full lock → asserts the ship turns + the rudder builds while held + eases back on release + centring zeros the axis; touch-control check now asserts `#ship-wheel`. perf **30/130 draws · ~84k/150k tris** (one SVG + DOM, no new WebGL draws). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/v0.0.20260628015755-shipwheel.png` (the helm over the sunny sea, throttle ▲▼ + ⚔🔥 cluster, no overlaps). ⚠️ **DEVICE/iOS: best-effort, UNCONFIRMED pending owner iPhone re-test** (verified in headless 390×844 hasTouch emulation) — don't stack dependent work until confirmed. **Follow-ups:** haptic detent tick · wheel skins/themes · tunable sensitivity setting · optional non-self-centring lock mode. |
| 54 | **#101 — Dress ports with CC0 Pirate Kit props: instanced barrels, crates & palms (#101 advanced; phase 1+2 of 4; from-owner P2).** The world stops being 100% procedural placeholder: each port becomes a **working harbour** — barrels + crates as **cargo on the jetty planks**, a pair of **palms framing the dock foot** — using the **CC0 Kenney Pirate Kit** (same kit family as the #32 hull). Loaded **like the #32 hull** (`GLTFLoader`, graceful fallback — the world simply sails **bare** if a fetch fails, never broken). Repeated props are **InstancedMesh'd (one draw per type per port)** and each port's cluster is **distance-culled wholesale** (≤520u), so a furnished world stays nearly free. New **pure `src/systems/props.js`** (`localToWorld` jetty-bearing transform · `propWorldPlacement` · `placementsForType` · `clusterVisible` cull · `scaleForHeight`) TDD'd first; three.js builder `src/props.js` (`loadProps` → per-port clusters of InstancedMeshes, never rejects); `ports.js` exposes `portPlacements` (incl. seaward angle); `main.js` builds + ticks it and exposes `tw.props`/`tw.qaTeleport`. Assets `assets/props/{barrel,crate,palm-straight}.glb` + shared `Textures/colormap.png`, **CC0** (credited in `CREDITS.md` + `assets/props/LICENSE.txt`). CREATIVE SPARK (Graphic Designer): a port stops being an abstract marker and reads as a **place** — cargo stacked, palms leaning over the berth — so every approach looks like landfall somewhere lived-in. | #101 (advanced; phase 1+2 — port dressing; island/loose-prop phases queued as follow-ups) | v0.0.20260628022029 | 482 unit tests (+8 TDD in `tests/unit/props.test.mjs`: type/target coverage · scaleForHeight + degenerate guard · localToWorld identity + Y-rotation convention · seaward/landward placement · spin folds into bearing · per-type placement counts · cull in/out of radius); playtest ✓ PASSED, zero console errors — new headless section asserts props placed (21 across 3 clusters), **drawn near a port** (7 visible), and **culled to 0** at open sea. perf **35/130 draws · 85k/150k tris** at spawn (instanced + culled — huge headroom kept). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/v0.0.20260628022029-pirate-kit-props.png` (Saltpurse Quay: kit palms framing the jetty, cargo on the planks, buoy + flag over the sunny sea). **Follow-ups:** island dressing (palms/rocks/huts instanced) · loose props (lanterns/market stalls/bottles, feeds town-market #96) · a 2nd/3rd port-prop variety pass · embed the prop textures into the GLBs (drop the shared-PNG fetch) like the #32 hull. |
| — | Retro 10 (subagent) | — | — | Covered loops 48–54 (**the structural-pivot chapter**: #95 mode scaffold → #67/#96/#66 auto-harbour town/market → #94 ph1 mode-aware sound → DL #3 research (#102–#109) → #106 ph1 seam-hardening → #97 ph1 gull flock → #93 ship's-wheel touch → #101 ph1+2 port dressing; 411→**482 tests**, gate green, perf 35/130 draws). **Headline GAME finding:** the owner's mode-system pivot is BUILT — Tidewake crossed from "rich sandbox" to a **structured, multi-mode world** (sailing↔town↔battle, world lives under a paused helm, furnished ports, living sea, touch helm); but the spine is a skeleton — town is still "a better trade panel," battle an empty room → next leverage is **make the modes matter** (DL #3: town = destination-not-menu; transition = the drama). New top-3: **#103 tavern "listen for word"** → **#102 crafted landfall** → **#107 mode-transition QA**. **Headline PROCESS finding:** a planted **"cut a v0.1 release" prompt-injection** hit a DL #3 subagent (refused) → extended the injection guardrail to **release/scope/version/gate-bypass** (edited `LOOP.md` dispatch template); also added a **PHASE-LABEL RULE** to `queue.md` (multi-phase umbrellas state shipped-vs-remaining; #94 ph1 acceptance MET → not top-of-queue) + flagged **3 iOS slices (#77/#87/#93) to batch for one owner re-test**. #100 battle kept HELD for the owner brief. Logged 3 decision blocks. **Retro counter reset to 0** (next ~loop 61); DL #3 done → next DL ~loop 64. Queue-sync rule (Retro 9) held all block. |
| 55 | **#103 — Town tavern "listen for word": procedural rumours from world-state + reputation (CLOSES #103; DL #3's #1 reactive-verb-ashore).** Turns TOWN from a menu into a **destination** — the tavern gains a reactive verb: **listen for word** surfaces 1–2 in-character rumours composed **DETERMINISTICALLY** from live game state — the ports around you (what each *craves* → sell dear, what it makes *cheap* → buy low), **who you've become** (Infamy↔Standing dominant pole + renown tier: unknown→known→renowned, feared-pirate vs respected-governor diverge), and **recent deeds** (last fight foe + raised isle pulled from the #78 voyage log). Each rumour **names a real target**, so it reads as a **soft sea objective** you may chase with the verbs you already have — no quest log, just a nudge. New **pure `src/rumours.js`** (`composeRumours(world,{count,nonce})` — kind-balanced selection rep+trade-lead so a 2-listen always pairs "who you are" with "where to sail"; `nonce` turns the room's talk over for a fresh "listen again" without breaking determinism) TDD'd first. `src/ui/town.js` gains the tavern affordance (🍺 **Listen for word** button + **R** key, rumours block, fresh-visit reset) reusing the Ballad tone; `main.js` exposes `tw.tavern`/`tw.tavernListen()` QA. Asset-light: text + existing #67 town UI, zero new draws/tris. CREATIVE SPARK (Game Designer): a hunched regular in the corner who "heard a thing" — a rumour that points the bow somewhere worth going, reflecting who you are and where the world is. | #103 **CLOSED** | v0.0.20260628024509 | 485 unit tests (+11 TDD in `tests/unit/rumours.test.mjs`: no-port→[] · ≥1-while-docked · determinism · count clamp 1..4 · names another port (soft heading) · pirate-vs-governor word diverges · unknown="clean slate" · deed echoes foe+isle · nonce re-listen varies · clean prose no placeholders); playtest ✓ PASSED, zero console errors; perf **35/130 draws · 85k/150k tris** (text/DOM only — identical to baseline, no perf cost). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/v0.0.20260628024509-tavern-rumours.png` (Saltpurse Quay tavern: "🍺 What you hear" — a Barnacle-Bottom rum trade tip + a governor-flavoured lawful errand, Listen-again + market below). **Follow-ups (filed, not built):** a map **MARKER** for a rumour's target heading (the literal soft-objective pin) · **economy/bounty hooks** so a chased rumour pays off mechanically · per-town rumour identities (#69) · time-decay so stale word fades. |
| 56 | **#102 — Landfall: the SAILING↔TOWN transition as a crafted eased gesture (phase 1 of multi-sensory; #102 stays OPEN).** DL #3's headline ("the transition IS the drama"): making port (and Set Sail) was a snap — mode flipped, town screen appeared, camera teleported. Now it's a deliberate eased moment off the #95/#106 mode seam. New **PURE `src/systems/landfall.js`** — a phase/ease/duration state machine (idle→landing→ashore→leaving) yielding a single eased `blend` 0→1; **dt-driven (never wall-clock), deterministic, headless-safe, skippable, interrupt-safe** (bailing mid-landing eases the reverse from exactly where you were). Wiring in `main.js`: (a) the camera eases from the open-water chase cam to a closer/lower **3/4 "ashore" framing** of the moored ship; (b) a warm **"golden harbour" grade** lerps fog/haze + ocean sea-haze + sky-dome horizon + sun colour/intensity toward a low gold glow over the gesture — **captured on the first warmed frame and restored EXACTLY when fully back under sail** so it never fights day-night (#58); colour/uniform writes only, **no new draws**; (c) the **town view (#67) opens only once ASHORE** (`landfall.townReady`) so the eased camera/grade play out in the open first, then the town takes the screen; on Set Sail it closes at once and the reverse gesture eases you back to sea; (d) **skippable** — Enter/Space or a tap during the ease jumps straight ashore/out (pointer-skip ignores the town panel's own controls), never steals input at sea. QA surface `tw.landfall {phase,blend,active,townReady}` + `tw.skipLandfall()`; `newVoyage()` resets it. CREATIVE SPARK (Game Designer): "make landfall" should *feel like arriving* — the helm goes quiet, the ship glides to her moorings, the light turns gold, THEN the town opens around you; Set Sail is the mirror. | #102 (phase 1; **OPEN** for audio + swell) | v0.0.20260628030539 | 496 unit tests (+11 TDD in `tests/unit/landfall.test.mjs`: clamped-smoothstep ease · eased 0→1 over duration · monotonic + **duration-not-framecount** determinism (9×0.1 == 1×0.9) · graceful reverse 1→0 · skip · continuous interrupt (no snap) · enter/leave guards · reset · NaN/neg-dt safety); playtest ✓ PASSED, zero console errors — new landfall section asserts the gesture eases UP deterministically (not a snap), the **town view stays closed mid-gesture**, it's **skippable** to fully-ashore-with-town-open, and Set Sail begins the reverse + closes the town + eases blend back down; perf **35/130 draws · 85k/150k tris** (camera/colour writes only — no new draws). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/landfall-102-making-port.png` (golden close-in moored 3/4 framing, town not yet open) + `landfall-102-set-sail.png` (cooling-back, camera pulling seaward). **Follow-ups (#102 OPEN):** music swell + transition **stinger on the next downbeat** (bar-clock, constant-power sea→town crossfade — the music director already crossfades the bed by mode) · **glassy "moored" swell-amplitude lerp** (needs a swell-amplitude uniform threaded through the ocean.js shader + the CPU `swellHeight` sampler) · deepen the gold / a small camera dolly settle. |
| 57 | **#107 — Mode-transition QA coverage (CLOSES #107; DL #3 theme 3 "keep the gate trustworthy as the state space explodes").** The mode surface is now rich — seam #95/#106, landfall #102, town #67, mode-aware sound #94 — but the headless gate only walked the happy path, so a whole mode could rot silently between captures. Locked it down with fast PURE tests + one gate invariant, and **fixed the long-standing perf-counter flake (a REAL race, not environmental).** **Slice 1 — N×N transition matrix (manager level):** `mode.test.mjs` now table-walks every `MODES×MODES` pair through the live `createModeManager`, cross-checked against the pure `canTransition` guard, asserting resulting `current`, `playerPaused`, and the `onChange` fire-count (incl. the no-op re-entry) — a future edit that desyncs the manager from the legal graph fails here cheaply. **Slice 4 — golden mode-trace fixture:** new `tests/unit/mode-trace.test.mjs`, a PURE replica of main.js's mode→landfall→combat wiring driven by a committed intent-log, pinning the exact `SAILING→TOWN→BATTLE→SAILING` event sequence + sampled gesture state (incl. the subtle *fight-erupts-ashore reverses the town gesture* edge) — every future mode bug ships as a 3-line repro. **Slice 2 — cross-mode "world keeps living" invariant:** the playtest now asserts the snap-freeze guard (#95) for **BATTLE** too (helm freezes mid-fight yet ≥1 other vessel sails on), so the regression can't return via any mode. **Slice 3:** the pure mix decision (SAILING≠TOWN≠BATTLE) is already covered by `music-director.test.mjs`; the gate-level mix exposure + a once-per-transition bell remain a follow-up (noted on #94) — not built to avoid inventing a bell that doesn't exist. **PERF-FLAKE FIX:** perf counters are refreshed ONLY by the rAF render loop, which headless Chrome throttles so hard (fps≈0) that the gate could read `tw.perf` before any real frame latched → `drawCalls=0` → "perf counters unpopulated". Reproduced 1/6 runs pre-fix. Fixed deterministically with `tw.qaRender()` (one synchronous render+measure the playtest calls before reading perf) + a defensive `isMeasuredFrame()` latch so an empty/throttled frame never clobbers a good reading; 6/6 green after. CREATIVE SPARK (Game Designer/QA): a fight can break out anywhere — even ashore — but you can never make port mid-broadside; the golden trace makes that *truth* a committed, replayable fixture. | #107 (**CLOSED**); follow-up noted on #94 | v0.0.20260628032245 | 510 unit tests (+15: N×N manager matrix + canEnter cross-check in `mode.test.mjs`; 3 golden traces in `mode-trace.test.mjs`; 2 `isMeasuredFrame` guards in `perf.test.mjs`); playtest ✓ PASSED, zero console errors, BATTLE world-keeps-living invariant green, perf **38/130 draws · 86k/150k tris** (deterministic via qaRender). CI green, live 200. Mostly invisible → no gallery shot. **Follow-up (on #94):** gate-level resolved-mix exposure on `__tidewake` + once-per-real-transition bell beat. |
| 58 | **#102 — Landfall phase 2: glassy "moored" swell settle + downbeat "made port" stinger (CLOSES #102 — landfall craft is "done for now").** Deepened the SAME moment phase 1 shipped (eased camera + golden grade + town-opens-ashore), adding the two named multi-sensory flourishes, each PURE-cored, TDD'd, deterministic, headless-safe. **(1) Glassy moored swell settle** — new pure **`mooredSwellScale(blend)`** in `landfall.js` (1 at sea → 0.2 ashore, monotonic, clamped) drives a new **`uSwellScale` GPU uniform** threaded through the ocean.js vertex shader **AND** the CPU `sampleHeight` sampler in lock-step (no sampler drift) so the whole sea eases to a calm glassy "moored" amplitude as the ship comes to rest — and back to full open-water life on Set Sail. A reactive verb: the paused helm = still water. Colour/uniform write only, **no new draws**. `main.js` calls `ocean.setSwellScale(mooredSwellScale(landfall.blend))` each frame. **(2) "We've made port" stinger** — new pure **`isDownbeat(step)`** bar-clock + a `music.stinger()` arming a bright bell-like D-major arpeggio fired ONCE on the **next bar downbeat** as the SAILING→TOWN gesture eases in (DL#2 juice on the *transition*); routes through `musicGain` so the mute covers it; **headless-inert** (no AudioContext → no-op). QA: `tw.landfall` now exposes **`swellScale`**; the playtest landfall section asserts the sea is full under sail, settles glassy ashore, and is restored at sea — and the stinger never throws (zero-console-errors gate). CREATIVE SPARK (Game Designer + Musician): the water goes glass-calm at her moorings and a bright chord rings out on the beat — *you've arrived*. | #102 **CLOSED** (phase-2 DoD met) | v0.0.20260628034134 | 512 unit tests (+2 TDD: `mooredSwellScale` full-at-sea/glassy-ashore/monotonic/clamped in `landfall.test.mjs`; `isDownbeat` bar-boundary-only + negative-step safe in `music.test.mjs`); playtest ✓ PASSED, zero console errors, new swell-settle assertions green; perf **38/130 draws · 86k/150k tris** (one uniform + one headless-inert voice — no perf cost). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/landfall-102-ph2-moored-swell.png` (moored at Saltpurse Quay mid-gesture: eased ashore framing + the sea gone glass-calm, town not yet open). **Follow-ups (filed, not built):** per-town stinger/bed identity (#69) · constant-power (vs linear) sea→town crossfade refinement · a small camera dolly settle / deepen the gold further. Loop pivots to BREADTH next (port-remembers-you #104 / islands #71 / ocean #70). |
| 59 | **#104 — The port remembers you: persistent per-town memory greets you on return (CLOSES #104; DL #3 theme 1, the reactive verb that makes town a place with a memory).** Each port now keeps a tiny PERSISTENT record of your prior dealings *there* — visit count + a snapshot of your standing as seen locally (tier + dominant pole) — and reflects it back on the next landfall, so renown stops being a flat number and becomes a relationship that accrues port-by-port. On arrival main.js RECALLS the PRIOR memory FIRST (reflecting who you were last time), then BANKS the visit + persists: a return leads the arrival toast AND the town harbourmaster line with a **remembered-return greeting** — warmer for a regular ("that's your 3rd time alongside"), cooler if you've turned pirate since ("you sailed out honest and come back under a darker flag"), a "they remember you smaller, you've grown" if you've risen a tier — while a true first visit keeps the stranger's welcome. New **PURE `src/systems/port-memory.js`** (`recallLine`/`rememberArrival`/`portRecord`/`sanitizePortMemory` — deterministic greeting selection by salience, immutable store update, size-capped) TDD'd first. **Save bump v8→v9**: `portMemory` persisted, sanitised + **fail-open** like legends/onboarding/ballad (junk forgets, never rejects the save); `sailing.restore`/`reset` carry/clear it. `ui/town.js`+`index.html` show + highlight the recall in the harbourmaster slot. QA `tw.portMemory`/`tw.portRecall`. Asset-light: text + existing #67 town UI, **zero new draws/tris**. CREATIVE SPARK (Game Designer): your reputation precedes you — the harbourmaster remembers YOUR face, not just your number. | #104 **CLOSED** | v0.0.20260628035943 | 530 unit tests (+15 TDD in `port-memory.test.mjs`: first-visit→null recall · return recalls + substitutes {port}/{n} · turned-pirate cools / turned-governor warms / risen-tier noticed · determinism · visit increment + newest snapshot · ports independent + no-mutate · full remember→recall loop · sanitiser fail-open + drops junk/zero-visit; +4 in `save.test.mjs`: portMemory round-trip · junk sanitised / pre-memory empty · malformed never rejects); playtest ✓ PASSED, zero console errors — new port-memory section lands at a port (visits=1, no recall), sails out, grows a name, returns (visits=2, recall names the port, town greeting visibly changes + is flagged remembered-return), and survives a save round-trip; perf **38/130 draws · 86k/150k tris** (text/DOM only — no perf cost). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/v0.0.20260628055748-port-remembers.png` (Saltpurse Quay on return: the highlighted gold remembered-return greeting "the rogue they recall has come back a credit to the lanes — welcome, and well met", HUD reads Harbourmaster · respected). **Follow-ups (filed, not built):** per-port **"last deed"** recalled by name (#104b) · the **"Your Harbour"** home-port stretch (claim one port + invest across sessions so it physically grows — DL #3 stretch) · per-town greeting identities (#69) · local familiarity decay over long absences. Loop continues BREADTH: #71 islands TLC / #70 ocean micro-details next. |
| 60 | **#71 — Islands TLC: per-isle palette + silhouette variety + instanced dressing (CLOSES #71; from-owner P2 breadth, owner "Islands, a little bit of Tender-Loving-Care there too").** The isles were tonally identical green-on-tan blobs; each now has a deterministic FACE derived from its INDEX — so an isle is the same warm sandy cay or cooler peaked rock every voyage (it pairs with the named-island lore #19). New **PURE `src/systems/island-style.js`** (DOM-/three-free, node-tested like systems/props.js + fauna-math.js): `islandPalette(i)` → bounded, correlated **HSL OFFSETS** per material role (warmth/lush drivers, applied via `material.clone().offsetHSL` — the npc.js fleet trick), `islandSilhouette(i)` → wider squash (0.65–1.5 vs the old 0.8–1.3), tall/peak/lean, `islandDressing(i,r)` → a deterministic scatter of **rocks · palms · driftwood · grass tufts** in isle-local space (footprint-squash baked in). `world.js` rebuilt to consume it: per-isle bodies carry hue-jittered cloned materials + softer beach→grass blend; the repeated dressing is **INSTANCED per type across the WHOLE archipelago** (one InstancedMesh each: rock / palm-trunk / palm-crown / driftwood / tuft) in its OWN scene group (NOT under `world.islands`, so collision/naming/NPC/fauna iteration of isle bodies is untouched) — a far lusher coast for a handful of draws. Collision footprint (physics.js) follows the squashed ellipse unchanged; the playtest collision section made **ellipse-aware** (expects the true shoreline along the approach bearing from the now-exposed sx/sz, not a circle) so wider squash doesn't false-fail. QA hook `tw.islandStyles` (per-isle sand-tone offset, sx/sz, tall/peak, dressing counts). CREATIVE SPARK (Graphic + Game Designer): give each isle a face — a warmer/cooler sand, a taller/flatter hill, its own scatter of palms and a driftwood log at the tide-line — so a glance at the horizon tells you WHICH isle that is. **No new external assets** (all dressing is procedural primitives) → no CREDITS change. | #71 **CLOSED** | v0.0.20260628042000 | 540 unit tests (+10 TDD in `tests/unit/island-style.test.mjs`: bounded HSL offsets · palette/silhouette/dressing determinism (same index → identical look across reloads) · varied tones + varied silhouettes across the archipelago · only-known prop types · every type appears · counts bounded for perf · props sit on the footprint · `islandStyle` bundle); playtest ✓ PASSED, zero console errors — new islands-TLC section asserts every isle is dressed (rocks+palms), tones + silhouettes are varied, and the look is **byte-stable across a reload**; collision section now ellipse-aware. perf **32/130 draws · 90k/150k tris** (instanced dressing — well within budget; the perf mandate held). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/v0.0.20260628042000-islands-tlc.png` (Mutineer's Folly: warm sand ellipse + grassy dome, three palms, a shoreline rock, a driftwood log + a weathered hut — a second, differently-sized dressed isle on the horizon, gulls aloft, over the turquoise Caribbean sea). **Follow-ups (filed, not built):** the deferred **M silhouette remodel** (noise-displaced geometry + LOD) · per-biome palettes / mangrove + rocky-stack variants · distance-LOD on dressing if the archipelago grows · coordinate sand tone again once the Caribbean sea pass (#61) lands. |
| 61 | **#70 — Ocean micro-details: drifting whitecaps froth the sunny sea's crests (#70 stays OPEN).** The brief reframed #70's first increment as the cheapest always-working sea-FEEL beat (the issue's literal sail-over-curio slice stays queued on #70): the flat sea now has small surface life — the wave **CRESTS** catch the light and froth white, breaking into drifting patches as the swell rolls, so the sunny Caribbean water reads alive and in motion. **Pure colour wash in the EXISTING ocean fragment shader — ZERO new draws/triangles.** Gated on the swell **HEIGHT** the shader already computes (so it's lock-step with the CPU `sampleHeight` #102/#65 by construction — no swell/height drift, ports/coasts stay dry). New **PURE `src/sea-foam.js`** is the SINGLE source of truth (like swell.js): crest-window `smoothstep` (`FOAM_LO 0.7 → FOAM_HI 1.3`, both ≤ MAX_SWELL 1.4 so foam is reachable) + `crestFoam`/`whitecap` combine + tuning consts — the shader's foam GLSL is GENERATED from these same constants. `ocean.js`: fragment whitecaps = crest factor × a drifting **streak** (a sum of slow sines, softstepped → broken moving patches, not a band) mixed toward a `uFoam` tint; + `whitecapAt()`/`foam` QA surface on the same swell the ship rides. `main.js`: `tw.sea` (foam tuning + live crest-foam at the ship) + `tw.whitecapAt(x,z,t)`. **Visual tune (2nd commit):** first pass gated foam behind a sine PRODUCT (peaked too rarely → barely visible); swapped to a sine SUM so the crests clearly froth while troughs stay clean. CREATIVE SPARK (Game Designer + Graphic Designer): the trade winds catch the crests — whitecaps drift and break across the swell, a sense of breeze and motion on a sea that was glassy-flat. | #70 (slice 1 of N; **OPEN** — sail-over curios + standing "1–2 micro-details/loop" rule remain) | v0.0.20260628043637 | 542 unit tests (+5 TDD in `tests/unit/sea-foam.test.mjs`: smoothstep edge-clamp + degenerate window · foam never in troughs/calm mid-swell · foam REACHABLE on the live swell + reads on peaks (crestFoam(MAX_SWELL)>0.8) · crestFoam monotonic 0..1 · whitecap a bounded 0..1 wash, monotonic in streak); playtest ✓ PASSED, zero console errors; perf **29/130 draws · 89k/150k tris** (shader-only — identical to baseline, the ≤130/≤150k mandate held). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/v0.0.20260628043637-sea-whitecaps.png` (under full sail on the turquoise sea: drifting patches of bright foam frothing the mid-distance crests, clean troughs in the foreground, gulls aloft, the ship's wake astern). **Follow-ups (filed, not built):** the issue's **sail-over ocean curios** (flotsam/turtle/message-in-a-bottle → SFX + never-repeat witty-line pool) — #70's original Slice 1 · **bow-spray flourish at speed** (#113) · **subtle sea colour variation / drifting current streaks** (#114). |
| 62 | **#112 + #111 + #115 — Rumours that pay off: chase a tavern tip → map marker → arrival reward (CLOSES #111 + #115; #112 stays OPEN as the umbrella).** Retro 11 #1 + DL #4 headline: the reactive **town → rumour → sail → reward** loop was half-built + silent + invisible — now it's real, end-to-end, for ONE rumour type (a **trade tip naming a port**). **(#115 keystone)** new PURE `src/objectives.js` — the typed world-target model (`sanitizeTarget`/`makeObjective`/`resolvesAt`/`payoffFor`/`resolveObjective`/`sanitizeObjective`): ONE source of truth the marker, arrival-detection and payoff all read instead of three prose re-parsers; `composeRumours` now emits typed `{ text, target }` entries (trade tips carry `{ kind:'port', name }`). **(#111 marker)** the tavern gains a **⚑ Chase this** affordance per chase-able tip → sets the active objective (target enriched in `main.js` with the named port's live coords); the minimap + bigmap draw a bright **sea-green pin** on that heading (bigmap labels it **⚑ chasing**), clamped to the rim out of range, cleared the instant it resolves; the chosen tip reads back **⚑ Chasing…**. **(#112 payoff)** arriving at the chased port pays a **modest deterministic coin bounty (60c)**, sings a **'rumour' Ballad verse** (#78), and clears the pin — `resolvesAt` guards against double-pay. **Save bump v9→v10**: the active objective persists (sanitised + **fail-open** like prior bumps); `sailing.restore`/`reset` + `newVoyage` carry/clear it; QA `tw.objective` + `tw.chaseRumour()`/`tw.chaseTarget()` + `tw.tavern.targets`. Asset-light (2D-canvas marker + DOM), **zero new WebGL draws**. CREATIVE SPARK (Game Designer): chasing a rumour is a real **verb** now — the tip becomes a pin you steer toward and a coin in the palm when you make port. | #111 **CLOSED** · #115 **CLOSED** · #112 **OPEN** (umbrella — richer rewards filed: #127 live price-spike · #128 disposition/bounty vessel) | v0.0.20260628052005 | 563 unit tests (+10 `objectives.test.mjs`: sanitize/accept/resolve/payoff/no-double-pay/save round-trip · +2 `rumours.test.mjs`: typed entries + a trade tip's port target names a real other port · +3 `voyage-log.test.mjs`: 'rumour' verse + no-dedup · +3 `save.test.mjs`: objective round-trip / drops resolved+junk / malformed never rejects); playtest ✓ PASSED, zero console errors — new rumour-chase section: listen → chase a trade tip → marker carries target coords → leave → sail to the chased port → coins bounty + pin clears + Ballad verse + a fresh voyage clears it; perf well in budget (canvas/DOM only — no new draws). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/v0.0.20260628052005-rumour-chase.png` (the route chart with the green ⚑ chasing ring + label on the chased port). **Follow-ups (filed):** #127 trade live price-spike (vs flat bounty) · #128 disposition/bounty rumours spawn a matching vessel (2nd rumour type) · #105 ashore digest reads the objective outcome · sea/deed/rep rumour kinds made chase-able. |

| 63 | **#105 — "While you were ashore…" digest reads back your visit on Set Sail (DL #4 / Retro 11 living-world legibility).** Makes the mode spine's "world keeps living underneath" promise LEGIBLE: snapshot the delta-able world-state the instant TOWN takes the screen (purse/hold, standing/name/tier, dominant pole, a chased rumour), then on **Set Sail** compose a short, in-character digest from the REAL deltas and show it on the shared HUD toast in place of the generic "making sail" line. New **pure `src/systems/ashore-digest.js`** (`snapshotAshore(state)` + `composeAshoreDigest(before, after, {port, max})` → `{title, lines}`, salience-ordered, capped, deterministic, fail-open, headless-safe) TDD'd first. Lines: turned-darker/respectable (pole flip) ▸ a tier climb (name travels + the going rates here warm — the renown price-modifier is tier-keyed, so a tier climb IS a real price drift) ▸ a heading taken to chase ▸ purse heavier/lighter (with singular/plural coin) / a reshuffled hold ▸ a deterministic ambient "the world turned a notch" line on a genuinely quiet call. `main.js` stays thin: snapshot in mode.onChange→TOWN, compose+toast in `leaveHarbour()`, `tw.ashore` QA surface ({snapshot, digest}). **Did NOT** build offscreen world-sim — surfaced only deltas the existing systems already move; richer world-sim filed as follow-ups. CREATIVE SPARK (Game Designer): lingering has a consequence, and casting off is when you feel it — the watch reads you back what your time ashore amounted to. | #105 **CLOSED** | v0.0.20260628053810 | 577 unit tests (+14 TDD in `tests/unit/ashore-digest.test.mjs`: snapshot purse/hold/standing + objective name + null-safe · digest always titled+non-empty · purse heavier/lighter + singular coin · names a freshly-chased rumour but not a standing one · tier-rise note · darker-flag note · deterministic ambient fallback · max cap · no stray tokens · null-snapshot safe); playtest ✓ PASSED (+digest gate: snapshot live ashore → digest composed on Set Sail → snapshot consumed), zero console errors; perf **32/130 draws · 90k/150k tris** (text/DOM only — no new draws). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/v0.0.20260628053624-ashore-digest.png` (the toast over open water on Set Sail: "⚓ While you were ashore… word to chase for Barnacle Bottom · purse 44 coins lighter"). **Follow-ups (filed below):** rival sacks a named port / navy moves (true offscreen world-sim, the issue's stretch) · price-drift line keyed to a real offscreen economy tick · digest reads a chased-objective OUTCOME (#127/#128 hooks). |
| 64 | **#69 — Per-town music identity: each port sounds like ITSELF (transposition-first; #94 phase 3).** The owner's "if towns had different town musics that'd be awesome!" — every harbour shared the one tavern bed. Now each port gets a deterministic musical identity keyed to its name, and the existing #94 PORT-layer tavern drone is re-voiced toward it. New **pure `src/town-theme.js`** — `townMusicIdentity(name)` → `{ root, mode, thirdInterval, tint, lowpassHz, leadType, tremoloHz, chordMidi }` via an FNV-1a hash spread across key/mode/tint palettes (deterministic, headless-safe on junk input). `music.js` stores the 4 drone oscillators + lowpass + bellows-LFO and adds **`setTownTheme(identity)`** — transposes the chord, re-aims the lowpass timbre tint + wheeze rate, all a click-free `setTargetAtTime` GLIDE (applied on start() if set before the engine is up). `main.js` re-keys to the **nearest** port (`ports.nearestPortName(pos)`, no-alloc 3-port scan), **debounced on change** — so the port layer already sounds like that town as it swells in on approach (the #94 auditory horizon now carries the town's identity), reverting on a fresh voyage. **TEMPO stays FIXED** (TL constraint — live tempo glitches the scheduler; richer melodies/instrument sets/docked cue/live tempo → **#129**). `tw.townMusic` ({port, identity}) + `tw.townMusicFor(name)` QA. The 3 ports land distinct: Saltpurse Quay = C minor/mellow · Barnacle Bottom = G major/woody · Gullet's Rest = E mixolydian/mellow. CREATIVE SPARK (Musician): you hear a town before you can see its jetty — the tavern wheeze drifts across the water already in that port's key, and sailing back greets you in the key it always had. | #69 **CLOSED** | v0.0.20260628055104 | 583 unit tests (+6 TDD in `tests/unit/town-theme.test.mjs`: deterministic across calls · the 3 real ports musically distinct · every field in sane range · chordMidi = root·mode-third·fifth·octave · headless-safe junk input · good key spread); playtest ✓ PASSED, zero console errors (audio inert headless); perf **32/130 draws · 90k/150k tris** (audio = pure CPU, zero new draws). CI green, live 200. Audio is not visible → no gallery shot. **Follow-up (filed):** #129 — richer per-town melodies/instrument sets + a dedicated docked cue + live tempo. |
| 65 | **#104b — Port-memory DEPTH: a port recalls your LAST notable deed BY NAME on return (the #104 follow-up).** #104 made a port remember your FACE (visit count + standing tone); #104b makes it remember what you DID there — a specific memory, not just a warm/cool greeting. Extended pure `src/systems/port-memory.js`: the tiny per-port record gains an optional `lastDeed` phrase; new pure **`deedPhrase(event)`** maps a combat win / chased-rumour payoff onto a terse in-character memory framed from the port's vantage ("the day you sent Captain Ironwake to the seabed in these waters"); **`recordDeed(store, port, phrase)`** banks it WITHOUT bumping visits (so a deed in a port's waters is remembered even before you next dock — "your reputation precedes you", the #104 verb); `recallLine` now LEADS with the named deed (return ▸ reputation-precedes ▸ home variants), and `sanitizePortMemory`/`rememberArrival` carry the deed through a save round-trip + later visits. Also laid the **"Your Harbour" seed**: pure **`homePort(store)`** = most-visited port once it clears `HOME_MIN_VISITS` (=3), ties broken by name → a frequented port greets you as its own (full investment system tracked in **#118**). `main.js` stays thin: combat onEnd + rumour payoff call `rememberPortDeed`/`recordDeed` (deed attributed to `ports.nearestPortName`); `onArrive` banks the visit then reads the home flag off the fresh store; `tw.homePort` QA surface added; **save v10→v11** (additive, fail-open — deed rides the existing `portMemory` store). CREATIVE SPARK (Game Designer): the harbourmaster doesn't just know your name, he knows your *story* — "word reached here ahead of you." | #104 (closed; #104b depth — commented) | v0.0.20260628061042 | 595 unit tests (+16 TDD in `tests/unit/port-memory.test.mjs`: deedPhrase names the foe / rumour / junk · recordDeed stamps without a visit, newest wins, no-op on junk, immutable · deed survives arrival + save round-trip · deed-only record kept · recallLine leads with the deed by name · reputation-precedes at a never-docked port · stranger stays null · homePort threshold + most-visited + name tie-break · home greeting + home-deed); playtest ✓ PASSED, zero console errors; perf **29/130 draws · 89k/150k tris** (pure logic + greeting text — zero new draws). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/v0.0.20260628060652-port-remembers-deed.png` (first landfall at Saltpurse Quay, the harbourmaster recalling "the day you sent Captain Ironwake to the seabed in these waters" — reputation precedes you). **Follow-ups:** #118 the full "Your Harbour" claim-&-grow-a-home-port investment system (this slice lays the home-port seed) · richer tone+deed blending (a deed recalled with a turned-pirate chill) · deeds for trades/duels-ashore. |
| 66 | **#125 — Emergent at-sea encounter: a foundering ship, RESCUE vs PLUNDER (CLOSES #125; DL #4 reservoir).** Chosen to enrich **SAILING** — our most-used but least-reactive mode — by turning traversal itself into a story-generator (the DL #4 "traversal as story-generator" lens). While sailing the open sea a **seeded** founderer occasionally heaves into view and presents a real moral CHOICE — **RESCUE** her crew (lawful → **Standing** + a grateful line) or **PLUNDER** the wreck (infamous → **Infamy + coin** + a colder line) — echoing the False-Colours (#79) / Strike-the-Colours (#72) moral-choice DNA. It resolves to the right reputation **pole** + reward + a distinct **Ballad/voyage-log verse** (#78), then **despawns cleanly** (and slips quietly under if you sail on past it without choosing — a missed chance). **Smallest always-working increment:** ONE encounter type, end-to-end. New **PURE `src/systems/encounter.js`** (DOM-/three-free, the #53 standard): seeded spawn cadence keyed to **distance SAILED** (`rollNextSpawn`/`dueToSpawn` — deterministic + headless-safe, NOT wall-clock), `placeFounderer` (ahead of the bow at ~170u, within the choice range so it reads immediately), `resolveEncounter(choice)` (rescue→governor pole/Standing 120; plunder→pirate pole/Infamy 120 + 80c), name/line pickers — all TDD'd FIRST (19 cases). `voyage-log.js` gains a new **`encounter`** deed type with separate rescue/plunder verse pools (chosen by `e.choice`, ahead of the existing treachery/lawful/capture branches). `main.js` stays thin: a per-voyage seeded mulberry32 RNG (re-seeded on `newVoyage`), **reuses the hero hull** for the wreck (its OWN `loadShip()` GLB instance so the player's False-Colours pennant tint never leaks onto it; heeled over + settled low each frame so she reads stricken; shown ONLY when an encounter is live → ~1 draw call exactly when on screen, hidden otherwise), applies the reward on the shared ledger via the same `initEconomy`/`syncRenown` path as combat, and exposes `tw.encounter` / `tw.encounterSpawn()` (the deterministic QA spawn hook the contract asked for) / `tw.encounterChoose()`. Keys: while a founderer is alongside, **1 RESCUES / 2 PLUNDERS** (claims the digit keys + blocks hailing/firing so the beat isn't stepped on). `hud.js` + `index.html`: the **rescue-vs-plunder choice panel** (`#encounter`, teal distress accent, reuses the duel panel's inner classes; tappable on touch; hidden in town). CREATIVE SPARK (Game Designer): the open sea looks back at you — a distress flare is a mirror; rescue and sail off a touch more respected with a crew's blessing in your wake, plunder and sail off richer and a touch more feared. | #125 **CLOSED** | v0.0.20260628063323 | **618 unit tests** (+19 TDD `tests/unit/encounter.test.mjs`: spawn cadence in-bounds + only-once-due · placement ahead within choice range · rescue→governor-only / plunder→pirate-only + coin · unknown choice rejected · poles genuinely opposite · deterministic name/line pickers · controller seeded-spawn lifecycle / canSpawn-suppress / forceSpawn / choose-once / despawn-on-wander / inRange / reset / JSON-safe snapshot; +4 `voyage-log.test.mjs`: encounter accept/reject · distinct rescue & plunder verses · no-dedup); playtest ✓ PASSED, zero console errors — new section spawns a founderer via the QA hook, asserts RESCUE pays only Standing (no coin/infamy), PLUNDER pays only coin+Infamy (no Standing), each despawns + sings into the Ballad **by name**, survives a save round-trip. perf **32/130 draws · 90k/150k tris** (one extra hull shown only when on-screen — the ≤130/≤150k mandate held). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/v0.0.20260628063022-foundering-ship.png` (the "🆘 A ship in distress!" hail toast + the "🆘 the Brine Mary — foundering!" choice panel with 1-Rescue / 2-Plunder over the open sea). Also: **queue hygiene** — struck the now-SHIPPED top-of-queue items (#112/#111, #105, #69, #104b) so the top line tracks live work. **Follow-ups (filed in queue under #125):** more encounter types (a becalmed trader, a derelict, a press-ganged crew) · richer outcomes (a grateful crew that fights at your side, a cursed hold) · a flying distress flag on the wreck. |
| 67 | **#118 — Your Harbour: CLAIM & GROW a home port — the GOVERNOR pole's first reactive verb (DL #4).** The pirate pole had reactive verbs (raid / false-colours / legend); the respected-governor half of the fantasy was still just a number on the needle. Cheapest fix, reactive-verbs-first: at **sufficient Standing** you **CLAIM** your docked port as a home harbour, then **SPEND coin** there to **GROW** it a level at a time — each investment earns **Standing** and warms a **homecoming greeting** as the port visibly prospers under your hand. Promotes the parked #104b "Your Harbour" seed (the emergent most-visited home) into a deliberate, persisted CHOICE + a coin sink that pays the lawful pole. **Smallest always-working increment:** claim + ONE working investment, end-to-end. New **PURE `src/systems/home-port.js`** (DOM-/three-free, the #53 standard): `claim`/`canClaim` (Standing-gated: `CLAIM_STANDING`=40, earns `CLAIM_REWARD`=15), the `invest`/`canInvest` ladder (cost 150/350/700 → +40/+70/+120 Standing, up to `MAX_LEVEL`=4), `sanitizeHarbour` (fail-open), and a per-level warming `harbourGreeting` — all TDD'd FIRST (20 cases). `town.js` gains the ashore **🏠 Your Harbour** panel (claim plank when respected enough / a clear Standing-needed or has-a-home hint otherwise; at home: the tier label + homecoming greeting + an **⚒ Invest Nc — grow your harbour (+S standing)** plank); `main.js` stays thin (owns the claim/invest mutations on the shared ledger via `initEconomy`/`syncRenown`, persists each, exposes `tw.harbour` + `tw.claimHarbour()`/`tw.investHarbour()` + `tw.setCoins` QA seam). `voyage-log.js` gains a **`harbour`** deed type (claim/grow verses, deduped per tier) so raising a home sings into the Ballad (#78). **Save bump v11→v12** (additive, fail-open — a clean `{name,level,invested}` claim round-trips; junk → null). CREATIVE SPARK (Game Designer): the lawful captain's answer to plunder — you don't take a place, you RAISE one; the first claim is a berth with your name carved in the post, by the top tier the whole coast lights its lamps early for your homecoming. | #118 **CLOSED** | v0.0.20260628065245 | **643 unit tests** (+20 TDD `tests/unit/home-port.test.mjs`: fresh/sanitize fail-open + round-trip · isHome/labels · claim Standing-gate + already/has-home/no-port + mints level-1 + earns Standing + pure-noop · invest cost/not-home/no-coins/maxed gates + grows-one-level returns spent+Standing + pure + climbs to MAX then refuses · greeting null-off-home + names port + warms per tier; +3 `save.test.mjs`: harbour round-trip / drops junk+absent / malformed never rejects; +2 `voyage-log.test.mjs`: harbour accept/reject + ballad sings claim deduped per tier); playtest ✓ PASSED, zero console errors — new your-harbour section: land → claim GATED (locked poor, open respected) → claim earns Standing + a homecoming greeting in the panel → invest spends coin + earns Standing + grows a level + sings the Ballad + shows the invest plank → survives a save → a fresh voyage clears it. perf **32/130 draws · 90k/150k tris** (DOM/logic only — zero new draws). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/v0.0.20260628085124-your-harbour.png` (the town panel at Saltpurse Quay: "🏠 Your Harbour — Thriving home port lvl 3/4", the warming homecoming greeting, the "⚒ Invest 700c — grow your harbour (+120 standing)" plank). **Follow-ups:** deeper investment TIERS + named upgrades (warehouse/shipyard/lighthouse) · VISIBLE in-world port growth (instanced dressing scales with level) · MOVE/relinquish a home port · home-port perks (cheaper refit, a safe berth, a passive standing tick) · a docked home-port musical cue. |
| 68 | **#122 — Declarative save-migration codec + frozen old-save corpus (DL #4 hardening).** The save schema was bumped v8→v12 in ~20 cycles, each an ad-hoc edit to a growing sanitise god-function, and `deserialize` HARD-rejected any `v !== SAVE_VERSION` → **every bump silently wiped player progress**. Replaced the gate with an **ordered, declarative forward-migration pipeline** in `src/save.js`: `migrations[v] = fn` steps (v_n → v_{n+1}) upgrade an old save one rung at a time to the current shape, then hand it to the **single validated reader** (still the source of truth for field sanity; **fail-open preserved as the final safety net**). Most steps are pure version bumps (v4+ fields are reader-fail-open flavour); the only data-adding step is v1→v2 (seed starting purse + empty hold); v3's combined renown can't be decomposed → poles start 0 (the one documented historical loss; all load-bearing state survives). New **`export migrate(obj)`** (pure, total, null on missing/future/non-integer/gapped version). **Behaviour-preserving:** a current save is a no-op through migrate and loads identically (asserted). Added a **FROZEN old-save corpus** (`tests/unit/save-corpus.test.mjs`, one real blob per historical version v1..v11) asserted to migrate forward without loss or error — the GATE that stops the next bump (#100 battle) wiping a voyage; plus a coverage guard that every prior version has a frozen blob. **No live save-format change** (still v12 — the refactor didn't require a bump). CREATIVE SPARK (Tech Lead): an old save is a message in a bottle — you don't burn the ones you can't read on sight, you teach the ship to read the older hands. | #122 **CLOSED** | v0.0.20260628070429 | **669 unit tests** (+15: corpus v1..v11 each migrates forward without loss · `migrate()` stamps to current · no-op on a current save · fails open on missing/future/non-integer/below-range version · a migrated save re-serialises to a clean current save that loads identically · corpus covers every prior version; +3 rewritten in `save.test.mjs`: old/v1/v2/v3 now MIGRATE forward not wipe, missing/future still fail open); playtest ✓ PASSED, zero console errors; perf **32/130 draws · 90k/150k tris** (logic-only — zero new draws). CI green, live 200. **Not visible → no gallery shot.** Refused/flag note: nil — clean slice, no injection encountered. **Follow-ups:** #123 (QA golden-replay fixture for the full reactive loop) rides the same hardening theme; keep adding a frozen prior-version blob to the corpus on every future SAVE_VERSION bump (the corpus coverage guard enforces it). |
| 69 | **#126 — Reputation-reactive world grade: the look reflects WHO YOU'RE BECOMING (Retro 12 TOP #1 / DL #4).** The whole spine is the Infamy↔Standing pole, but the WORLD's look never said who you'd become. Smallest always-working increment: a deterministic pole→grade mapping that eases the live scene grade toward your dominant pole — **infamous → colder, steelier, lower-key** (stormy/ominous); **lawful → warmer, brighter, golden** (prosperous); **balanced → today's sunny Caribbean default, untouched**. New **PURE `src/systems/reputation-grade.js`** (DOM-/three-free, the #53 standard): `reputationLean(infamy,standing)` — the same tilt `renown.dominantPole` uses, **deadzoned to the neutral band** (a 60/40 lean still reads balanced → no tint), **smoothstep-eased**, and **bounded to `MAX_LEAN`=0.35** so it tints the mood without ever drowning the Caribbean; `leanPole` + `gradeHaze`/`gradeSun`/`gradeSunKey` colour eases (reuse `mixHex`) — all TDD'd FIRST (9 cases). `main.js` stays thin: `applyReputationGrade()` runs **AFTER `daynight.update` (#58)** and **BEFORE `applyLandfallGrade` (#102)** — the CRITICAL composition note. It **re-sources its base every frame** (day-night's live palette when the cycle owns the look, else the captured sunny default) so it **never compounds**; restores the default **byte-exact at neutral** so nothing leaks; and layers UNDER the landfall golden warm so the two never fight. **Colour/uniform writes only — zero new draws.** QA hook `tw.grade` exposes `{lean,pole,tinted,haze,sunIntensity}`. CREATIVE SPARK (Game Designer): "every sail becomes a mirror of your legend" — sail infamous and the sea itself grows wary and steel-grey; sail lawful and the light warms to gold; sit balanced and it's the sunny holiday it's always been. | #126 **CLOSED** | v0.0.20260628073440 | **678 unit tests** (+9 TDD `tests/unit/reputation-grade.test.mjs`: neutral-by-default + junk-safe · balanced deadzone reads neutral · infamy→+pirate / standing→−governor + symmetric · bounded to MAX_LEAN · monotone past the band · gradeHaze neutral byte-exact + cool-vs-warm + never overshoots target · gradeSun cool/warm · gradeSunKey dim/lift/neutral-1 bounded); playtest ✓ PASSED, zero console errors — new rep-grade section drives the ledger infamous (cooler, lower-key haze + dimmer sun), then lawful (warmer haze + lifted sun), then back to neutral and asserts the sunny default + sun key restore **byte-exact** (proving clean composition over #58). perf **32/130 draws · 90k/150k tris** (uniform/colour writes only — zero new draws). CI green, live 200. Owner-facing VISUAL → gallery `studio/qa/gallery/v0.0.20260628073132-reputation-grade-{infamous,lawful}.png` (same ship+island frame, cold steely cast as Dread Captain · feared vs warm golden cast as Magistrate · respected). Refused/flag note: nil — clean slice, no injection encountered. **Follow-ups:** in-world diegetic heading wisp for #111 (rhumb-line wisp + horizon smudge, not a UI pin — the issue's second ask); richer reputation signals (banners, NPC density) per dominant pole. |
| 70 | **#120 — Self-registering systems registry → thinner `main.js` (Retro 12 TOP #1 / DL #4 hardening, battle de-risk).** `main.js` had crept back to ~1019 lines and `update()` was a long hand-wired list of per-system tick calls — exactly what rots as the world grows, and the thing battle (#100) would have to fork. Smallest always-working **behaviour-preserving** increment: a small pure backbone **`src/systems/registry.js`** (`createSystemsRegistry()`, DOM-/three-free, the #53 standard) — each system `register({name,order,update})`s its per-frame tick and the registry **dispatches in deterministic order** (sort by `order` asc, ties broken by **registration sequence = stable**), with guards (missing name / non-function update / duplicate name all throw) + `names`/`count`/`clear` introspection. Migrated a **representative contiguous block** — `wake → fauna → props → hud` — onto it as the proven pattern, **byte-for-byte** preserving the old call order + arguments (orders 10/20/30/40, spaced so a future system slots between without renumbering); the remaining hand-wired systems migrate in a follow-up. `update()` loses 4 inline calls for one `systems.run({dt,t,state})`. New read-only QA hook `tw.systems` lists the migrated systems in dispatch order; **all existing `window.__tidewake` hooks intact**, `tw.step` determinism preserved. CREATIVE SPARK (Tech Lead): the update loop stops being a hand-copied muster list and becomes a ship's watch-bill — every future system signs on with one line and falls in at its ordered station, so battle can pipe its own crew aboard without rewriting the whole roster. | #120 **OPEN** (a few systems migrated as the proven pattern; remaining systems = follow-up #130) | v0.0.20260628074440 | **687 unit tests** (+9 TDD-first `tests/unit/systems-registry.test.mjs`: same-ctx dispatch + returns ctx · equal-order stable · lower-order-first · slot-into-gap · default-0 + tie order · `names`/`count` reflect dispatch order · guards throw on bad name/update/dup · `clear` empties · repeatable across frames); playtest ✓ PASSED, zero console errors; perf **32/130 draws · 90k/150k tris** (UNCHANGED — pure refactor, zero new draws); golden mode-trace unaffected (pure test). CI green, live 200. **Not a visual change → no gallery shot** (refactor only). Refused/flag note: nil — clean slice, no injection encountered. **Follow-up:** #130 migrate the remaining hand-wired systems onto the registry (incrementally, behaviour-preserving each step); registry is the natural home for #36 fixed-timestep + the seam battle #100 plugs into. |
| 72 | **#130 — Finish the systems-registry migration: the registry is now the DEFAULT per-frame loop + a `when(ctx)` mode gate (CLOSES #130; DL #5 unanimous #1).** #120 put a representative block (wake/fauna/props/hud) on the self-registering registry; newer slices kept hand-wiring their per-frame tick, so `main.js update()` had regrown to a ~140-line hand-wired list — exactly the seam battle (#100) forks. This migrates **every** remaining tick (mode/landfall/sailing/disguise/npcs/encounter/combat-beats/ocean/daynight/grades/ports/town/islands + the #120 block + legends/onboarding/charts/HUD-panels/audio/music) onto the registry as 31 ordered systems; `update()` collapses to a single `systems.run({dt,t,state,seenThrough:false})`. **Byte-for-byte behaviour-preserving:** identical dispatch order, the SAME mutable frame `ctx` threads the control-state the old loop kept in locals (`fighting/paused/mode/harbourDistance/seenThrough`), `window.__tidewake` intact, `tw.step` determinism preserved — proven by the #107 mode-trace + #123 reactive-loop golden replays + playtest passing identically. **DL #5 `when(ctx)` predicate** added to the registry (TDD'd first, +6 unit tests): a system declares the modes/conditions it runs in instead of an inline `if`-guard — the landfall-camera (`blend>0`) and disguise (`!paused && isDeceptive`) gates now declare it; the battle seam can register its ordered sub-loop with a `when()` mode gate rather than editing `update()`. **main.js lines 1399→1399** (the `update()` loop went 142→8 lines; the equivalent volume is now declarative `register()` calls in `main.js` — extracting heavy tick bodies, e.g. scene-grade/disguise/encounter, into `src/systems/` modules is the documented follow-up tail, now UNBLOCKED because they're declarative systems, not hand-wired loop code). CREATIVE SPARK (Tech Lead): the watch-bill is now the WHOLE ship's company — every hand falls in at its ordered station and battle pipes its crew aboard with one line + a `when()` "report only when at battle stations", never rewriting the roster. | #130 (**CLOSED**) | v0.0.20260628083147 | 695 unit tests (+6 TDD in `tests/unit/systems-registry.test.mjs`: when→false skips · when→true runs · when gets same ctx · re-evaluated per frame · bad-when throws · names lists gated systems); playtest ✓ PASSED, zero console errors; perf **32/130 draws · 90422/150k tris** (unchanged). Not a visual change → no gallery shot. CI green, live 200. Refused/flag note: nil — clean slice, no injection encountered. |
| 73 | **#132 — The reputation needle, made personal & audible (DL #5; the "felt/charm" gap).** #126 made the WORLD mirror the Infamy↔Standing pole, but a reputation change itself was still a silent number. This makes every shift FELT: a HUD gauge needle (⚖ Standing ↔ ⚔ Infamy) that swings toward your pole and, the instant a real shift lands — a kill, a rescue, a rumour payoff, an investment, ALL just move the ledger — strikes a short modal **audio sting** (dark freygish/minor-6th bite toward Infamy; warm rising Ionian/Lydian chime toward Standing — echoing the #94 mode-aware bed, asset-light) and murmurs a **tiered personal in-character line** acknowledging who you're becoming ("You are the storm they warn the children about." / "The isles would crown you, and gladly."). TDD-first PURE core `src/systems/reputation-needle.js` (`needleTarget` signed+deadzoned like #126 · `reputationShift` delta→pole/tier/cue/line · `needleTier`/`needleAngle`/`ackLine`/`easeNeedle` frame-rate-independent) with +21 unit tests; a self-contained HUD component `src/ui/reputation-needle.js` owns the SVG gauge, eases the pointer, pulses + tints + shows the line on a shift, and fires the cue via an injected callback so audio stays guarded (`audio.playRepSting(pole,tier)`, headless-silent). Registered on the #130 registry (`reputation-needle`, order 215, right after the HUD). `window.__tidewake.needle` QA surface (pos/target/pole/tier/last shift) + a headless playtest assertion (fresh centred · infamy gain swings pirate + registers cue+line · standing gain swings back governor). CREATIVE SPARK (Game Designer + Sound Engineer): a reputation change should land like a struck bell — the needle leaps, the dial flashes its pole's colour, the world names the captain you're turning into. | #132 (**CLOSED**) | v0.0.20260628084634 | 716 unit tests (+21 TDD `tests/unit/reputation-needle.test.mjs`); playtest ✓ PASSED, zero console errors; perf **32/130 draws · 90422/150k tris** (unchanged — HUD is DOM/SVG, zero new THREE draws). Owner-facing VISUAL → `studio/qa/gallery/v0.0.20260628084634-reputation-needle.png` (needle mid-swing toward the pirate pole + the personal line). CI green, live 200. Refused/flag note: nil — clean slice, no injection encountered. **Follow-ups:** richer needle visuals / per-tier flourishes; could share the needle param with Slice B (continuous modal recolour of the bed) per DL #5. |
| 74 | **#119 — Home-isle governorship: the lawful endgame crown (DL#4/#5; the governor mirror of the pirate legend-crown #46).** The Infamy pole's summit crowns a legend (#46, "Terror of the Tidewake" at renown 2400); the Standing/governor pole now has its NAMED capstone, tied to the very port you RAISED (#118). Grow your home harbour to its top tier AND climb Standing past `GOVERNOR_STANDING` (400 — a little above what the home-port arc alone banks, so a focused governor tips over with a sale or two more; deliberately well below the 2400 legend gate so the named home crown lands FIRST, giving the lawful climb a nearer, stateable destination + a felt distance to it). The isle then proclaims you **Governor of [your home port]**, crowned ONCE with the exact #46 fanfare/persistence pattern: a near-full-screen crown overlay (reuses the legend-card via a shared `showCrown`), a once-per-isle Ballad/voyage-log verse (counts as a crown in the tally), a persistent HUD badge, an acknowledgement at your home quay on every landfall, and the title locked into the save. TDD-first PURE core in `src/systems/home-port.js` (`earnedGovernorship`/`governorTitle`/`governorshipBeat`, junk-safe). **Save bump v12→v13**: `governorship` boolean, additive + **fail-open** like `legends` (junk coerces, never rejects), with a frozen v12 corpus blob added. Per-frame `checkGovernorship()` on the #130 registry (order 221, just after `legends`). Also fixed a **latent #118 bug**: `sailing.restore` never re-applied the claimed harbour, so a home port (and now its governorship) didn't survive a page reload — now restored + cleared on new voyage. QA `tw.governorship`/`tw.governorshipEarnable` + snapshot fields. Asset-light: text + reused overlay/badge, **zero new draws/tris**. CREATIVE SPARK (Game Designer): the lawful captain's answer to plunder is permanence — you don't seize a place, you raise it, and the isle you built crowns you its own ("A street now bears your name. So, against all advice, does a remarkably good pie."). | #119 (**CLOSED**) | v0.0.20260628090954 | 726 unit tests (+9 TDD: home-port governorship gate/title/beat · voyage-log verse+dedup · save v13 round-trip+coerce · frozen v12 corpus); playtest ✓ PASSED (end-to-end claim→grow→crown→persist→clear→no-refire), zero console errors; perf **32/130 draws · 90422/150k tris** (unchanged — HUD is DOM/SVG). Owner-facing VISUAL → `studio/qa/gallery/governorship-119-crown.png` (the crown overlay "You are now THE GOVERNOR OF SALTPURSE QUAY" + the persistent badge). CI green, live 200. Refused/flag note: nil — clean slice, no injection encountered. **Follow-ups:** richer governor PERKS/endgame (home-port tax revenue, a governor's writ, council quests). |
| 75 | **#110 — Living fauna phase 2: jumping dolphins (CLOSES #110; continues #97, the #70 sea-delight rule).** Phase 1 put an instanced gull flock over the ship; this adds the second living-sea beat — a small **pod of dolphins** that occasionally surfaces and **arcs (leap → dive) alongside the moving ship**, then slips cleanly back under. Smallest always-working increment: ONE dolphin behaviour, on a seeded Poisson-ish cadence (the `nextGullDelay` random-interval idiom), surfacing off the bow-beam **only while genuinely under way**, the pod cascading out of the water (staggered per-dolphin delays) and **despawning cleanly** after the arc. Reused the #97 pattern: a second `THREE.InstancedMesh` pod pool in `src/fauna.js` (**≤1 extra draw call**, hidden wholesale = 0 draws between appearances + **distance-culled** at CULL_RADIUS). PURE breach math TDD'd first in `src/fauna-math.js` (`nextPodDelay`, `dolphinParams` spread+cascade, `breachArc` bounded leap pitching nose-up out/nose-down in, `dolphinPosition`, `podSpawnOrigin`). Registered on the **#130 systems registry**; surfaces at the ocean **waterline** (`sampleHeight`); **paces the ship** (slides at the ship's own speed, min POD_SWIM_SPEED) so it rides alongside instead of being overtaken. **Deterministic & headless-safe:** mulberry32-seeded schedule → same stepped breach times every run; `window.__tidewake.fauna.pod` exposes active/drawn/progress/breaches/surfaced for QA. CREATIVE SPARK (Game Designer + Graphic Designer): a pod that leaps in to ride your bow-wave when you're making way — the sea answering "you're alive and moving" — and dives off as you slow; a low-poly grey body with dorsal fin + tail fluke, swept to read in silhouette. | #110 (**CLOSED**) | v0.0.20260628094945 | 731 unit tests (+11 TDD in `tests/unit/fauna.test.mjs`: pod-delay clamp/range · params deterministic+spread+cascade · breach bounded/peaks-mid/pitch-sign · position rides-heading+surfaces-mid-leap · spawn ahead-of-bow off-beam never-on-hull); playtest ✓ PASSED (breach fires on schedule · a dolphin surfaces above water · pod drawn mid-arc · despawns to 0 draws after), zero console errors; perf **32/130 draws · 90422/150k tris** (pod adds ≤1 draw only mid-breach, 0 at rest). Owner-facing VISUAL → `studio/qa/gallery/dolphin-breach-110.png` (the pod arcing alongside the ship — one diving nose-first, others mid-leap; gulls above, an island astern). Capture note: froze the rAF loop + `tw.qaRender()` for a deterministic mid-arc frame (the live loop otherwise overwrites a stepped frame). CI green, live 200. Refused/flag note: nil — clean slice, no injection encountered. **Follow-ups:** a dolphin-chirp SFX layer paired to the breach; a rarer bow-riding variant (a dolphin tucking under the bow). |
| 76 | **#121 — Gate: resource-conservation invariant + transition-frame perf (CLOSES #121; DL#4 hardening; the pre-battle de-risk #100 inherits).** #107 pins the mode EVENT order and #108 budgets ONE settled frame — neither catches a mesh/material/geometry that is BUILT on a mode transition but never DISPOSED, growing unbounded across repeats. This adds that missing oracle. New headless playtest section (2s): drives an **N×N (8×) cycle over every LEGAL mode edge** (SAILING↔TOWN↔BATTLE, incl. a fight erupting ashore TOWN→BATTLE) **plus the full landfall enter/leave gesture**, and asserts the renderer's OWN tracked counts (`renderer.info.memory.{geometries,textures}` via `tw.perf`) **return to a warmed baseline — no growth** — the leak class draw/tri budgets cannot see. Robust against false positives: a one-cycle **warm-up flushes one-time lazy GPU allocation** before the baseline is sampled, so the gate fires only on UNBOUNDED growth, not first-touch init. Second half latches the **worst TRANSITION-frame perf sample** (the enter/leave INSTANT, where a future mesh-heavy battle builds while another mode tears down) and gates it against the perf budget (`checkBudget`). enter/leave drive **`mode.subscribe`** — the exact seam battle #100 will hook build/teardown into — so the day a transition leaks a geometry, THIS catches it before it ships. CREATIVE SPARK (QA/Tech Lead): a tripwire wired across the mode machine, not a number in a doc — battle becomes safe to land behind a teeth-bearing gate. **Leak verdict: CLEAN** — modes build no per-mode meshes today, so it is GREEN (geom **62→62 (+0)** · tex **4→4 (+0)** · worst transition **48 draws/90,390 tris**, in budget); the invariant itself is the deliverable, standing in front of the parked slice-4 disposal (#106). | #121 (**CLOSED**) | _none — test-only (tests/**), no release/version fires (workflow `paths:` is `src/**`); pinned by commit 84bdcc7_ | 731 unit tests (unchanged — renderer-level invariant lives in the playtest, which is its only faithful home: it needs the REAL `renderer.info.memory` a pure node test cannot see; main.js untouched/thin); `node tests/playtest.mjs` **✓ PLAYTEST PASSED** ×3 stable, zero console errors; perf **32/130 draws · 90,422/150k tris** (test-only, no runtime change). No CI run fires for a tests-only push (workflow `paths:` gate) → gates verified LOCALLY; live site HTTP 200 (unchanged at the prior build v0.0.20260628094945). Not a visual change → **no gallery shot**. Refused/flag note: nil — clean slice, no injection encountered. **Follow-ups:** extend the cycle to drive battle via REAL combat (not just the enter/leave toggle) once #100 lands its sub-loop; assert disposal symmetry per-system as #106's slice-4 dispose registry arrives. |
| 77 | **#117 — Seeded per-pass melody variation kills sail-loop fatigue (CLOSES #117; DL#4/#5, the "felt/charm" half DL#5 flagged as under-delivered).** The sailing theme is the most-heard audio in the game and `melodyPattern()` is ONE fixed 8-bar loop that audibly tiles on a long sail — the ear locks the seam and the charm curdles into wallpaper. Smallest always-working cure that still feels *composed*, not random: ONE coherent mechanism — at every 32-beat pass seam the lead is **re-laid with a seeded set of octave ornaments** (1–3 lead notes lifted/dropped an octave per pass, deterministically chosen). **In-key BY CONSTRUCTION** — octave-only shifts (±7 scale degrees) preserve pitch class exactly, so it's always the same scale/chord tone, just a register glint, never a wrong note; **timing untouched** (only `deg` changes → still fills the loop, bass/pad/cadence land where they always did); **pass 0 is canonical** (a fresh sail opens in the home version); the **phrase-entry + cadence notes are protected** so every pass still opens and resolves home; a bounded ornament cap + register window [deg 1..13] → a shimmer on the repeat, never a re-composition. TDD-first PURE core in `src/systems/melody-variation.js` (mulberry32 PRNG · `variationPlan`/`varyMelodyPass`, junk-safe, never mutates input); `music.js` rebuilds its lead schedule at each pass boundary in the lookahead scheduler and exposes `music.variation()` (headless-safe — pure, no AudioContext; pass stays 0 with no engine). **Deterministic** (same seed → same sequence) + `window.__tidewake.seaVariation` / `seaVariationFor(pass)` QA surface. CREATIVE SPARK (Musician): a shanty player never sings the verse identically twice — they glint a phrase up an octave here, drop a high note into the chest voice there; now the sea theme breathes the same way on the repeat. | #117 (**CLOSED**) | v0.0.20260628104148 | 742 unit tests (+11 TDD in `tests/unit/melody-variation.test.mjs`: PRNG determinism/spread · pass-0 canonical · plan determinism + seed-sensitivity · bounded count + protected entry/cadence · octave-only/in-key · timing preserved fills loop · register window · cross-pass distinctness · no input mutation); `node tests/playtest.mjs` **✓ PLAYTEST PASSED**, zero console errors; perf **48/130 draws · 90,390/150k tris** (audio-only, no runtime draw change). **Audio is not visible → no gallery shot** (per brief). CI green, live 200. Refused/flag note: nil — clean slice, no injection encountered. **Follow-ups:** per-section A/B reseating + motif-fragment rotation (richer variation); adaptive variation density (intensity/mode-driven); a per-town variation seed so each port's bed also evolves; a paired ornament-velocity touch so a lifted note also brightens. |
| 78 | **#133 — Contested rumour: a rival races you for the prize (CLOSES #133; DL #5, the *competition* layer on the reactive rumour lane #112/#115 — distinct from #128's spawned vessel).** A still rumour becomes a real DECISION. SOME chased tavern tips are now **contested**: a **seeded, recurring named rival captain** (picked off the target name, so the same tip always names the same captain — the sea remembers who keeps beating you) races you for the same bounty on a **soft, distance/time-keyed clock** (NOT wall-clock — the #125 standard; `budget = grace + distance/rival-pace`, the rival pacing below your cruise so a *direct* run wins). Sail straight there → **win it as normal + a "you beat them to it" beat**; **dawdle ashore on errands / take the long way** → the clock runs down, the rival **CLAIMS it first**, and you arrive to find the prize **gone** (a wry line, NO reward). **Both win-race and lose-race paths sing DISTINCT verses into the Ballad** (#78) and into per-port memory (#104b). Non-combat (battle **#100 stays owner-held**); hands battle a ready-made recurring antagonist (note-support only). TDD-first PURE core in `src/objectives.js` (`pickRival` · `contestBudget` · `shouldContest` ~1-in-3 · `makeContestedObjective` · `tickContest` claims at clock-out · `payoffFor` claimed→0 · `sanitizeContest` fail-open, **re-derives `claimed` from the clock so a reload can't reset the race** — the anti-exploit). `voyage-log.js` + `port-memory.js` carry rival + won/lost. **Save v13→v14** (contest persisted inside the existing `objective` field, additive/fail-open; migration step + frozen v13 corpus blob per the #122 rule). `main.js` thin — a `contest` registry system ticks the clock **every frame, even ashore**; `onArrive` branches win/lose; QA spawn/resolve hooks `tw.chaseContested`/`tw.rivalClaim`. | #133 (**CLOSED**) | v0.0.20260628135819 | **758 unit tests** (+~16 TDD across objectives/voyage-log/port-memory/save/save-corpus); `node tests/playtest.mjs` **✓ PLAYTEST PASSED**, zero console errors — BOTH race paths asserted end-to-end **incl. a reload proving the rival claim persists** (no reload-reset exploit); perf **48/130 draws · 90,390/150k tris**, no mesh leak. VISUAL → `studio/qa/gallery/contested-rumour-133.png` (the "⚑ A race for Barnacle Bottom! …" beat + chased pin on the minimap). CI green, live 200. **No injection encountered.** CREATIVE SPARK (Game Designer): the rumour stops *waiting* for you — there's a rival's wake to beat, and a grudge if you don't. **Follow-ups:** a VISIBLE rival sail + interception (a real chase, not just a clock); the recurring rival feeding battle #100's antagonist when it lands. |
| 79 | **#131 — [DL#5] Harden the golden-replay gate: determinism-parity (the parity HALF of #131; the 2nd invariant remains).** The live `loop()` integrates the reactive-loop's time-driven systems with ONE variable `dt=min(clock.getDelta(),0.05)` per rAF frame (`main.js` ~L1186); the headless `tw.step()` AND every golden replay (#123) use **FIXED 1/60 sub-steps** (~L1500) — TWO integrators, so #123 only ever proved the path *players never run*, and a frame-rate-dependent regression in a loop system could sail clean through the gate. This pins them together: a parity harness in `tests/unit/reactive-loop-replay.test.mjs` drives ONE seeded run through **faithful mirrors of BOTH feeders** — the variable-dt LIVE feed (`tick(o, Math.min(ft,0.05))` per frame, the loop's exact clamp) and the fixed-1/60 HEADLESS feed (`while(acc>0){dt=min(1/60,acc)…}`, step()'s exact slicer) — over the two PURE dt-integrated reactive-loop systems: the **#133 contested soft clock** (`tickContest`, ticked on `f.dt` by the registry's `contest-clock` system) and the **#102 landfall gesture** (`landfall.step`). Same total sim-time in → same sampled state out (elapsed/claimed/blend/phase) within 1e-9; a non-linear dt term or a per-CALL effect that scales with sub-step count diverges the two paths and trips the gate. **Mutation-verified the guard is real:** it FAILS on an injected `step+step*step` (dt²) term and on a per-call `+0.001` constant in `tickContest`, and on a `dt+dt*dt` term in `landfall.step`; PASSES clean on revert. Closes the variable-vs-fixed-dt divergence class **structurally**, and is the parity contract a future **#36 fixed-timestep** must preserve. CREATIVE SPARK (QA + Tech Lead): the gate now races the SAME chase at a smooth 60fps and at a juddery, frame-pegged crawl and demands the rival claims the prize at the same moment either way — a laggy device must never change who wins. | #131 (**OPEN** — parity invariant SHIPPED; save-round-trip-survives-replay remains as the next slice) | _none — test-only (tests/**), no release/version fires (workflow `paths:` is `src/**`); pinned by commit 042aa08_ | **760 unit tests** (+2 in `tests/unit/reactive-loop-replay.test.mjs`: contest-clock parity over a 5s-budget seeded chase incl. claim-past-budget · landfall-gesture parity mid-ease + past-duration); `npm test` GREEN. `node tests/playtest.mjs` could not complete locally — headless Chrome/puppeteer **stalls at browser launch on this machine** (env, not the change; a clean-tree run with my change STASHED stalled identically) and intermittently hits the known **#107 perf-counter-unpopulated flake (drawCalls=0)** — but the change is **pure unit-test code with ZERO src/render impact**, so the browser gate behaviour is unchanged; CI runs the same playtest in a clean env (but **no CI fires for a tests-only push** — workflow `paths:` is `src/**`). Live site HTTP 200 (unchanged at the prior build v0.0.20260628135819). Not a visual change → **no gallery shot**. NOTE: the live 0.05 cap discards frame-overflow (no accumulator), so over equal *wall-clock* a laggy frame advances less sim-time — that's **#36's** accumulator to fix; noted, not in scope. Refused/flag note: nil — clean slice, no injection encountered. **Follow-ups:** the issue's 2nd invariant — run `deserialize(serialize(state))` at each replay tick and assert byte-identical continuation (needs wiring the full ship-state codec, not the replay's simplified pure replica) → the next slice on #131; the #36 fixed-timestep accumulator this de-risks. |
| 80 | **#131 — [DL#5] Harden the golden-replay gate: save-round-trip-survives-replay (the 2nd invariant — #131 now CLOSED).** The frozen corpus (#122) proves OLD blobs migrate forward and the golden replay proves the LIVE state sequence — but neither caught a **newly-added runtime field the codec forgets to persist** (silent loss on the *next* save, not at a version bump — the exact pre-#122 class). This wires the **REAL `src/save.js` `serialize`/`deserialize` codec** (NOT the replay's simplified replica) over the evolving replay state in `tests/unit/reactive-loop-replay.test.mjs`: at **every tick** of the seeded golden run, `deserialize(serialize(state))` must round-trip **byte-identical** AND every live persistent field returns intact. The assertion iterates the live state's **own keys** (minus a transient `docked` allowlist) — so adding a saveable field and forgetting to teach the codec trips the gate HERE, not in a player's vanished progress ("every new field is persisted" → enforced). Adds a **mid-run resume-parity** test (reload at every tick → events/purse/Ballad/objective byte-identical, proving a save is a true resume not a silent reset) and a focused guard on the **newest v14 contest** sub-object (#133 rival + soft-clock survive a save). `makeVoyage`'s state grew the full saveable shape (helm heading/speed/throttle/pos + non-zero infamy/standing) so every field carries a real value through the trip. **Mutation-verified the guard is real:** FAILS on a zeroed `standing`, on a dropped contest sub-object, and on a truncated 2-coord `pos`; PASSES clean on revert. No codec bug uncovered — `save.js` round-trips cleanly. | #131 (**CLOSED** — both invariants now on the fixture; DoD met) | _none — test-only (tests/**), no release/version fires (workflow `paths:` is `src/**`); pinned by commit e5dab33_ | **763 unit tests** (+3 in `tests/unit/reactive-loop-replay.test.mjs`: per-tick byte-identical round-trip over the golden spine · mid-run reload resume-parity · v14 contested-objective survives mid-soft-clock); `npm test` GREEN. `node tests/playtest.mjs` attempted ONCE → could not complete: headless Chrome **crashed under the machine's heavy load** (`Target closed`; env, not the change — the brief flagged this) — but the change is **pure unit-test code with ZERO src/render impact**, so the browser gate behaviour is unchanged and **no CI fires for a tests-only push** (workflow `paths:` is `src/**`). Live site HTTP 200 (unchanged at v0.0.20260628135819). Not a visual change → **no gallery shot**. Refused/flag note: nil — clean slice, no injection encountered. NOTE: #131 was already marked CLOSED on GitHub (15:04Z, pre-existing) — added a completion comment recording the DoD; both invariants are now real on the fixture. CREATIVE SPARK (QA + Tech Lead): the gate now writes the save and reads it back at *every* heartbeat of the golden voyage — quit mid-chase and you wake to the exact same wake, the rival's clock and all. **Follow-ups:** the #36 fixed-timestep accumulator the parity half de-risks (still open); the live 0.05 cap's discarded frame-overflow is #36's to fix. |
| 81 | **#124 — [DL#4] Crew morale/loyalty — a reactive meter fed by your CHOICES (CLOSES #124; DL #1's "earned mutiny", never built).** The Captain's Ledger (#45) tracks how the WORLD sees you; this tracks how your own CREW sees you — a single PURE, clamped, deterministic **0..100 loyalty meter** moved ONLY by the deeds you choose, **non-combat** (battle **#100 stays owner-held**). **Rises** on a rescue (#125 +12), a rumour win (#112 +6), home-port growth (#118 +8); **falls** on cruelty — plunder leaving a crew to a cold row (#125 −10) — a reckless grounding (#76 −8), and a missed rescue you sailed past (#125 despawn −5). Two FELT, **edge-triggered** thresholds ring once on the downward CROSSING (never every frame, never on recovery): a **low-morale GRUMBLE** (≤30) and a very-low **MUTINY-RISK WARNING** (≤12) — the *warning*, not the mutiny itself (consequence filed). Makes who-you-are COST something: the sea may fear your black flag, but the men at the capstan answer to whether you're worth following. TDD-first PURE core in `src/systems/morale.js` (`clampMorale`/`sanitizeMorale` fail-open → START 60, never NaN · `moraleDelta`/`applyMorale` · `isLow`/`atMutinyRisk`/`moraleTier` · edge-triggered `moraleBeat` + grumble/mutiny line pools). `main.js` thin — `applyMoraleEvent` funnels all six real wired events through the model + persistence and flashes the beat toast; `window.__tidewake.state.morale`/`.moraleTier` + `tw.driveMorale(event)` QA surface. **Save v14→v15** (morale additive + fail-open: junk/absent → START baseline, so an older save boards with a willing crew; migration step + frozen v14 corpus blob per the #122 rule). CREATIVE SPARK (Game Designer): a crew is a mirror you can't flag your way past — they tell you in a grumble first and a harder word after. | #124 (**CLOSED**) | v0.0.20260628152909 | **776 unit tests** (+11 TDD in `tests/unit/morale.test.mjs`: rises/falls on the right events · clamp both ends · fail-open junk · isLow/atMutinyRisk · tiers · grumble/mutiny edge-crossings + no-refire/no-recovery-fire · a realistic slump drives a fresh crew through grumble→mutiny; +corpus v14 blob); `npm test` GREEN. `node tests/playtest.mjs` attempted ONCE → could not complete locally: headless Chrome **crashed under the machine's heavy load** (`Target closed`; env, not the change — the brief flagged this) — but **CI's headless play-test gate PASSED** in the clean env (logic-first slice, correctness rests on `npm test`). CI green, live HTTP 200 at v0.0.20260628152909. Visual surface is toast-only/tiny → **no gallery shot** (per brief; can't screenshot-verify under load). Refused/flag note: nil — clean slice, no injection encountered. **Follow-ups:** full mutiny CONSEQUENCE (desertion / a turned crew); richer inputs (plunder split, rationing, time at sea, fair-dealing sale streak); a loyal-crew sailing/boarding BONUS; a warm-banter pool that brightens as morale climbs; a small HUD meter once index.html grows a slot. |
| 82 | **#90 — Richer Ballad composition: weave crew-morale into the song + a dominant-pole closing couplet (deepens #78; PURE-LOGIC).** The #78 balladeer already sang every deed type the game emits *except* the one shipped last loop — crew morale (#124). This slice closes that gap and enriches the overall composition. **New `morale` deed type** in `src/voyage-log.js`: a loyalty CROSSING the crew remembers, with a **3-variant grumble (`low`)** + **3-variant mutiny-risk (`mutiny`)** verse pool selected by `e.tier` (ahead of the type-keyed NARRATORS, like the encounter rescue/plunder branch); **non-deduped** — a crew can slump, be won back, and slump again, each its own anecdote. **Richer composition:** a **closing couplet reflecting your dominant pole** — `poleLean()` weighs the pirate road (raids/false-colours/plunder/feared crown) vs the governor road (rescues/home-port/lawful hunt/governorship); a clear lean closes on its own warm-witty couplet, a tie on the "thoroughly both" line, appended after the tally + before the footer. **Thin verse pools rounded out to 3 deterministic variants** each (legend, harbour, governorship, treachery, lawful, capture) so a long voyage reads varied — uniform pool size also cleans the `seen[type] % len` indexing. `main.js` thin: a one-line hook in `applyMoraleEvent` logs `{type:'morale',tier:beat.tier}` on the edge-triggered beat the morale system already rings. **Save-safe with NO version bump** — additive to the existing `voyageLog` array; unknown event types already fail open in `sanitizeLog`, so an older client just drops a morale verse (never a wipe). CREATIVE SPARK (Game Designer + Musician): the ballad now sings the cost of who you became — the crew's grumble, the near-mutiny you talked down, and a final couplet that names your pole and grins about it. | #90 (**kept OPEN** — partial: share-as-image + best-trade/coin-milestone/rank/ports deed types + the "daily voyage" tie-in remain) | v0.0.20260628155021 | **783 unit tests** (+7 TDD in `tests/unit/voyage-log.test.mjs`: morale type accepts low/mutiny only · distinct grumble vs mutiny verses + non-dedup · pirate vs governor closing couplet · no-couplet on empty log · capture/lawful/treachery each cycle 3 distinct verses); `npm test` GREEN. `node tests/playtest.mjs` attempted ONCE → env-stalled (`Target closed`, headless Chrome crashed under heavy machine load; env, not the change — brief flagged this) → relied on unit tests + the **CI headless gate which PASSED** in the clean env. CI green, live HTTP 200. Text-only flourish → no gallery shot. Injection: none encountered. **Follow-ups:** share-as-image PNG render (next #90 sub-slice, needs canvas/visual verify); economy/ladder-hook deed types (best trade, coin milestones, rank climbed, ports visited); seeded "daily voyage" shareable ballad; an upward morale "rally" verse once the system rings a recovery edge. |
| 83 | **#116 — Diegetic feedback for the reactive loop: listen/approach/payoff/loss cues (DL#4, audio).** The rumour→chase→reward loop is the studio's #1 product signal but landed SILENT. Adopted + finished a prior-run **pure `src/systems/loop-cues.js`** (the cue VOCABULARY + selection: `selectCue`/`payoffCueName`/`approachCrossed`/`APPROACH_RADIUS`/`LOOP_CUE_NAMES` — browser-free, fail-open, returns recipe COPIES) and wired it through the existing music bus: **LISTEN** (soft cup-an-ear rising lean-in as the room leaks word) ▸ **APPROACH** (a hopeful rising fifth→octave horizon nod, EDGE-triggered on crossing inward through r=200 toward the chased pin — inside the port-bed horizon 260, outside dock 90) ▸ **PAYOFF** (a satisfied diatonic D-major flourish that lands HOME) ▸ **LOSS** (a sour, lowpassed, slightly-flat chromatic tritone droop when a rival CLAIMED it first, #133). PAYOFF/LOSS are deliberately OPPOSITE in colour (bright diatonic vs chromatic-sour) so the ear knows win vs wake. `music.loopCue(name)` resolves the recipe + arms it for the **next bar downbeat** (quantised like the landfall stinger), `voiceLoopCue` renders it through `musicGain` (existing mute covers them). `main.js` thin: `onListen` town callback → listen cue; a new `loop-cue-approach` system (order 46) edge-detects the crossing; payoff/loss fire in `onArrive` via `payoffCueName({claimed})`. `window.__tidewake.loopCue` exposes the last-armed cue NAME so the headless playtest asserts the beats without an AudioContext. CREATIVE SPARK (Musician + Sound Engineer): the loop now SINGS — a curious lean-in, a hopeful nod onward, a flourish home, or a wry sour stab for a rival's wake. | #116 **CLOSED** | v0.0.20260629210608 | **801 unit tests** (+9 TDD in `tests/unit/loop-cues.test.mjs`: cue names/order · each recipe renderable + modest gain + envelope · junk→null · returns a COPY · claimed→loss vs win→payoff (fail-open) · payoff diatonic vs loss chromatic · approachCrossed edge-only + non-finite-safe · radius within horizon/dock); `npm test` GREEN. `node tests/playtest.mjs` **✓ PASSED** locally (+ extended the rumour-chase + contested scenarios to assert listen→approach→payoff and the contested-lose LOSS cue end-to-end), zero console errors, perf 48 draws/90k tris. CI green, live HTTP 200. Audio slice → no gallery shot. Injection: none encountered. **Follow-ups (queue):** richer per-rumour-kind interaction SFX, a coin-chime layered under payoff, a distinct rival-sail-sighted sting, a continuous wake/helm water-bed SFX. |
| 84 | **#134 — Your Harbour, threatened: the home port acquires a STAKE (DL#5, design/product).** The two reputation poles were parallel tracks with NO downside — you could drift both needles freely. This prices committing to a pole while you hold a claimed home port: come home under a hard **Infamy** lean and your notoriety draws a **navy BLOCKADE** off your own quay; under a hard **Standing** lean the prosperous lawful harbour you raised invites a **pirate RAID**. New **pure `src/systems/harbour-threat.js`** (browser-free, fail-open, TDD'd first): `assessThreat({harbour,infamy,standing})` triggers off the **#132 reputation needle** (`needleTarget`/`needleTier` ≥ tier 1 — a balanced captain at centre is SAFE) + a claimed harbour; `threatDemand(level,tier)` scales tribute with the port you built (+surcharge at tier 2); `sanitizeThreat` for the save; and the **lightweight NON-BATTLE resolution** (the brief's choice/dice beat, NOT battle #100): `payTribute` (certain, costs the demanded coin) or `resolveStandFirm({threat,harbour,roll})` — a **seeded dice** beat whose odds climb with how grown (defensible) the port is (`standFirmOdds`); win → repel for +Standing, harbour intact; lose → `demoteHarbour` (new in home-port.js) sacks it a level (losing that tier's invested coin) or overruns a claimed berth outright, and costs Standing. **Save v15→v16** (the standing #122 rule: migration step 15 added + a frozen **v15 corpus blob** added + `sanitizeThreat` reader, additive/fail-open → a gathering threat survives a reload). Surfaced via the **#105 ashore digest** (warns FIRST if you cast off leaving it unresolved) + a landfall banner + an alarm-red **town panel** with the two resolution planks. `main.js` thin: assess in `mode.onChange→TOWN` at home (`checkHarbourThreat`), a seeded `threatRng`, `payHarbourTribute`/`standHarbourFirm` apply the pure result + persist + Ballad + morale, `tw.harbourThreat`/`forceHarbourThreat`/`standHarbourFirm` QA surface. CREATIVE SPARK (Game Designer): the home port stops being a trophy and becomes a thing with a door you must bar — battle's ready-made reason to fight (stakes-in → consequence-out) without implying any #100 design. | #134 **CLOSED** | v0.0.20260629214854 | **846 unit tests** (+26: `tests/unit/harbour-threat.test.mjs` ×23 — trigger off needle+home, no-threat when balanced/unclaimed, blockade vs raid, demand scaling+surcharge, sanitize fail-open, standFirmOdds, payTribute affordability, dice win/lose/boundary/junk-roll-is-loss/no-mutate, the voice; +3 `demoteHarbour` in home-port.test; +1 v15 corpus blob). `npm test` GREEN. `node tests/playtest.mjs` **✓ PASSED** (+ end-to-end threat scenario: balanced=safe, hard-Infamy landfall draws a blockade + panel + planks, pay tribute spends coin & lifts, hard-Standing draws a raid, stand-firm forced WIN earns Standing & keeps the level, forced LOSS sacks a level, a gathering threat survives a save, new voyage clears it), zero console errors, perf 32/130 draws · 90k/150k tris. CI green, live HTTP 200. Gallery: `studio/qa/gallery/v0.0.20260629214854-harbour-threatened.png` (the raid panel + Pay-tribute/Stand-firm planks). Injection: none encountered (the standing ignore-injection preamble had nothing to refuse). **Follow-ups (queue):** the DEFENSIVE engagement when battle #134↔#100 lands (this threat is its reason to exist); richer threat kinds/escalation if ignored across visits; a latent **render-baseline mismatch** surfaced by this slice's at-sea grade churn — day-night #58 captures its sunny restore-baseline at boot (`0xbfe8e6`) while the #126 reputation grade captures a hair-different sunny baseline at first frame, so a grade tint+restore at sea leaves the scene a hair off day-night's baseline (imperceptible; isolated in the playtest, worth a one-line source fix). |
| 85 | **#135 — Battle Mode shell: the DELIBERATE fight stance (from-owner P1, slice 1 of the owner's Option-2→4 battle lane).** Until now a fight was only ever REACTIVE — 'g' instantly runs out the guns (the turn-based #59 exchange). This adds the Sid Meier's Pirates! "give battle" beat the owner asked for: press **E** to square up to the nearest sail → the world flips to a HELD **BATTLE** stance on the **#95** mode infra (not just derived from firing), the **camera swings to a quarter-view** (pulled astern + to the ship's quarter + lifted, a three-quarter framing), a **`⚔ BATTLE — <foe>` banner** drops with an on-tone engage line, the bed settles to **battle music (#94)**, **NPCs keep sailing underneath** (the world keeps living), and **FLEE (E again) is always available** (commitment that's never a trap). The existing cannonade (#59) is the TEETH inside the stance — fight resolved there ends the engagement (`cannons.onEnd→battle.end`); a new voyage clears it. New **pure `src/systems/battle.js`** (browser-free, TDD'd first): `createBattle` engage/flee/end lifecycle + `quarterViewPos(shipXZ,heading,opts)` camera geometry + on-tone engage/flee line pools; mirrors createCannons/createDuel so the combat paths are siblings. `main.js` stays thin — mode holds BATTLE while `battle.active OR fighting`; the E key verb (engage when SAILING + a sail in range, flee when engaged & not mid-cannonade); a `battle-camera` system (order 52, `when` engaged) eases to the quarter-view; `tw.battle`/`engageBattle`/`fleeBattle` QA hook. index.html: E verb added to the controls legend + challenge prompt. **NO save-schema change** — the stance is transient (stays **v16**). | #135 (**kept OPEN** — epic; slice 1 shipped, slices 2–5 + Option 4 remain; commented with shipped/remains breakdown) | v0.0.20260630054448 | **832 unit tests** (+12 TDD in `tests/unit/battle.test.mjs`: quarterViewPos astern/starboard/lift + heading-rotation + stable stand-off · engage in-range/no-op-out-of-range/idempotent · flee always-ends/no-op-when-idle · end() no-flee-flourish · onEnter foe name · throwing subscriber never corrupts · line pickers); `npm test` GREEN. `node tests/playtest.mjs` **✓ PASSED in CI** (new section: autopilot to a sail → engageBattle → assert mode='battle' + battle.active → world-keeps-living (NPC still moves under the stance) → fleeBattle → assert mode back to 'sailing'), zero console errors. _(Local play-test reports perf drawCalls=0 — a known swiftshader counter-readback quirk on THIS Mac that also fails on clean `main`; CI/Linux populates it and the gate passed.)_ Perf unchanged: **48 draws / 90k tris** (the quarter-view repositions the existing camera → zero new draws). CI green, live HTTP 200 at v0.0.20260630054448. Gallery: `studio/qa/gallery/battle-mode-shell-135.png` (the quarter-view + `⚔ BATTLE — the Black Gannet` banner with the Flee hint). Refused/flag note: nil — clean slice, the standing ignore-injection preamble had nothing to refuse. CREATIVE SPARK (Game Designer): a battle isn't a menu you fall into — it's a CHOICE OF STANCE you commit to, with flight always on the table; the quarter-view is the drama, the helm pulling back so you finally SEE your ship and your foe square off across the swell. **Follow-ups (queue):** slice 2 real-time broadside (steer for a beam angle + manual Fire) · slice 3 workshop loadouts + mid-combat ammo cycle (#96) · slice 4 boarding→crew brawl→verbal duel (capture=Standing/sink=Infamy) · slice 5 expanded 50+ insult duel · then Option 4 phase-coupling; shell-local: a persistent battle HUD panel once index.html grows a slot · an arena-spawn so the engaged foe is a real maneuvering target (today reuses the open-sea NPC) · the flee-vs-flee nuance mid-cannonade. |
| 86 | **#146 — Port/town view clips on small/mobile viewports (from-owner P1, Telegram 2026-06-30 07:35; FILED the issue as step 1, labelled from-owner).** Owner: "Port views … often have sizing issue … the content of the window can be scrollable. But I want a better UI with less scroll but more navigation … Try to simulate mobile viewports too in automated and/or QA tests." (No screenshot retrievable — reproduced the clip by simulating phone viewports.) The `#town` view was a flat stack with a fixed `max-height: min(88vh,720px)` and **no scroll region** (default `overflow:visible`), so on a phone the quayside content (harbour + #134 threat + #103 tavern + full market) spilled past the panel box and the **⚓ Set Sail** plank slid off-screen — unreachable. **Quick fix shipped:** wrapped the growable content in a scrollable **`.town-scroll`** flex-body (`flex:1 1 auto; min-height:0; overflow-y:auto; overscroll-contain; momentum scroll`) while **Set Sail + help stay a PINNED footer outside it** (mirrors the settings-panel pattern), so the gangway home is always on screen; `#town` now `overflow:hidden` (clip corners) + caps to `calc(100vh − 24px − safe-area insets)`; responsive padding/width at `max-width:560px` and `max-height:560px`. **Standing MOBILE-VIEWPORT guard** (the owner's explicit process ask) added to `tests/playtest.mjs`: makes landfall + listens (content-heavy) at **390×844 · 360×640 · 844×390** and asserts the `.town-scroll` body exists, the panel box fits the viewport, the body scrolls to its end, and Set Sail stays on screen + within the panel. `tests/unit/town-layout.test.mjs` (+3 TDD, fake-DOM) locks the scroll-body + pinned-footer structure via render() markup. | #146 (**CLOSED**) — follow-up design **[OWNER-DECISION] #147** filed OPEN | v0.0.20260630060146 | **838 unit tests** (+3 TDD `town-layout`); `npm test` GREEN (835→838). `node tests/playtest.mjs` **✓ PASSED in CI** with the new mobile-viewport section (Linux populates the renderer counters); _local play-test reports the known swiftshader `drawCalls=0` counter quirk — verified it fails IDENTICALLY on a STASHED clean `main`, so it is the machine, not this change (CSS/DOM-only, zero src render impact)._ Perf unchanged (no new draws). CI green, live HTTP 200 at v0.0.20260630060146. Gallery: `studio/qa/gallery/port-view-mobile-146.png` (the full port view fitting a 390×844 phone, Set Sail visible, nothing clipped). Refused/flag note: nil — the standing ignore-injection preamble had nothing to refuse; the embedded "less scroll/more navigation" line is a genuine owner ask, routed to an owner-decision (#147), NOT auto-built. CREATIVE SPARK (Graphic Designer): a quayside you can always step off of — the harbour can grow as busy as it likes, but the gangway back to your ship never slips under the fold. **Follow-ups:** **#147** the richer redesign (A segmented tabs / B accordion / C quayside hub + drill-in) — owner picks; a deterministic mobile-pixel baseline diff for the port view; apply the scroll-body/pinned-footer pattern to any future tall panel. |
| 87 | **#135 — Real-time broadside: slice 2 of the owner's Option-2 battle lane (from-owner P1, epic).** Slice 1 gave the deliberate BATTLE *stance* but its teeth were still the turn-based #59 exchange. This makes the in-stance fight REAL-TIME and positional: inside the stance the **helm now stays LIVE** (TOWN and a reactive cannonade still pause), so you **steer to bring the foe ABEAM** and press **SPACE** to discharge the loaded guns. A clean beam shot bites hard (damage scales with how dead-abeam she sits); firing wide flies past; the guns **reload on the sim clock** between volleys (≈2.2s) so it's maneuver→line up→FIRE→come about, never a button-mash. Sinking pays **Infamy**, a crew that breaks pays **Standing** (capture) — reusing `cannons.js`'s damage/morale model. The turn-based #59 exchange is **kept untouched** as the alternate verb (still 'g'). New **pure helpers, TDD'd first:** `resolveBroadside` (cannons.js — quality-scaled volley + foe reply + the same crewShock/strikesColours morale/yield) and `broadsideAim` (battle.js — beam-arc geometry: forward/starboard dot → quality 0..1 + side + inArc). `createBattle` extended with a real-time engagement (player/enemy hull, morale, reload), `tick(dt)`, `fire()`, `aim()`. `main.js` stays thin: a `deliberateStance` flag keeps the helm live in the stance only, a `battle-reload` system ticks the guns, **SPACE** fires (preventDefault), a `hud-battle` panel (#275) draws two hull bars + a live ABEAM/reload cue + the Fire prompt, and `tw.battleFire`/`tw.battleAim` + `state.paused` join the QA surface. CREATIVE SPARK (Game Designer): the broadside makes POSITION the verb — the bosun calls "lay her alongside!", the panel lights gold when she's abeam, and a wide shot earns a wry "Wide! Bring her round." **NO save-schema change** — the stance is transient (stays **v16**). | #135 (**kept OPEN** — epic; slices 1–2 shipped, slices 3–5 + Option 4 remain; commented with shipped/remains breakdown) | v0.0.20260630073159 | **848 unit tests** (+13 TDD: `resolveBroadside` clean/wide/clamp/scale in cannons.test; `broadsideAim` abeam-starboard/port/dead-ahead + engage-loads-guns + fire damages & sets reload + tick reloads + repeated-fire-sinks-to-win + wide-shot-burns-load + fire-noop-when-idle in battle.test); `npm test` GREEN (835→848). `node tests/playtest.mjs` **✓ PASSED** — new 2b4 section: autopilot to a sail → engageBattle → assert helm LIVE (`state.paused`=false) → steer the foe abeam → fire in real time → assert hull damaged + result **"win"** (broadside diag: engaged✓ helmLive✓ sawAbeam✓ 4 shots, sank her). _(Local play-test perf flakes `drawCalls=0` — the known swiftshader counter quirk on THIS Mac that also fails on clean `main`; one local run DID populate it and passed, and CI/Linux populates it reliably.)_ Perf unchanged: **48 draws / 90k tris** (battle is HUD + camera only → zero new draws; leak-invariant worstTransition still 48/90390). CI green, live HTTP 200 at v0.0.20260630073159. Gallery: `studio/qa/gallery/battle-broadside-135.png` (the Broadside panel vs *the Black Gannet* — Your hull 80 / Their hull 79 after a real-time volley, the steer-to-abeam cue + Space-fires controls). Refused/flag note: nil — the standing ignore-injection preamble had nothing to refuse. **Follow-ups (queue):** slice 3 workshop loadouts + mid-combat ammo cycle (#96) · slice 4 boarding→crew brawl→verbal duel (capture=Standing/sink=Infamy) · slice 5 expanded 50+ insult duel · then Option 4 phase-coupling; slice-local: an arena-spawn so the engaged foe is a dedicated maneuvering target (still reuses the open-sea NPC); false-colours/letters-of-marque reward nuances on the real-time path (today the basic spoils); a subtle "fire wide while reloading" lockout polish. |
| 88 | **#135 — Workshop loadouts + mid-combat shot cycle: slice 3 of the owner's Option-2 battle lane (from-owner P1, epic; ties town #96).** Slice 2 gave the real-time broadside, but every volley was the same iron ball. This adds the choice the owner asked for: you **FIT shot at a town Gunner's Workshop** (no buying mid-combat), then **cycle the LOADED shot with one key (X) mid-fight** — each with a **distinct effect on the broadside**. New **pure `src/systems/ammo.js`** (browser-free, TDD'd first): 6 shot profiles — **round** (balanced workhorse), **chain** (shreds rigging: low hull, weak reply via `returnMult` + the `'rigging'` crewShock), **grape** (sweeps the deck: tiny hull, `moraleMult` 2.4 → fast capture), **light** (forgiving arc via `aimForgive` 0.35), **heavy** (double-shotted: `hullMult` 1.7, slow `reloadMult` 1.4, heavier reply), **swivel** (quick-firing `reloadMult` 0.55) — each mechanically distinct; plus `cycleAmmo`/`fitAmmo`/`defaultLoadout`/`ammoProfile` loadout helpers (round can never be unfit). `cannons.resolveBroadside` extended to take an **optional `ammo` profile**; **omitting it is BYTE-IDENTICAL to a round shot** (a `NEUTRAL_AMMO` default + a regression test), so the slice-2 path is untouched. `battle.js` owns the loaded shot: `engage` loads the first fitted shot, `cycleShot()` walks the fitted locker (wraps), `fire()` applies the profile + a per-shot reload; `snapshot()` carries `ammo`/`ammoProfile`/`loadout`. `main.js` stays thin: **session-scoped `state.loadout`** (a transient combat fit, **NOT persisted** → save stays **v16**, no migration/corpus churn), the **X** cycle key (`c` was already False Colours), workshop wiring (`onFitAmmo`), QA hooks (`battleCycleShot`/`loadout`/`ammoCatalogue`/`fitAmmoType`). `town.js`: **⚒ The Gunner's Workshop** panel (fit/unfit each shot, round always fitted) — the first ashore activity beyond the market the #96 town-mode brief left "room to grow" for. `hud.js`: a **loaded-shot chip** in the battle panel (tap or X to cycle). CREATIVE SPARK (Game Designer): a broadside becomes a QUESTION — sink her (round/heavy → Infamy) or scare her colours down (chain+grape → Standing), hard-and-slow or quick-and-forgiving — switched on the fly as the fight turns. | #135 (**kept OPEN** — epic; slices 1–3 shipped, slices 4–5 + Option 4 remain; commented with shipped/remains breakdown) | v0.0.20260630210414 | **873 unit tests** (+25 TDD: `tests/unit/ammo.test.mjs` ×18 — catalogue completeness · distinct-fingerprint · round-neutral · fantasy ordering · cycle wrap/not-in-loadout/single/garbage · fit toggle/round-locked/canonical-order/unknown-id · resolveBroadside default==round + chain-less-hull-weaker-reply + heavy-more-hull + grape-more-morale + light-forgives-angle; +7 in `battle.test.mjs` — engage loads first fitted/defaults round · cycleShot wraps + no-op-when-idle + onCycleAmmo · loaded shot changes the bite · cycle doesn't reset reload); `npm test` GREEN (848→873). `node tests/playtest.mjs` **✓ PASSED** — new 2b5 section: fit grape at the workshop (round stays locked) → engage loads round → X walks chain/grape and wraps → assert the loaded shot carries a broadside-effect profile. Perf **32/130 draws · 90k/150k tris** (zero new draws — pure logic + HUD text + DOM). CI green, live HTTP 200 at v0.0.20260630210414. Gallery: `studio/qa/gallery/workshop-loadout-135.png` (the 6-shot workshop with fit toggles) + `studio/qa/gallery/ammo-cycle-135.png` (the battle HUD's `🎱 Chain shot · Cripples rigging (X)` chip + ABEAM cue). Refused/flag note: nil — the standing ignore-injection preamble had nothing to refuse. **Follow-ups (queue):** slice 4 boarding→crew brawl→verbal duel (capture=Standing/sink=Infamy, early strike-colours→ransom) · slice 5 expanded 50+ insult duel · then Option 4 phase-coupling; slice-local: **PERSIST the loadout** + a coin-priced BUY economy at the workshop (would bump save v16→v17 + the #122 migrate-all-forward + frozen-corpus rule — deliberately deferred to keep this increment smallest); a per-shot reload/aim HUD readout; an arena-spawn so the engaged foe is a dedicated maneuvering target. |
| 89 | **#135 — Boarding → crew brawl → verbal captain duel: slice 4 of the owner's Option-2 battle lane (from-owner P1, epic).** Slices 1–3 gave the deliberate stance, real-time broadside and shot loadouts; this adds the **finisher**. Once a foe's hull is beaten to **≤30%** a gold, pulsing **⚔ BOARD HER! (F)** prompt lights — **F** (or tap) sends the crew over the rail for a quick **auto crew brawl** (crew × morale × loadout, 2–3 original comic lines), which **hands off to the existing verbal captain's duel (#33 — reused, the climax)**. Winning the boarded duel is a **CAPTURE**: you take her deck intact → it pays the **governor pole (Standing)** on top of the Infamy; sinking her with the broadside stays **pure Infamy** — so the owner's **capture=Standing / sink=Infamy** fork is now real, decided by *how* you finish her. The brawl is crew × morale × loadout (your #124 crew nerve drives it; a deck-sweeping shot — grape/swivel — boards with an edge; a runaway brawl **softens the captain** with an opening morale dent, clamped so the duel is always still a real fight); a close/lost brawl still carries through (it's the comic *bridge*; the duel is the *decider*). New **pure `src/systems/board.js`** TDD'd first: `canBoard` (≤30% hull) · `resolveBrawl` (→ won/advantage/margin + 2–3 anti-repeat comic lines) · `brawlMoraleDent`. `battle.js`: `canBoard()`/`board()` controller + `onBoard` hand-off; snapshot carries `canBoard`/`boarded`/`brawl`. `duel.js`: `tryChallenge({openingDent, boarded})` — **backward-compatible** (the open-sea hail #79/#91 callers are byte-identical). `main.js` stays thin: **F** boards in-stance, the hand-off opens the captain's duel, a boarded win pays the capture Standing + logs a `captured/boarded` deed (Ballad #78 + port-memory #104b). `hud.js`+`index.html`: the pulsing Board! prompt + a tap-to-board button. CREATIVE SPARK (Game Designer): the fight you won with iron you must FINISH with wit — cutlasses, a soup-ladle boarding axe and a fainting parrot, then it all narrows to two captains trading insults across a wrecked deck. **NO save-schema change** — boarding is transient combat state (stays **v16**). | #135 (**kept OPEN** — epic; slices 1–4 shipped, slice 5 + Option 4 remain; commented with shipped/remains breakdown) | v0.0.20260630213520 | **890 unit tests** (+17 TDD: `board.test.mjs` ×10 — canBoard threshold/junk-safe · resolveBrawl won/lost/loadout-edge/2–3-distinct-lines/junk-safe · brawlMoraleDent clamp; `boarding-duel.test.mjs` ×3 — plain hail un-boarded@full-morale · boarding opens boarded+dented · dent clamped ≥0; +4 in `battle.test.mjs` — canBoard gates on hull/engaged/already-boarded · board no-op-unless-beaten · board resolves brawl + hands off); `npm test` GREEN (873→890). `node tests/playtest.mjs` **✓ PASSED in CI** — new 2b6 section: engage → `battleWeaken` to the board window → assert Board! lights (canBoard false@full, true@≤30%) → board → assert brawl narrates ≥2 lines + the stance ENDED + the captain duel OPENED (`boarded`) → win the duel → assert a **captured prize paid Standing** (+70 in the run). _(Local play-test perf still flakes `drawCalls=0` ~half the runs — the known swiftshader counter quirk on THIS Mac; CI/Linux populates it reliably and PASSED.)_ Perf **32/130 draws · 90k/150k tris** (zero new draws — pure logic + HUD text + CSS). CI green, live HTTP 200 at v0.0.20260630213520. Gallery: `studio/qa/gallery/boarding-brawl-135.png` (the gold BOARD HER! prompt, foe hull 25) + `studio/qa/gallery/captain-duel-climax-135.png` (the Insult Broadside climax vs *Three-Pint Pete*). Refused/flag note: nil — the standing ignore-injection preamble had nothing to refuse. **Follow-ups (queue):** slice 5 expanded 50+ insult duel (anti-repeat within an engagement) · then Option 4 phase-coupling (brawl casualties → duel confidence); slice-local (deferred to keep this increment smallest): the **early strike-colours → ransom** short-circuit, a post-duel explicit **sink-or-spare** choice, the brawl casualties actually gating the duel, an arena-spawn for a dedicated boarded target. |
| 90 | **#135 — Expanded verbal captain's duel: slice 5 (FINAL Option-2 slice) of the owner's battle lane (from-owner P1, epic).** Slice 4 made the boarding fork hand off to #33's verbal captain's duel — but that duel was still the 14-line, 5-category stub. This grows it into the climax it deserves. **Content:** **14 → 56 original jab/riposte pairs**, every line authored for Tidewake in the wry pirate voice (a content test rejects any duplicate line, riposte, or id — no recycled copy); **5 → 7 categories**, adding **Superstition** (what a sailor fears at sea — Friday sailings, dead albatrosses, painted bow-eyes) and **Hygiene** (what he smells like ashore — beards with their own ecosystem, socks that could stand a watch), each with a ≥6-line bench. **Anti-repeat selection:** `pickOptions(rng, enemy, n, recent)` PREFERS lines not shown recently; the duel controller keeps **session-scoped memory** — `engagementSeen` (never re-offer a jab *within one fight* while the bench can cover it) + a `DUEL_MEMORY`=18 rolling window *across hails* (back-to-back duels open with a fresh hand). The two HARD guarantees — a weakness/winning line is always offered, the hand is always `n` distinct — ALWAYS win over freshness, falling back to stale lines only when the bench can't satisfy them. `dealOptions()` is the one seam both call sites (open hail + each round) share. **NO save-schema change** — the corpus is static and the anti-repeat history is session-scoped (stays **v16**); `window.__tidewake` preserved. CREATIVE SPARK (Game Designer + Musician): the duel finally has RANGE — a captain can be needled about his cowardice one round, his ship's curse the next, and the reek of his crew the round after, and never hear the same jab twice in a fight. | #135 (**kept OPEN** — from-owner P1 epic; **all 5 Option-2 slices now shipped → core complete, lane-switch gate cleared**; Option 4 phase-coupling remains the later lane; commented with the full shipped/remains read — **closing left to the owner**) | v0.0.20260630215140 | **895 unit tests** (+13 TDD in `tests/unit/duel.test.mjs`: 50+ corpus with distinct line/riposte/id · 7 categories incl. Superstition+Hygiene each ≥6 lines · `pickOptions` skips recent while a fresh bench remains + always offers a sharp line + n distinct · graceful fallback when the whole corpus is recent · a multi-round duel never re-offers a line while the bench covers it · anti-repeat memory carries ACROSS duels in a session); `npm test` GREEN (890→895). `node tests/playtest.mjs` **✓ PASSED** (the existing 2b duel section still wins via the sharp line each round against the expanded pool). Perf **32/130 draws · 90k/150k tris** (text/DOM only — zero new draws). CI green, live HTTP 200 at v0.0.20260630215140. Gallery: `studio/qa/gallery/expanded-duel-135.png` (a fresh exchange vs *Three-Pint Pete* — his pride6 riposte over four jabs incl. a new Superstition line). Refused/flag note: nil — the standing ignore-injection preamble had nothing to refuse. **Follow-ups (queue, all deferred to keep this increment smallest):** the Option-4 lane — early **strike-colours → ransom** short-circuit, a post-duel explicit **sink-or-spare** choice, **brawl casualties → duel confidence** gating, an **arena-spawn** for a dedicated boarded/maneuvering target. |
| 91 | **#132 — Your ship wears your legend: Slice A of the DL #5 reputation-needle (art).** #126 made the *world* mirror the Infamy↔Standing pole; this makes the player's **own ship** — the thing the captain looks at most — mirror the SAME single signed lean (`repLean`) #126 already computes. **Lean infamous** → the canvas greys with salt + powder-grime and the timbers darken weathered/matte; **lean lawful** → the sails brighten to clean trim with a soft warm trim-glow + a cared-for sheen (lower roughness); **balanced** stays the honest working sloop, byte-for-byte untouched (deadzoned at the neutral band like #126). Asymmetric-but-physically-honest on purpose: a colour multiplier over the GLB's `colormap` texture can only DARKEN, so grime reads as darkening (the GLB base roughness is already 1.0 → can't roughen further) while the lawful "clean" reads as a warm tint + emissive glow + sheen. New **pure `src/systems/reputation-aura.js`** TDD'd first (`shipAura(lean)` → per-part sail/hull `{color, roughnessAdd, emissive, emissiveIntensity}` + `auraCommitment`; neutral/junk → the exact untouched identity; bounded + monotone). `main.js` stays thin: clones the hero ship's **sail + hull** materials ONCE (name-matched — GLB `sail-a`/`ship-pirate-small` + procedural fallback `sail`/`hull`, each cloned so tinting never leaks to the founderer or siblings), captures each base roughness, and a new **`ship-aura` registry system (order 122**, right after `reputation-grade`) writes colour/roughness/emissive each frame off `repLean` — **uniform writes only, ZERO new draws**. `window.__tidewake.aura` QA surface (live pole/mag + the ACTUAL applied sail/hull material read straight off the cloned materials) + a `qaFrameShip` hero-framing hook for the gallery. **NO save-schema change** — the cast is *derived* from existing reputation state, nothing persisted (stays **v16**); `window.__tidewake` preserved. CREATIVE SPARK (Graphic Designer + Game Designer): your ship wears your legend — a feared ship looks the part before she fires a shot, a respected one gleams, and a balanced captain sails the honest sloop she always was. | #132 (**REOPENED + kept OPEN** — was prematurely CLOSED on `v0.0.20260628085044` by the *HUD-gauge needle + sting + line* slice, which did NOT build this issue's two stated slices; Slice A now genuinely shipped, **Slice B (harmonic modal recolour of the bed) remains** its own next cycle; commented with the full read) | v0.0.20260630221704 | **902 unit tests** (+7 TDD in `tests/unit/reputation-aura.test.mjs`: neutral identity/junk-safe · `auraCommitment` over [0,MAX_LEAN] · infamy grimes/darkens/roughens no-glow · standing warms/glows/sheens · the two poles distinct · monotone + bounded · end-to-end off the ledger); `npm test` GREEN (895→902). `node tests/playtest.mjs` **✓ PASSED in CI** — new 2j-4 section: fresh = neutral white-multiplier identity no-glow → infamy darkens sail+hull colour + roughens + no glow → standing warms colour + lights the trim glow + sheens → a neutral ledger restores the untouched ship exactly (reads the ACTUAL applied materials). _(Local play-test perf flaked `drawCalls=0` ONCE — the known swiftshader counter quirk on THIS Mac; passed on the immediate re-run and CI/Linux populates it reliably.)_ Perf **32/130 draws · 90422/150k tris** (zero new draws — material clone adds a material, not a draw; colour/roughness/emissive are uniforms, no shader recompile). CI green, live HTTP 200 at v0.0.20260630221704. Galleries: `studio/qa/gallery/ship-aura-infamy-132.png` (the SAME ship, dark grimed canvas + weathered hull, needle leaning pirate, "You are the storm they warn the children about.") + `studio/qa/gallery/ship-aura-standing-132.png` (bright warm-glowing canvas + cared-for hull, needle leaning governor, "The isles would crown you, and gladly."). Refused/flag note: nil — the standing ignore-injection preamble had nothing to refuse. **Follow-ups:** **Slice B** — the harmonic reputation needle (continuous modal recolour of the procedural bed: Infamy→freygish bite / Standing→warm Ionian-Lydian, one cross-faded gain, percussive bed fixed, no `loadTrack`); generalise the hull-material cast into a reusable battle hull-damage state (a #135 bonus, not designed here); a heading-wisp-style diegetic pole flourish. |
| 92 | **#132 — The harmonic reputation needle: Slice B of the DL #5 reputation-needle (audio) — COMPLETES & CLOSES #132.** Slice A cast the SAME signed lean (`repLean`) onto the *ship*; this casts it onto the *score*. A continuous **modal recolour of the procedural bed's lead**, RTPC-style on one knob: **lean Infamy** → the lead leans into a **freygish / phrygian-dominant "bite"** (flat-2 + flat-6 over a major third — the klezmer/Spanish menace colour from the Musician's DL#1 wildcard); **lean Standing** → it brightens to a **warm Lydian voicing** (raised 4th); **balanced** → the honest D-major **Ionian** hornpipe, byte-for-byte (deadzoned — `repLean` is exactly 0 in the neutral band). **One cross-faded gain across the needle:** a parallel recolour lead voice (same melody, voiced in the pole's mode read live from `recolourState.scale`) crossfades up (`recolourGain` → `blend`) while the neutral lead ducks complementarily (`leadBus` → `1-blend`, so a wisp of the honest theme always survives) — both under `seaLayerGain` → `musicGain`, so the **mute AND the #94 port-duck both cover it**. The **bass + chord / percussive bed stay FIXED** in D major (the DL#3 trap); the three modes share root/third/fifth/octave → phase-coherent over that fixed bed. No `loadTrack` assets. New **pure `src/systems/harmonic-mood.js`** TDD'd first (`harmonicMood(lean)` → `{pole,blend,scale}` + `recolourBlend`; mirrors Slice A's `auraCommitment` — same needle curve; neutral/junk → the untouched Ionian bed; bounded + monotone). `music.js`: imports the map, adds a `setMood()` (stores the cast headless-safe, then smoothly `setTargetAtTime`-glides the crossfade once the engine is up), a `recolourGain`/`recolourBus` pair built silent in `start()`, the recolour voice rendered in `fireStep` only when audible (cheap when neutral), and a `mood()` QA getter; `update()` drives it from `state.lean`. `main.js` stays thin: the `music` registry system (order 310, after `reputation-grade`@120 so `repLean` is fresh) passes `lean: repLean`; `window.__tidewake.harmony` exposes the live `{pole,blend,scale}`. **NO save-schema change** — derived from reputation state, nothing persisted (stays **v16**); `window.__tidewake` preserved. CREATIVE SPARK (Musician + Sound Engineer): "the score wears your legend" — you don't hear a *new* song when you turn pirate, you hear the *same* hornpipe turn on you (a flat-2 sob under a cloak); turn honoured and a lifted 4th opens the air like a church window. | #132 (**CLOSED — completed**; both Slice A (art) + Slice B (audio) now shipped → the issue's full stated scope is delivered) | v0.0.20260630223335 | **911 unit tests** (+9 TDD in `tests/unit/harmonic-mood.test.mjs`: neutral/junk → Ionian bed · IONIAN ≡ `music.js` MAJOR_SCALE · three modes share root/3rd/5th/octave · FREYGISH flat-2/maj-3/flat-6/flat-7 · LYDIAN raised-4th-only over Ionian · pirate→freygish / governor→lydian · `recolourBlend` over [0,MAX_LEAN]→[0,RECOLOUR_MAX] bounded · monotone crossfade · rides the real deadzoned needle); `npm test` GREEN (902→911). `node tests/playtest.mjs` **✓ PASSED** — new 2j-5 section: fresh = neutral Ionian (blend 0) → infamy → freygish bite (scale[1]===1, scale[5]===8, blend>0) → standing → warm Lydian (scale[3]===6, blend>0) → a neutral ledger restores Ionian exactly. **AudioContext-free** in the gate (no gesture → the engine never starts; the recolour cast is set from the lean every frame regardless). Perf **48/130 draws · 90390/150k tris** (audio adds ZERO THREE draws). CI green, live HTTP 200 at v0.0.20260630223335. No gallery — an audio-only slice has no visible change (kept the browser render load down). Refused/flag note: nil — the standing ignore-injection preamble had nothing to refuse. **Follow-ups:** generalise the hull-material cast into a reusable battle hull-damage state (#135 bonus); a per-pole lead *timbre* shift (not just mode) for deeper character; a diegetic pole flourish on a crossing of the neutral band. |
| 93 | **#148 — Per-rumour-kind LISTEN colour + coin-chime under the payoff (#116 follow-up, audio).** #116 shipped four loop-BEAT cues (listen/approach/payoff/loss) and left a reservoir of untickceted follow-ups; this files & ships the smallest always-working slice. **(1) LISTEN takes a colour from the KIND of word the room leaks:** `composeRumours` now tags each surfaced entry with its pool `kind` (rep/trade/sea/deed); `town.listen` hands the rumours to `onListen`; `main.js` maps `rumours[0].kind` through a new pure `listenCueName(kind)` — a hopeful rising sixth for a **trade tip** (`listenTrade`), the intimate base whisper for a **reputation** line (`listen`), a wary muffled falling step for **danger on the water** (`listenSea`), a warm settle-home for your own **deed** echoing back (`listenDeed`); unknown/first-listen fails open to base `listen`. **(2) A bright coin-chime layers UNDER the PAYOFF** when a tip actually pays coin — pure `coinChimes({claimed,coins})` gates it; `music.loopCue(name,{under:'coin'})` arms a SECOND recipe fired on the SAME bar downbeat under the primary (a glassy octave-up→tenth tinkle). A rival-claimed zero-coin loss does NOT chime. All procedural WebAudio over the existing bus → **mute covers it**; new cues sit under the loop beats in gain. New pure exports in `src/systems/loop-cues.js` (`listenCueName`, `coinChimes`, `LISTEN_CUE_NAMES`, interaction recipes via the same `selectCue`); `LOOP_CUE_NAMES` kept the canonical four. `music.js`: `pendingUnder`/`lastUnderCue` + `lastUnder()`; `main.js`: `window.__tidewake.loopUnderCue` QA surface. **NO save-schema change** — SFX stateless (stays **v16**); `window.__tidewake` preserved. CREATIVE SPARK (Musician + Sound Engineer): the room no longer leans in the same way for every secret — a heading worth chasing pricks the ears up, danger drops the voice; and the prize finally *jingles* when the coin hits the purse. | #148 (**CLOSED** — filed as a #116 follow-up, then shipped same cycle) | v0.0.20260630224900 | **917 unit tests** (+ TDD in `tests/unit/loop-cues.test.mjs`: `listenCueName` per-kind + fail-open · `LISTEN_CUE_NAMES` order · every listen colour + coin chime resolves to a renderable in-key recipe under the beat gain · coin brighter/softer than payoff · `coinChimes` only-honest-paying-win; + `rumours.test.mjs` every entry carries a known `kind`); `npm test` GREEN (911→917). `node tests/playtest.mjs` **✓ PASSED** — the rumour-chase section now accepts any LISTEN-family cue and asserts `tw.loopUnderCue==='coin'` after a paying payoff. **AudioContext-free**. Perf **32/130 draws · 90422/150k tris** (audio adds ZERO THREE draws). CI green, live HTTP 200 at v0.0.20260630224900. No gallery — audio-only, no visible change. Refused/flag note: nil. **Still queued in the #116 reservoir:** a distinct "rival sail sighted" sting + a continuous wake/helm water-bed SFX (speed/turn-rate driven). |
| 94 | **#90 — a "best of voyage" superlative in the Ballad: richest haul + fiercest foe (deepens #78; PURE-LOGIC).** #90's remaining "richer composition" scope explicitly named a "best of voyage" superlative line; this ships it as the smallest always-working increment. The balladeer now crowns the voyage's two PEAKS **by name**, on one line just before the closing tally: the single **richest haul** (most coins off one deed) and the **fiercest foe** (most infamy hard-won in one fight). When **one name both pays AND fights the hardest**, the two boasts MERGE into a single "one name towers over the voyage" line (foe named once, not duplicated). **Skipped gracefully** when nothing was plundered or feared — a voyage of only landfalls, rescues, or grumbles has no peak to crow about, so no line. Peaks are chosen by MAX coins / MAX infamy with **ties to the earliest deed** → fully deterministic (vital for the save text, the share text, and the unit tests). Pure helpers `subjectOf(e)` (foe/ship/port/isle name) + `superlativeLine(events)` in `src/voyage-log.js`, composed from `coins`/`infamy` **already in the log** — no new event types, no `main.js`/game-system hooks. **NO save-schema change** (stays **v16**); `window.__tidewake` untouched (UI is `src/ui/ballad.js`, which already maps `ballad.lines` → `<p class="bal-line">`, so the new line paints for free in the same styling). CREATIVE SPARK (Game Designer + Musician): the raw tallies turn into a tavern boast — every run now has a brag-worthy peak the crew toast by name, the natural hook for the shareable screenshot. | #90 (**kept OPEN** — partial; this delivers the superlative line. **Remains:** share-as-image PNG · economy/ladder deed types (best trade, coin milestones, rank, ports) · seeded "daily voyage" tie-in · further mood/length variation) | v0.0.20260630230030 | **925 unit tests** (+4 TDD in `tests/unit/voyage-log.test.mjs`: crowns richest-haul + fiercest-foe by name before the closing · the merged single-name boast names the foe once · skipped on a coin/infamy-less peaceful voyage · picks the MAX deeds + byte-identical determinism; three existing name-filter tests made position-based — `lines.slice(1, 1+log.length)` — now that the superlative also names a foe); `npm test` GREEN (921→925). `node tests/playtest.mjs` **✓ PASSED** (perf populated: **32/130 draws · 90422/150k tris**; one local run hit the known swiftshader `drawCalls=0` counter quirk — passed clean on re-run, zero render impact from a text-only change). CI green, live HTTP 200 at v0.0.20260630230030. No gallery — a procedural-text line in an existing panel (same `.bal-line` styling, no layout/scene change), fully pinned by unit tests; kept the browser render load down. Refused/flag note: nil — the standing ignore-injection preamble had nothing to refuse. **Follow-ups (queue, all deferred to keep this smallest):** the share-as-image PNG (#78 stretch; needs canvas + a visual verify); the economy/ladder-hook deed types (a best-trade hook in sell, coin milestones, rank rungs, ports visited); the seeded "daily voyage" shareable ballad. |
